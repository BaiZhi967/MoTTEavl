# M1 验证记录（原生 Agent 与通用评分）

记录约定：`result` 只使用 not_run / passed / failed / blocked / not_applicable；
skip 不计 passed；每个层级独立记录。实现分支：`codex/m1-native-agent-evaluation`。
源基线：`440d532`（规划提交）；开工核对基线：`main@440d532`，工作树干净。

环境：macOS 25.3 (arm64)、Python 3.12.11（uv）、Node 24 / pnpm 9.15.0、
Docker 27.4.0（一次性 `postgres:16-alpine` 容器做 PG 验证）。

## 工作包记录

```yaml
- work_package: M1-T01
  goal_ids: [M1-G11]
  implementation_commit: eea9cc5
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/contract/test_observation_v1.py
  environment: 本地 macOS，离线
  evidence: 13 passed；旧 Observation/Score 往返、多指标复合键、NaN/外部引用拒绝、
    evidence_hash 绑定、InvocationRecord 边界。相邻回归 tests/contract + tests/sdk
    400 passed。
  limitations: PG 层在 T03/T10 验证。

- work_package: M1-T02
  goal_ids: [M1-G11, M1-G12]
  implementation_commit: 990f33d
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/evaluators/test_deterministic_matrix.py
  evidence: 9 passed；9 类评分器 success/有效失败/缺证据/异常矩阵；regex 灾难回溯
    可终止（超时 → evaluator_error）；不完整轨迹不判通过。新增依赖 jsonschema 4.26。
  limitations: 无。

- work_package: M1-T03
  goal_ids: [M1-G11, M1-G13, M1-G17]
  implementation_commit: afb9d55
  verification_layer: contract+integration
  result: passed
  command: uv run pytest -q tests/runtime/test_agent_scoring_passes.py
  evidence: SQLite/InMemory 一致；rescore 新 pass、旧 pass 字节不变、Provider spy
    零新增调用；migration 0004（PG）+ SQLite 原地升级；PG 实测见 T10。
  limitations: 无。

- work_package: M1-T05
  goal_ids: [M1-G02, M1-G06, M1-G07]
  implementation_commit: ab46f3e
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/runtime/test_agent_tool_history.py tests/runtime/test_builtin_react.py
  evidence: 16 passed；完整 assistant/tool 历史、重复 call_id 无二次副作用、参数
    schema 拒绝、预算阻断具体原因、无 usage 不虚构硬预算（A13）、取消阻断。
  limitations: 无。

- work_package: M1-T06
  goal_ids: [M1-G04, M1-G10, M1-G14]
  implementation_commit: 7d88bf2
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/sandbox/test_agent_workspace_policy.py
  evidence: 5 passed；穿越/绝对路径/双向 symlink/设备文件拒绝；单文件/总量/文件数
    配额可区分；同名产物跨 Case 隔离；清理失败报残留。
  limitations: 宿主本地目录 + 策略护栏，非容器隔离（已声明）。

- work_package: M1-T04
  goal_ids: [M1-G01, M1-G02, M1-G03, M1-G04]
  implementation_commit: e5d3477（含修复 f3e0dfe 区间的 routing 修正提交）
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/runtime/test_builtin_agent_backend.py
  evidence: 4 passed；registry 注册 builtin-agent@1（wired）；native+supports_tools=false
    创建期拒绝且模型调用数 0（A06）；两 Case workspace/历史隔离；隐藏断言不进模型输入。
  limitations: scripted provider 单元层；HTTP 层在 T10。

- work_package: M1-T07
  goal_ids: [M1-G05, M1-G07, M1-G08, M1-G09]
  implementation_commit: 后续于 T05/T04/T10 三个提交落地（invocation store 随 e5d3477）
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/runtime/test_agent_interruption.py
  evidence: 4 passed；工具副作用后崩溃 → dispatching 开放 → 恢复 needs_review、
    调用数不增加（A09）；瞬时失败不 second-chance；取消后无工具执行（A08）；
    步数耗尽无背景写入。
  limitations: 无。

- work_package: M1-T08
  goal_ids: [M1-G05, M1-G10, M1-G14]
  implementation_commit: 71b25f4（随 T08 提交；见 git log）
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/trace/test_agent_observation_capture.py
  evidence: 3 passed；秘密哨兵不出现在持久事件/调用摘要（A11 部分）；evidence_hash
    绑定与篡改检测；采集失败降 coverage 且评分侧 insufficient（不因未采集判通过）；
    事件截断标记。
  limitations: Artifact 字节为冻结评分输入存原文（hash 绑定），展示层另行脱敏——
    口径差异已在协议文档声明。

- work_package: M1-T09
  goal_ids: [M1-G15]
  implementation_commit: 9f3bdfd
  verification_layer: contract
  result: passed
  command: uv run pytest -q tests/api/test_agent_run_flow.py; pnpm --dir apps/web test -- tests/AgentPages.test.tsx
  evidence: API 4 passed（创建期拒绝/GET 零副作用/rescore 新 pass/artifact 归属/
    retry 语义）；Web 10 passed（空态/禁用原因/错误输入保留/截断/取消/retry/历史 pass
    切换/并列阅读非正式声明）。make openapi 重生成并随代码提交。
  limitations: UI 设计 skills（design-taste-frontend 等）不在本 checkout
    （PR-15 已记录的前置缺口）；按 DESIGN.md 宪法直接实施。

- work_package: M1-T10
  goal_ids: [M1-G16, M1-G17, M1-G18]
  implementation_commit: 本提交
  verification_layer: integration
  result: passed
  command: >
    uv run pytest -q tests/integration/test_native_agent_slice.py;
    MOTTE_PG_DSN=postgresql://postgres:...@127.0.0.1:55432/motte_test
    uv run pytest -q tests/integration/test_native_agent_slice.py
      tests/runtime/test_agent_scoring_passes.py tests/storage/test_postgres_e2e.py
  evidence: >
    本地假 HTTP（OpenAI 兼容形状）+ 真实 API/WorkerLoop/存储：三组代表任务
    （正常产物 / 可恢复错误 / 越权+预算耗尽）闭环，多指标评分与报告正确；
    Direct LLM v1 评分口径与 Replay 回归不变（A01/A02/A05/A15 场景）。
    真实 Docker：一次性 postgres:16-alpine 容器，alembic 0001→0005 从零迁移，
    19 passed（含完整 agent 切片在 PG 上、多指标 pass、平台 e2e）。
  limitations: live（真实 subject 模型）not_run——需操作者授权与预算。
```

## 阶段门禁

- `uv run pytest -q -m "not live"`：**1039 passed, 16 skipped**（本地全量）。
- `uv run ruff check .` / `uv run mypy packages/contracts` / compileall：通过。
- `pnpm --dir apps/web test`：12 files / 170 tests 通过；`pnpm --dir apps/web build` 通过。
- `make check`：见最终提交记录（含 compose config）。
- 15+1 个 skip 为 PG/Docker/live 门控（无 DSN/标记时跳过），不计通过；
  其中 PG 与 Docker 变体已按上述命令实际执行。

## 已知基线现象（未掩盖）

`tests/test_dev.py::test_cleanup_owned_descendants[False]` 在完整套件中偶发
`os.killpg(pid, 0) PermissionError`（进程组 PID 回收竞态），单文件运行 20/20 通过。
该现象在规划期（提交 440d532 前的验证）已两次出现并记录于计划 REVIEW.md；
本次开发中复现 1 次、未复现 2 次，与本次改动无关，未通过跳过/删断言处理。
根因待独立排查。

## M1-Core / M1-Supported 结论

- **M1-Core：满足**。离线（合成 fixture、scripted provider、本地假 HTTP）与
  真实 Docker（一次性 PG 容器 + 迁移 + 完整切片）确定性链路全部通过。
- **M1-Supported：未满足**。需要两个已发布模型在同一文件任务上的真实运行与
  取消证据（live）；未取得前不宣称 M1 正式支持真实 Agent。

## 操作者 live 验收准备（待授权）

```bash
# 预算说明：每 case 每步一次模型调用（native-tool 默认 ≤8 步/case）；
# 费用 = 输入(逐步增长的历史) + 输出(≤模型上限)；无隐性调用。
uv run python -m motte_cli agent-tasks import --file tasks.json --name file-report
uv run python -m motte_cli models publish YOUR_MODEL   # 或 Web
uv run python -m motte_cli agent-tasks run --scenario file-report@1 \
  --model YOUR_MODEL --mode native-tool --max-steps 8
uv run python -m apps.worker.motte_worker --once
uv run python -m motte_cli runs show <run_id>            # 状态与多指标分数
```

执行后把 run id、模型 id/版本、观察到的费用与终止原因登记进本文件，即可将
对应模型标记为 verified_in_live。
