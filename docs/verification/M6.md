# M6 验证记录：实验、比较、统计与门禁

> 当前状态：`planned / not_run`。本文是 M6 实施验证记录的入口，不把规划、静态代码阅读或已有 M6-Lite 原语当作通过证据。实际开发必须在当前 HEAD 重新填写 current summary；历史轮次追加保存，不覆盖原始结果。

## 1. Current summary

- 规划日期：2026-09-21
- 规划基线首次核对：主分支已包含 M5 合入及其后的 SQLite schema 对齐修复；实际实施必须重新记录起始 SHA。
- 工作区：首次核对时已有用户/其他 Agent 的未提交源码、测试和 `var/` 证据目录；M6 实施不得重置、清理或纳入这些变更。实现和测试必须在 `.worktree/m6-experiments-comparison-gates` 独立 worktree 完成。
- 迁移：首次核对已知 head 为 `0013_scenario_skill_resources`；实施前必须由工具重新查询并沿实际 head 追加。
- M6-Lite 代码事实：已有 ComparisonPolicy/RunReportRef、比较服务、coverage、基础 Gate、Baseline repository 和只读 API；尚未以本阶段公共产品验收证明 M6-Lite 完整交付。
- M6-Full：ExperimentSpec/Cell 编排、完整统计、分组历史 Baseline、版本化完整 Gate、统一 Web/CLI/CI 和跨 suite 集成尚未开始本阶段验收。
- 外部边界：M4 live、M5 真实 Judge/人工校准/外部 Harness 的证据需按当前环境重新核对；缺失时相关 M6 Gate 保持 `experimental`/`not_run`。
- 用户已授权：全部 M6 功能和离线门禁完成后，使用当前项目配置的 DeepSeek V4.1 Flash 做有界真实模型验收；实际模型 ID、调用数、token、费用和结果必须记录。
- 实际费用：本规划阶段未调用付费模型、Judge、外部 Harness 或真实业务工具。

## 2. 证据分层

| 层级 | 允许证明 | 当前状态 |
|---|---|---|
| contract/unit | 契约校验、纯比较、覆盖、统计公式和 Gate 纯求值 | not_run（本阶段） |
| storage | Memory/SQLite/PostgreSQL 的不可变、CAS、迁移、并发和恢复 | not_run |
| integration | API/CLI → Dispatcher/Worker → Run/Pass → Report → Compare/Gate | not_run |
| web | Web 消费公共 API、状态/unknown/coverage/固定引用展示 | not_run |
| live | 真实 C-Eval/Harbor/Harness/模型/Judge/人工校准 | pending / not_run |

ImportError、collection error、skip、子 Agent 自述和页面静态存在不构成通过证据。失败、blocked、not_run 与 passed 必须分开记录。

## 3. 目标与工作包分母

| 范围 | 分母 | 当前状态 |
|---|---:|---|
| M6-G01–G21 | 21 | not_run |
| M6-T01–T11 | 11 | not_run |
| M6-A01–A20 | 20 | not_run |
| 非编号需求 | 原需求第 1–12 节、协议、运维、回退和 M7 handoff | not_run |

详细责任映射和验收条件见 [`2026-09-21-m6-execution.md`](../superpowers/plans/2026-09-21-m6-execution.md)。每项最终记录：状态、实现路径、真实消费者、测试 node/命令、环境、commit、证据引用和限制。

## 4. 实际命令账本

| 时间 | 命令 | SHA/环境 | 结果 | 证据 |
|---|---|---|---|---|
| - | - | - | not_run | - |

## 5. Review ledger

| ID | 严重度 | 复现 | 修复 commit | 复验 | 状态 |
|---|---|---|---|---|---|
| - | - | - | - | - | not_run |

## 6. 外部条件与回退

需要另行准备并单独登记的条件：真实 PostgreSQL 两连接与迁移/回退、目标 OS/Docker/Harbor fixture、M4 runtime 真实证据、M5 Judge provider 和至少 30 条人工校准样本、真实模型/费用授权。条件缺失不降低 M6 离线验收要求，也不能把 not_run 写成 passed。

回退时先关闭 Experiment 新建/领取和新的 GatePolicy 发布，确认自有活动资源停止，保留 Experiment/Cell/Run/Trial/ScoreSet/ScoringPass/Baseline/GateResult/ReportSnapshot/Artifact pin 与不确定记录；不得删除历史来隐藏失败。
