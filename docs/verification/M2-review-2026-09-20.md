# M2 代码审查反馈（2026-09-20）

审查范围：`e9edb80..50c8070`，79 个变更文件。依据 M2 roadmap、M2 详细工作包、M6-Lite 前置要求及开工交接文档。

**结论：本轮不能通过代码交付审查。确认 16 项需要修复的问题（9 项 P1、7 项 P2）。** 这些是代码缺陷或承诺范围的实现缺口，与未授权真实模型调用、PG 环境未运行等验收条件分别处理。未修改业务代码、未提交或推送。

## 验证证据与边界

本轮实际运行 `make check`，退出码 0：

- Python：1150 passed / 18 skipped，40.58s。
- Web：13 files / 180 passed。
- Ruff、contracts mypy、compileall、Web build、pi bridge selftest、Compose config 通过。
- 构建提示动态 import 无效及 bundle 大小警告，不作为本轮阻塞缺陷。
- 另运行临时离线 Python 复现：真实本地 fake Runner 进程、临时 SQLite 导入故障、双 RunService 取消、临时目录 symlink、比较/Gate 纯函数、空结果终态。未调用真实 Provider、付费模型或真实外部 Harness。
- 页面异步问题属于代码路径审查，未声称做过浏览器交互复现。
- 新子代理调度遇到 agent thread limit，本轮审查与复现由主代理完成。
- 现有集成测试允许 `completed|failed`，未断言 Case 对应关系、非空 Score、原生指标与报告证据，因此全绿不能排除下列问题。

## P1：优先修复

### M2-R01：真实 Runner 缺少可消费的模型、数据和执行配置

位置：
- [packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:54-63](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:54)
- [packages/sdk-python/motte_sdk/execution_backends.py:432-448](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/execution_backends.py:432)
- [apps/api/app/main.py:1683-1696](/Users/whitezhi/Dev/MoTTEavl/apps/api/app/main.py:1683)

`CevalJobAdapter.prepare` 只写 `spec.profile`。公开入口只冻结模型 ID；JobSpec 不包含模型快照、endpoint/credential refs、题目内容或数据 Artifact 引用。`prepare_external_dataset` 校验后只保留 hash/ID/has_gold，不保留 Runner 所需题干和答案。全仓搜索 `build_opencompass_config` 只有函数定义及测试调用，没有生产调用点，因此独立测试通过的配置转换器没有进入执行链路。

默认 argv 指向仓库没有交付的 `/opt/motte-runner/bin/opencompass-entry`；生产路径也没有 adapter 注册调用，API/Worker/CLI 的注册表独立且缺少配置加载。仅安装 OpenCompass 不能使文档中的 prepare → run → worker 流程成立。另外 environment_digest 只是 Runner 版本字符串的 hash，不能标识真实环境内容。

复现/核验：公开入口创建 Run 后检查持久化 JobSpec，仅有 Profile/IDs/versions/limits；工作目录只有 profile，不包含选择集的数据和模型配置。现有 fake Runner 完全忽略这些输入，固定输出两条样本，遮蔽了缺口。

修复验收：从固定数据/模型/凭据引用构造实际 Runner 配置；API/Worker/CLI 从同一受控配置加载 adapter；交付固定环境与 wrapper。真实固定 Runner 对本地确定性 HTTP 端点执行，断言实际请求模型、题目、few-shot、选样范围和配置 hash；该本地验证不需要真实模型付费。

### M2-R02：公开 C-Eval Run 绕过评分插件，完成后没有 Score

位置：
- [apps/api/app/main.py:1703-1713](/Users/whitezhi/Dev/MoTTEavl/apps/api/app/main.py:1703)
- [packages/cli/motte_cli/main.py:138-141](/Users/whitezhi/Dev/MoTTEavl/packages/cli/motte_cli/main.py:138)
- [packages/sdk-python/motte_sdk/service.py:814-838](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/service.py:814)
- [packages/sdk-python/motte_sdk/benchmark_plugins.py:226-235](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/benchmark_plugins.py:226)

API/CLI 只调用 `resolve_execution`，没有产生 `benchmark_provenance/evaluation`。`_score_results` 仅在 provenance 存在时走 managed scorer，否则要求 Case 行含 `expected`；外部 Job 行不含该字段。因此注册 ceval-external 插件并不会自动使用它。该插件的 prepare 回调也与 `prepare_with_plugin(scenario, manifest, resources)` 的实参职责不一致，不能只补一个调用就认为接通。

已复现：API prepare → run → fake Runner → dispatch，返回：
`status=completed, scores=[], scoring_pass.scorer_id=generic-v1, summary={scores:0, passed:0}`。

修复验收：公共入口生成完整冻结评分身份，生产 Run 落不可变 ScoreSet/ScoringPass；有 gold 的结果必须实际评分，无 gold 的结果明确 unscored。API、CLI、report、rescore 都消费同一插件契约。

### M2-R03：Runner 序号被直接当作 Case ID，导致错题归属

位置：
- [packages/benchmark-runtime/motte_benchmark/opencompass/parser.py:399-402](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/opencompass/parser.py:399)
- [packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:100-106](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:100)

准备接口允许用户提供任意非空 Case ID；Parser 却拼接 `subject-idx`，Adapter 将它直接用作 source_case_id，没有通过冻结选样清单映射。排序、子集、从 1 开始的 ID 都会丢结果或串题。

已复现：准备 `logic-1, logic-2`，Runner 产出 index 0 和 1。导入键变为 `logic-0, logic-1`；第 2 个 Runner 结果 C/gold=C 被归到第 1 题 `logic-1`（准备数据 gold=B），`logic-2` 被标记 not_attempted，Run 仍 completed。

修复验收：冻结 runner row index → stable Case ID 映射；gold 从对应冻结数据核对；未知/重复/冲突映射拒绝或隔离。覆盖任意 ID、随机子集、重新排序及多个学科。

### M2-R04：导入前写终态，崩溃恢复永久丢结果并覆盖证据

位置：
- [packages/sdk-python/motte_sdk/external_jobs.py:285-299](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/external_jobs.py:285)
- [packages/sdk-python/motte_sdk/external_jobs.py:313-345](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/external_jobs.py:313)

`_settle` 先把 Job 写成 settled/failed，再逐条导入。中途故障后恢复看到终态，只读取已经导入的记录，不重新采集或恢复完整冻结 outcome；随后写回同一路径 `outcome.json`，把原始完整证据覆盖成截断版本。冲突后的 failed/error 也只在临时 outcome 修改，没有同步为可靠恢复事实。

已用临时 SQLite 复现：fake Runner 产生 4 条，第 2 次 import 抛 OSError。故障后 Job=settled、库内 1 条、artifact 4 条；恢复后仍 settled、结果和库内均只有 1 条，artifact 从 4 条被覆盖成 1 条，start 次数仍为 1。

修复验收：区分进程终止与导入完成，保存不可变完整源证据和可恢复游标，事务完成/冲突状态可恢复；在每条导入前后及最终化前注入中断，恢复后完整性、原始 hash 与错误结论不变，且不重复启动。

### M2-R05：API 与 Worker 分进程时，取消不会中断外部 Job

位置：
- [packages/sdk-python/motte_sdk/service.py:424-443](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/service.py:424)
- [packages/sdk-python/motte_sdk/external_jobs.py:132-150](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/external_jobs.py:132)

取消钩子仅保存在执行该 Job 的 RunService 内存字典。API 使用另一实例时只落 cancellation_requested；Worker 的 observe 循环只 poll 进程和 deadline，从不检查持久取消请求。公开创建路径又没有 max_wall_seconds，挂起 Job 可以无限继续占资源或调用模型。

已复现：两个 RunService 共用一个 store，Worker 执行 slow_ok，另一实例 cancel。取消后 Run=running，child 仍运行；直到它自行完成才变 cancelled，而 Job 记录仍为 settled。

修复验收：Worker 在观察循环消费持久取消状态并中断已核验所有权资源。用两个服务实例/独立进程和挂起 Runner 验证有限时间内停止；补取消落在 prepare/start/journal/collect 各窗口的回归。

### M2-R06：输出根目录 symlink 绕过安全边界

位置：
- [packages/benchmark-runtime/motte_benchmark/opencompass/parser.py:268-293](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/opencompass/parser.py:268)
- [packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:92-96](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:92)

Parser 把传入 outputs 的 `resolve()` 结果直接当作可信根，仅检查最终文件是否为 symlink。如果 outputs 本身指向工作目录外，外部目录会被重新认定为安全根。检查后的 Path.read_text 也没有绑定目录描述符，父链替换和文件变大仍有竞态。

已复现：在临时工作目录创建 `outputs -> outside`，outside 内放合成 OpenCompass JSON。解析成功并返回 `EXTERNAL_ROOT_SENTINEL`，未拒绝越界。

修复验收：信任边界固定在原 Job 工作目录，沿目录描述符打开并禁止跟随 symlink；读取时约束大小及总预算。覆盖根目录、父链、文件的符号链接和检查后替换，不能仅测末端文件 symlink。

### M2-R07：Gate 忽略 metric 名称，费用门禁实际比较准确率

位置：
- [packages/evaluators/motte_eval/gates.py:48-61](/Users/whitezhi/Dev/MoTTEavl/packages/evaluators/motte_eval/gates.py:48)
- [packages/sdk-python/motte_sdk/comparisons.py:62-94,125-126](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/comparisons.py:62)

policy.metric 从未用于选择指标，candidate_summary 总是重算 accuracy。因而费用、延迟或拼错的指标都会按准确率与阈值比较；费用是否已知仅由可选 require_cost_known 控制。

已复现：一题预测正确、费用未知，分别传 `metric=accuracy/cost.total_usd/not_a_metric, op=lte, threshold=2, required_coverage=1`，三次均 passed，规则文本均为 `1.0 lte 2`。

修复验收：按注册 MetricResult 的身份、单位、方向、分母和状态求值；不支持的 metric 返回明确错误，成本规则在费用未知时自动不足证据，不能靠调用方额外记得设置布尔值。

### M2-R08：比较只核对三个 Profile 字段，不同评测口径仍判可比

位置：[packages/evaluators/motte_eval/comparison.py:55-70](/Users/whitezhi/Dev/MoTTEavl/packages/evaluators/motte_eval/comparison.py:55)。

除了 dataset_revision，只比较 answer_extractor/extractor_version/prompt_template_version。few-shot、split、aggregation/scorer、benchmark 身份、Runner/environment 和实际数据内容差异均未纳入资格；公开创建也没有冻结 row hash/expectations 供下游核对。

已复现：同 ID/revision，将 few-shot 0→5、split val→test、aggregation macro→micro，返回 eligible=True、quality=True，原因只有费用未知。

修复验收：按任务来源与冻结数据/评分口径逐字段判定，明确允许变量，缺失身份不默认相等。每种禁止变化单独测试，允许 model 变化的反例继续通过。

### M2-R09：原始 Runner 文件与 native 指标没有进入持久证据链

位置：
- [packages/sdk-python/motte_sdk/external_jobs.py:324-335,355-370](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/external_jobs.py:324)
- [packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:118-122](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py:118)
- [apps/web/src/evalTypes/ceval/CevalPages.tsx:283-286](/Users/whitezhi/Dev/MoTTEavl/apps/web/src/evalTypes/ceval/CevalPages.tsx:283)

Adapter 先解析工作目录，durable runner 保存的是归一化 outcome，并非解析前的原始 JSON/目录证据。cursor 内 native/diagnostic 只放入返回值 `outcome.import.metrics`，RunService 不消费或持久化；artifact 也不含 cursor，Job checkpoint 只有计数。仅有原生聚合而无样本时尤其会丢失全部分数来源。结果页只有“分开保存与显示”的说明文字，没有实际双栏指标。

已复现：fake Runner 的原始文件含 `accuracy=50`；执行后 Job、ScoringPass、outcome artifact 均不含 `ceval_logic/accuracy`，原始文件只剩可变工作目录副本。

修复验收：先冻结原始文件 hash/内容与 Job 关联再解析；将 native/diagnostic 分别写入版本化指标事实并供报告/UI查询。清理工作目录或重启后仍可审计和重新解析；仅聚合输入保留原生指标及缺明细标志。

## P2：阶段交付前修复

### M2-R10：恢复把“存在 results.json”直接等同于成功退出

位置：[packages/benchmark-runtime/motte_benchmark/process.py:379-387](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/process.py:379)。

进程不可核验时，只要存在非末端 symlink 的 results.json 就返回 settled，同时标记 exit_code_unobserved=True。部分输出可以在非零退出前产生，因此不能据此证明成功。C-Eval 只写 outputs 树，又与这一固定 results.json 探测不一致。

已复现：无可核验 PID、exit_code=None、只有部分 results.json，poll 返回 settled。

修复验收：wrapper 持久记录可信退出/完成标记；缺标记只保留部分采集并进入不确定结论，不能成功升级。覆盖非零退出、只写部分文件、只有 OpenCompass outputs 树的重启。

### M2-R11：零条结果仍最终化为 completed

位置：[packages/sdk-python/motte_sdk/service.py:331-343](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/service.py:331)。

终态只看 settled 且没有 error；缺失 Case 被填成 not_attempted，但不会使该判断失败。CevalJobAdapter 在 outputs 不存在时直接返回空列表，构成实际生产触发路径。

已复现：selected=[a,b]、job_status=settled、results=[]、error=None，最终 completed，两个 Case 均 not_attempted。

修复验收：按 Profile 检查结果存在性、覆盖、无 gold 与空预测语义。零结果/全空预测不能伪装成功完成可计分任务；正常无 gold 预测任务应保留明确 unscored，而非一刀切为执行失败。

### M2-R12：公共比较/Gate 没有固定 ScoringPass 和 baseline 快照

位置：
- [packages/sdk-python/motte_sdk/comparisons.py:17-23,35-50,62-94](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/comparisons.py:17)
- [apps/api/app/main.py:1730-1769](/Users/whitezhi/Dev/MoTTEavl/apps/api/app/main.py:1730)

API 只收 run ID；report_ref 每次选 current pass，candidate_summary 又直接从 case_runs 重算，不读取该 pass 的 ScoreSet。构造器 baselines 未使用，baseline store 未装配到公共服务。相同请求无法选择或重放历史评分；传入的 RunReportRef 也没有在比较中校验。

补充已复现：Run 仍 queued、没有任何 ScoringPass，只要写入一条原始正确预测就能通过质量 Gate。

修复验收：输入固定 Run+ScoringPass/baseline snapshot，按对应不可变指标事实求值；报告与 Gate 输出记录引用。rescore 后旧引用及其结论不漂移；无评分证据、未完成的输入不能冒充已固定报告。

### M2-R13：数据准备状态只在 API 内存，重启及多实例不一致

位置：
- [apps/api/app/main.py:1490-1491,1549-1550](/Users/whitezhi/Dev/MoTTEavl/apps/api/app/main.py:1490)
- [packages/cli/motte_cli/main.py:55-57,91-98](/Users/whitezhi/Dev/MoTTEavl/packages/cli/motte_cli/main.py:55)

每次 create_app 都创建空 Catalog，prepare 只修改内存。另一个 API 实例或重启后，准备过的数据变回未准备，题目清单丢失。CLI prepare 的状态也只存命令内存；preflight 甚至固定使用 state=unprepared。

已复现：对 app A prepare 成功，再以同一 store/resources 建立 app B，catalog 变 registered、dataset=None、DATASET_UNPREPARED。

修复验收：准备结果、源 Artifact、清单和治理证据持久化；API/CLI/Worker 共享来源并按 revision 引用。覆盖重启、两个 API 实例、CLI prepare 后 API 查询与预检。

### M2-R14：创建入口未执行真实预检，允许虚假的 full/split/few-shot 声明

位置：[apps/api/app/main.py:1655-1695](/Users/whitezhi/Dev/MoTTEavl/apps/api/app/main.py:1655)。

只判断 model 是否存在，不检查 published 状态。split 直接来自请求，不校验准备数据实际分区；few-shot count 不校验示例是否可用；scope 接受任意字符串，并可把任意小数据集标 full。GET preflight 与创建使用不同输入，且创建不调用该服务。

已复现：准备 2 条 val 样本，模型 lifecycle=draft，提交 `scope=full,split=test,few_shot=5`（未提供任何 dev 示例），返回 202。GET 预检也可返回 ok=True。

修复验收：API/CLI 使用同一冻结输入和校验服务；生命周期、split、few-shot、scope 完整范围、输出/context 限额必须在入队前一致校验，失败 422 且 0 Job 启动。

### M2-R15：结果/比较页面缺少请求身份保护，会展示旧 Run 的结论

位置：
- [apps/web/src/evalTypes/ceval/CevalPages.tsx:237-242](/Users/whitezhi/Dev/MoTTEavl/apps/web/src/evalTypes/ceval/CevalPages.tsx:237)
- [apps/web/src/evalTypes/ceval/CevalPages.tsx:304-322](/Users/whitezhi/Dev/MoTTEavl/apps/web/src/evalTypes/ceval/CevalPages.tsx:304)

结果页切换 runId 后不清空/隔离旧状态，也没有清理函数或请求代号；旧请求晚到会覆盖当前页面，新旧 Job/Run 还可能混合。比较页重复提交两组 Run 时，第一组较慢的 compare/gate 成功或失败都可以覆盖第二组结果；输入改动后旧 gate 也继续显示。

证据类型：静态代码路径确认；现有五个页面测试没有延迟响应和跨路由场景。此条未声称浏览器已复现。

修复验收：对 Run/Case/pass 及每次比较提交绑定身份，隔离加载/成功/失败；延迟 Promise 测试覆盖 A→B 切换、反序返回、旧失败和旧 gate-pass，始终只展示当前对象的事实。

### M2-R16：CMMLU 仅有配置辅助，缺少可执行的独立产品链路

位置：
- [packages/benchmark-runtime/motte_benchmark/opencompass/cmmlu.py:104-156](/Users/whitezhi/Dev/MoTTEavl/packages/benchmark-runtime/motte_benchmark/opencompass/cmmlu.py:104)
- [packages/sdk-python/motte_sdk/benchmark_plugins.py:298-307](/Users/whitezhi/Dev/MoTTEavl/packages/sdk-python/motte_sdk/benchmark_plugins.py:298)
- [apps/api/app/main.py:1490-1491](/Users/whitezhi/Dev/MoTTEavl/apps/api/app/main.py:1490)

新增 CMMLU 模块只提供学科和 Profile。当前仅注册 ceval-external plugin，CevalJobAdapter.collect 硬编码 dataset=ceval；API catalog 仅注册 ceval，没有 CMMLU adapter/suite/API/CLI/Web 路由。传 benchmark_id 给通用准备函数并不能产生可运行的 CMMLU Run。

核验：搜索生产注册/调用点及 FastAPI routes，cmmlu 路由列表为空。现有 T11 测试只覆盖准备/Profile/解析辅助，不能证明完整追加工作包完成。

修复验收：共享 Job 基础，交付独立 CMMLU suite、配置/解析/评分、来源与用户入口；独立确定性 Runner 验证，不能借 C-Eval 的测试结果宣称通过。未授权 live 仍单独记 not_run。

## 修复安排与复审门槛

建议按以下顺序分包修复，并逐包提交代码、测试和对应文档：

1. **执行输入与公共入口**：R01、R13、R14。先让真正受控 Runner 能拿到正确、冻结的模型和数据。
2. **评分与证据**：R02、R03、R09、R11。补公共端到端数值断言，不只检查“有终态”。
3. **恢复、取消和路径安全**：R04、R05、R06、R10。重启与中断使用 SQLite/分实例测试，不用内存 happy path 替代。
4. **比较和门禁**：R07、R08、R12。按固定 pass 和指标规则求值，补错误指标/不同 Profile/历史重评分反例。
5. **用户链路与独立范围**：R15、R16。补异步身份测试与 CMMLU 独立闭环。

必须补一个贯穿公开入口、实际 fake Runner、采集、ScoreSet、报告和 Gate 的样例：
selected=4、attempted=3、correct=2、wrong=1、not_attempted=1、Runner 非零退出；
断言 selected-case diagnostic accuracy=0.5、coverage=0.75、执行失败、全覆盖 Gate 不通过；
所有结果 ID、原始 Artifact hash 和 ScoringPass 可追溯。原生分数存在时独立保存，不存在时不生成。

现有阶段收尾文档的“G01–G17 均落地”“API/CLI 共用构造服务”“代码可交付”等结论需要按实际修复情况修订。真实 Runner 确定性端点测试属于尚需交付的本地集成工作；真实模型、PG 与生产资源验证继续分别记录执行证据，不把 skip/not_run 计为通过。
