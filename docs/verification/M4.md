# M4 验证记录（Pi 与外部 Harness）

状态：开发中（分支 `codex/m4-pi-external-harnesses`，基于 `4f5918e`）。
本文件按工作包逐项登记实现路径、测试命令、结果、commit、环境、证据级别与限制。
证据级别：contract（契约/单元）、storage（三存储语义）、protocol（协议夹具）、
integration（真实进程/SDK 集成）、live（真实模型/CLI 授权执行，默认未执行）。

环境基线：Windows 11 x64（本机开发环境）、Node v24.18.0、pnpm 9.15.0、
uv 0.12.6 / Python 3.14。PG/Docker/live 条件缺失时单列，不以 skip 冒充通过。

已知本机既有失败（与 M4 无关，主干同款）：`tests/runtime/test_review_round3/4/5_fixes.py`
中的 symlink/fd 链用例、`tests/runtime/test_runner_env_boundary.py`、
`tests/storage/test_external_job_store.py` 的进程语义用例——主干 `4f5918e`
在相同解释器下同样失败（Windows 无特权 symlink 与进程组语义差异），
登记为 POSIX 专用验证项，不计入 M4 回归。

## M4-T01 — Runtime 契约、不可变资源与 backend 条件校验

**状态：完成（contract + storage 级离线证据）。**

### 实现路径

- 新增 `packages/contracts/motte_contracts/runtime.py`：
  `RuntimeDefinition`（kind/transport/upstream_version/adapter/parser 版本、
  config_schema 字段 allowlist、supported_modes/interactive 一致性、
  model_control 三值、`RuntimeToolControl`、evidence_capabilities 白名单）、
  `RuntimeVersion`（仅 published、立即不可变）、`RuntimeProfile` /
  `RuntimeProfileVersion`（workspace 来源约束、credential_refs 仅凭据档案名，
  常见密钥前缀拒绝）、`validate_runtime_settings`（按 config_schema 的字段
  allowlist 与类型校验，未知字段拒绝）、`runtime_readiness`（installed →
  protocol_ready → execution_ready 分层及原因）。
- `packages/storage/motte_storage/resource_store.py`：新增 `runtimes`
  （runtime_versions）与 `runtime_profiles`（runtime_profiles）两个版本化
  资源（VERSIONED_TABLES：同内容幂等、异内容冲突、不可删除）；InMemory /
  SQLite 同语义；PG 由迁移建表。
- `migrations/versions/0009_runtime_resources.py`：PG 建表（head 0008 → 0009）。
- 新增 `packages/sdk-python/motte_sdk/runtime_backends.py`：
  `validate_runtime_manifest`（runtime 引用与 backend 身份一致、快照必须为
  已发布版本、model_control 三分支：platform-controlled 必须有解析后的
  provider 快照与发布模型引用；runner-configured 必须有通过 allowlist 的
  原生模型设置；externally-managed 必须声明凭据引用）、
  `register_runtime_backend` / `install_runtime_backends`（注册
  pi-agent/claude-cli/codex-cli/codex-app-server 四个 backend；
  build 在 T04/T06/T07/T10 接线前显式 `RUNTIME_EXECUTION_NOT_IMPLEMENTED`，
  不假成功）。
- `packages/sdk-python/motte_sdk/resolve.py`：`resolve_manifest` 解析
  `manifest.runtime` → 已发布 RuntimeVersion 快照（content_hash、
  model_control、tool_enforcement、config_schema）写入
  `runtime_snapshot` / `resource_snapshots.runtime_version`；
  `runtime_profile` 支持内联或 name@version 资源引用展开并回填 config_hash；
  非 platform-controlled 且未声明 provider/model 时不伪造平台 provider
  （`_connection_for` 返回 None）；managed 套件的 provider 强制改为按
  model_control 条件化。
- `packages/sdk-python/motte_sdk/execution_backends.py`：`resolve_execution`
  支持由 `manifest.runtime` 派生 backend 身份；runtime 与 legacy runtime
  字段（agent/skills/harness/pi）互斥；`_reject_unconnected_runtime_fields`
  扩展 runtime 字段（direct/replay 不接受 runtime）。
- `packages/contracts/motte_contracts/scenario.py`：`ScenarioSpec.model`
  改为可空（provider 驱动路径仍在 prepare_run/resolve 强制模型解析，
  不放宽旧校验）；`contracts/run.py` `ResolvedManifest` 增加
  runtime / runtime_profile / runtime_snapshot 字段。

### 测试命令与结果

```bash
uv run pytest -q -m "not live" tests/contract/test_runtime_profile.py tests/storage/test_runtime_resources.py
# 27 passed（红灯先行：实现前 17 failed —— runtime_backends 缺失、runtimes 存储缺失）
uv run pytest -q -m "not live" tests/sdk/test_resolve.py tests/runtime/test_execution_backends.py tests/contract/test_scenario_validation.py
# 全部通过（相邻回归）
uv run pytest -q -m "not live" tests/runtime tests/sdk tests/storage tests/contract
# 733 passed / 27 skipped / 49 failed —— 49 个失败与主干基线完全一致（本机
# Windows 既有环境失败，见环境基线节），无新增回归
uv run ruff check .        # All checks passed
uv run mypy packages/contracts  # Success: no issues found in 26 source files
```

更新：`tests/runtime/test_execution_backends.py` 注册表断言加入四个 runtime
backend；`tests/storage/test_postgres_repository.py` Alembic 链加入
`0009_runtime_resources`。

### 验收对照

- M4-G01（RuntimeDefinition/Version 固定实现）：契约 + 三存储不可变语义完成。
- M4-G02（三种状态分层带原因）：`runtime_readiness` 契约完成；probe/API 展示在 T02/T08。
- M4-G13（不可观察显示 unknown、策略可拒绝）：model_control 校验完成；usage/cost unknown 语义在 T06/T07。
- M4-G14（可控度进入可比性）：model_control 快照进入 manifest；比较侧在 T08。
- 反例：覆写已发布版本/空模型分支/参数丢失/跨 Run session 在
  `test_runtime_resources.py` 与 `test_runtime_profile.py` 覆盖（跨 Run session
  完整断言在 T04 的 `test_pi_run_backend.py`）。

### 限制

- PG 迁移仅静态校验（链检查 + SQL 同构），未在真实 PG 执行（`test_postgres_e2e`
  依赖 psycopg/PG 实例，属既有 live 边界）。
- runtime backend 的 build 尚未接线（T04/T06/T07/T10），当前 dispatch 会得到
  显式 `RUNTIME_EXECUTION_NOT_IMPLEMENTED` 拒绝——这是诚实的中间状态。

## M4-T02 — 固定上游与实际配置兼容性

**状态：完成（协议矩阵级离线证据；CLI 二进制未安装，登记为环境条件）。**

### 实现路径

- 新增 `docs/protocols/runtime-compatibility.json`：四个 backend
  （pi-agent@1 / claude-cli@1 / codex-cli@1 / codex-app-server@1）的 pinned
  上游、adapter/parser 版本、batch argv、能力、config_discovery（环境变量与
  自动发现文件清单）、readiness 规则。版本核验来源与边界在
  `verification_sources` / `verification_boundary` 字段声明。
- 上游核验（2026-09-20，registry 元数据，零模型调用零登录）：
  `@mariozechner/pi-agent-core@0.73.1`（MIT，Node ≥20，已安装进 bridges/pi
  并由 pnpm-lock 固定；dist 类型核验 Agent/StreamFn/faux provider 注入点）；
  `@anthropic-ai/claude-code@2.1.278`；`@openai/codex@0.155.1`（Apache-2.0）。
  后两个二进制未在本机安装——平台不自动安装/登录/升级全局二进制。
- 新增 `packages/harness-runtime/motte_harness/compatibility.py`：
  `load_runtime_compatibility`（manifest 缺失/损坏/缺 pinned 字段 fail
  closed）、`resolve_pinned_version`、`readiness_for_backend`（installed /
  protocol_ready / execution_ready 分层 + 每层原因；未安装与版本漂移是两个
  不同原因；execution_ready 永不由 --version/能力声明推出；probe 只做本地
  静态检查，无下载/登录/模型调用路径）。
- 协议夹具（`tests/fixtures/harness/`）：`claude-batch-result-v1.json`、
  `codex-exec-events-v1.jsonl`、`codex-app-server-rpc-v1.jsonl`；
  provenance=synthetic-from-docs，live 捕获待授权。
- `docs/protocols/provider-compatibility.md`：Agent/Harness 节改为引用
  runtime-compatibility.json（唯一事实源）。

### 测试命令与结果

```bash
uv run pytest -q -m "not live" tests/protocol/test_runtime_compatibility.py
# 9 passed（红灯先行：compatibility 模块缺失时 8 个用例无法收集）
uv run pytest -q -m "not live" tests/protocol tests/harness tests/runtime/test_pi_protocol.py
# 39 passed（含既有 pi bridge 硬化、install、process 回归）
uv run ruff check .  # All checks passed
```

### 验收对照（M4-A01）

- 未安装：`installed=false`，原因 "binary is not installed (no auto-install)"。
- 版本不兼容：`installed=true` + `protocol_ready=false`，原因含漂移版本与
  pinned 版本；fail closed。
- 缺认证：externally-managed 的 CLI 在创建层要求 credential_refs（T01 的
  `RUNTIME_AUTH_REQUIRED`）；auth 实际可用性属 live 验证，不在静态 probe 内
  读操作者凭据。

### 限制

- claude/codex 二进制未安装在本机：本阶段协议夹具为合成（按官方文档格式），
  真实 --version/捕获需安装环境或 live 授权后补充；不据此宣称 CLI 执行支持。
- app-server 协议子集（thread/turn + codex/event）按文档构造，真实握手验证
  在 T10 与 live 单列。

## M4-T03 — 真实 Pi bridge

**状态：完成（protocol + 真实 SDK 集成级离线证据）。**

### 实现路径

- `bridges/pi/bridge.mjs` v2.0.0（协议 v2）：probe / init / run / interrupt
  控制消息严格串行分发（promise 链，init 的 async 完成前不处理后续消息）；
  stdout 仅协议（每事件携带 run/case/session/operation 身份与单调 seq）；
  stderr 有界（64KB）且脱敏（工具错误只留首行 120 字符，避免参数 JSON 回显）；
  SDK 加载失败 → probe execution_ready=false + init/run `PI_BACKEND_UNAVAILABLE`，
  绝无 echo/builtin fallback；裸 `prompt` 消息 → `UNSUPPORTED_MESSAGE`。
- `bridges/pi/session.mjs`：真实上游 `Agent`（@mariozechner/pi-agent-core@0.73.1）
  + SDK 官方 `registerFauxProvider`（scripted model，零网络零真实模型调用）
  + `streamSimple`（真实 SDK 流路径）；`beforeToolCall` 在执行前阻断
  max_tool_calls 超限（确定性 block，而非事后 abort）；max_steps 步数预算；
  中断（interruptRequested 标记 → agent.abort → interrupted 先于 finished）；
  usage 诚实保持未上报（scripted model 无真实计量，不填 0）。
- `bridges/pi/tools.mjs`：bridge 侧工作区工具（read_file/write_file/list_files）
  平台强制沙箱——相对路径 only、拒绝 `..`/绝对盘符/symlink 链、单文件/
  总量/文件数配额；工具经 SDK 的 typebox/JSON-schema 参数校验。
- `packages/agent-runtime/motte_agent/pi.py`：`PiAgentRuntime` 降级为
  probe 客户端（v2，run 显式 `PI_SESSION_REQUIRED`）；新增 `PiBridgeSession`
  ——init → run → finished 会话客户端：增量行读取（行 4MB / 总量 32MB /
  空闲与总时长有界）、每事件字段 allowlist 严格校验、身份不匹配/seq 回退/
  未知事件/多余字段全部 fail closed、结构化错误保留 code 且不透传 bridge
  文本、interrupt 与进程树清理。
- 依赖：`@mariozechner/pi-ai@0.73.1` 提升为 bridges/pi 直接依赖（pnpm 不
  提升传递依赖；faux provider / streamSimple / fauxToolCall 为注入点）。

### 测试命令与结果

```bash
cd bridges/pi && node selftest.mjs
# pi bridge selftest: ok (real SDK, scripted model, file written)
uv run pytest -q -m "not live" tests/protocol/test_pi_real_adapter.py   # 6 passed
uv run pytest -q -m "not live" tests/protocol/test_pi_bridge_hardening.py  # 15 passed
uv run pytest -q -m "not live" tests/protocol tests/runtime/test_pi_protocol.py tests/runtime/test_execution_backends.py
# 全绿（含既有 process-tree kill / cancellation 注入回归）
uv run ruff check .  # passed
```

主断言 `test_real_pi_session_no_echo_fallback`：真实 Agent loop + faux
scripted model 驱动 write_file 真实落盘；prompt 不出现在任何输出；
usage reported=false；`test_real_pi_session_missing_sdk_is_explicitly_unavailable`
断言无 SDK 时显式不可用（不跳过、不假成功）。

既有硬化测试全部保留（帧边界/UTF-8/超长行/输出上限/身份不匹配/seq 回退/
错误字段 allow closed/结构化错误脱敏/超时与取消杀进程树），协议期望随
v2 演进更新；fixture 构造移至 `tests/protocol/bridge_process.py`（全部
静态模板 + 文件路径 argv，无代码串插值）。

### 验收对照

- M4-G03（真实上游包、bridge 不移植源码）：SDK 以依赖形式消费。
- M4-G08（结构化事件保留 source seq/parser/完整度）：seq 单调 + 身份 +
  预算停止事件；parser_version 在事件消费侧（T04）持久化。
- M4-A02（无 SDK → execution_ready=false，禁止 echo fallback）：bridge 与
  客户端双侧断言。
- M4-A05（单行超限）：行 4MB 上限 + 5MB 单行注入测试 fail closed。

### 限制

- interrupt 的确定性通过 tokens_per_second 拉长流式时长实现（SDK 官方
  faux 限速）；真实模型的取消语义属 live 授权范围。
- 平台 Provider（platform-controlled 模型经平台 HTTP provider 调用）在
  bridge 侧预留为 model_transport 决策点，当前离线路径是 faux scripted；
  live 接线时须把 provider 请求/响应桥接进协议（T04 记录该边界）。

## M4-T04 — Pi 平台纵向链路

**状态：完成（integration 级离线证据：真实 bridge 驱动 RunService 全链路）。**

### 实现路径

- `packages/sdk-python/motte_sdk/pi_runtime.py`（新增）：
  `PiRuntimeCaseExecutor` —— 每 CaseAttempt 独立 session/workspace：
  session_id/operation_id 每次全新（无跨 Case 复用；续跑走 retry 子 Run）；
  事件经 `service.emit_run_event` 持久化（`pi_*` 类型 + case/session 归属）；
  模型调用记 InvocationRecord（metering_source=scripted-model，usage 未上
  报如实记录）；产物经 ArtifactStore 冻结；Observation 满足
  FrozenObservation 契约（termination 词表映射、usage reported=false）；
  取消 watcher 线程轮询 run cancellation → session.interrupt()；异常路径
  保留原始异常与 evidence，cleanup 失败如实上报残留。
- `runtime_backends.py`：`_build_pi` 接入 pi-agent@1（ExecutionHandle：
  invoke + attach + capabilities{runtime,events,artifacts}）；
  `CANONICAL_RUNTIME_VERSIONS`（四个 backend 的规范定义，与
  runtime-compatibility.json 同构）+ `publish_canonical_runtime_versions`
  （幂等发布进资源仓库）。
- `resolve.py` / `agent_tasks.py` / `execution_backends.py` 的 runtime 条件化：
  agent-tasks 场景显式 runtime 时不强制平台模型/provider、不写 agent 字段；
  provider 注入（max_retries）只在 provider 存在时进行；`runtime_accept_unenforced_tools`
  进入 ResolvedManifest。
- Python 侧工作区：`_PiCaseWorkspace`（fixture 物化路径校验、lstat symlink
  检查、快照 hash、归属校验清理）。执行期边界在 bridge 侧（tools.mjs）；
  POSIX fd 链沙箱（motte_sandbox）在 Windows 上不可用（os.open 目录语义），
  该限制为本机既有环境边界（builtin-agent 的同类用例在主干同样失败），
  已单列不改判。

### 测试命令与结果

```bash
uv run pytest -q -m "not live" tests/integration/test_pi_run_backend.py
# 5 passed（红灯先行：pi build 未接线时 dispatch 显式
# RUNTIME_EXECUTION_NOT_IMPLEMENTED）
uv run pytest -q -m "not live" tests/sdk/test_resolve.py tests/runtime/test_execution_backends.py \
  tests/runtime/test_agent_scoring_passes.py tests/runtime/test_run_service.py tests/contract
# 全绿
uv run pytest -q -m "not live" tests/   # 全量差异对比
# worktree 279 failed / 1341 passed vs main 基线 278 failed —— 逐项差集比对，
# 唯一差异是本测试自身的注册表顺序污染（已修复并复验 19 passed）；
# 其余 278 项失败与主干完全一致（Windows 本机既有：sandbox os.open 目录、
# 进程组语义、symlink 特权），非 M4 回归
uv run ruff check .  # passed
```

### 验收对照

- M4-G04（独立 session/workspace）：主断言 `test_pi_backend_case_isolation_and_scoring`
  —— 两 Case session 集合不相交、事件按 case 归属、fixture 不串案。
- M4-G12（exit/完成 ≠ 质量通过）：forbidden/缺产物 → insufficient_evidence，
  passed=None 不进假成功。
- M4-G11/G05：queued cancel 保留计划分母；桥断连 → failed + 错误入证据，
  retry 显式子 Run；usage/model 计量来源记录（scripted）。
- M4-A03：session 隔离断言。
- M4-A15：禁用 pi-agent 后新 Run 拒绝、历史与事件可读（finally 恢复注册）。

### 限制

- Windows 本机无法运行 motte_sandbox fd 链路径（既有边界，见上）；POSIX
  复验列入最终验收清单。
- scripted model 的 platform-controlled 传输（真实平台 Provider 桥接）待
  live 授权；当前离线证据全部为 runner-configured scripted 路径。

## M4-T05 — 有界 supervisor、启动恢复与清理

**状态：完成（harness + runtime 级离线证据；Windows 本机 + POSIX 待复验）。**

### 实现路径

- `packages/harness-runtime/motte_harness/supervisor.py`（新增）：
  `SupervisedProcess` —— 受控 argv/env/cwd；stdout/stderr 后台线程并行
  增量读取（单行 max_line_bytes / 累计 max_total_bytes / idle / total
  全部有界）；无效 UTF-8 与预算截断分开报告（invalid_utf8 ≠ truncated）；
  `interrupt()` 原生信号（Windows CTRL_BREAK / POSIX 组 SIGINT）→
  interrupt_grace 宽限 → psutil 进程树强杀；父进程退出后 `_collect_residual`
  检测仍在写的后代（宽限 → 清理 → 残留如实上报）。
- `process.py`：`minimal_env`（env allowlist——受控输入通道，注释明确
  不是沙箱）、`descendants_of` / `kill_tree` / `pid_alive` /
  `process_identity` / `identity_matches`（psutil；PID 复用按命令行一致
  性判定，缺失时保守拒绝）。ProcessRunner 的隐式 cmd.exe /c 包装移除
  （无调用方、未经评审的 shell 包装是注入面）。psutil>=7 加入
  motte-harness 依赖。
- `session.py`（新增）：`new_session_record`（start_token 先落盘再
  spawn）/ `persist_session`（CAS）/ `mark_spawned`（进程创建身份）/
  `mark_terminal` / `recover_session` —— 恢复只观察（observe）或标
  needs_review（进程消失 / PID 复用），`replayed` 恒为 false；terminal
  记录不再恢复。
- `protocol.py`：标准事件 envelope 语义（身份 + source_seq +
  parser_version + coverage complete/partial）。

### 测试命令与结果

```bash
uv run pytest -q -m "not live" tests/harness/test_supervised_process.py   # 8 passed
uv run pytest -q -m "not live" tests/runtime/test_runtime_recovery.py     # 5 passed
uv run pytest -q -m "not live" tests/harness tests/runtime/test_pi_protocol.py  # 26 passed
uv run ruff check .  # passed
```

主断言 `test_bounded_pipes_and_descendant_cleanup`：2000 行 stderr 洪泛 +
慢消费者（sleep 1ms/行）不死锁、输出 ≤ 400KB 有界；父进程 final 后孙进程
持续写文件被检出，报告残留时写入已停止。故障矩阵另覆盖：超长单行
fail closed、非法 UTF-8 与截断分离、idle 超时、interrupt 宽限后强杀、
env allowlist 只透传声明变量、缺失二进制显式失败、未启动先 wait 拒绝。

### 验收对照

- M4-G09/A06：双管道并行有界消费，无死锁。
- M4-G10/A08：超时/取消/进程树终止/残留清理。
- M4-G11/A09：恢复只观察或 needs_review，不自动重放；PID 复用拒绝。
- M4-A05：单行超限协议错误。
- M4-A13（凭据不泄漏）：env allowlist 测试（隔离的强制主体按
  runtime-compatibility 声明记录为 runner；平台不虚报隔离）。

### 限制

- CTRL_BREAK_EVENT 只对共享控制台的进程组生效；无控制台宿主（服务化
  Worker）下 interrupt 直接走 terminate（仍满足宽限→强杀语义）。
- POSIX 侧（ps 组信号/proc 枚举）代码就绪，本机为 Windows 证据；
  POSIX 复验列入最终清单。

## M4-T06 — Claude batch / M4-T07 — Codex batch

**状态：完成（harness + integration 级离线证据；真实二进制 live 待授权）。**

### 实现路径

- `parsers/claude.py`（claude-json-v1）：单对象结果按 pinned 2.1.278 形态
  解析——subtype success/error_*、usage（input/output/cache token）、
  total_cost_usd（原生回报才记录）、modelUsage（observed 模型）、
  num_turns；未知 schema/缺 result/未知 subtype → insufficient（coverage
  partial），不假成功；未知多余字段保留清单并降 coverage。
- `parsers/codex.py`（codex-jsonl-v1）：exec JSONL 的 thread/turn/item
  事件——thread_ids、agent_message 文本、command_execution（命令/cwd/
  exit_code）、turn.completed usage、turn.failed、thread 终态；partial
  JSON 保留计数降 coverage；缺终态（exit=0）→ insufficient；turn.failed
  → error；usage/model 缺失保持 unknown（exec 流不回报模型，不用请求值
  冒充 observed；费用恒 unknown 不填 0）。
- `claude.py` / `codex.py` harness：`batch_argv` 按 pinned flag 构造
  （claude：-p/--output-format json/--model/--max-turns/--permission-mode/
  --settings/--add-dir；codex：exec --json/-m/-c/--sandbox/
  --skip-git-repo-check）；`run_batch` 走 SupervisedProcess（受控 cwd/env
  + 有界监督），返回 process 视图 + 原生 parsed。旧 `run()` 保留但语义
  升级为原生 parser（legacy 测试同步更新）。
- `cli_runtime.py`：`CliRuntimeCaseExecutor`（两后端共享）——每 Case
  独立 session/workspace；Claude 写干净 settings（--settings 隔离，
  发现不可禁用时 reproducibility 记 partial）；stderr 事件经
  emit_run_event 持久化（source_seq/parser_version/coverage）；产物/
  Observation/清理同 Pi 模式；usage 映射（claude 报 cost；codex 无 cost
  → unknown）；argv_redacted + env_names 进 envelope。
- `runtime_backends.py`：claude-cli@1 / codex-cli@1 build 接线
  （_build_cli）；canonical config_schema 增加 binary（pinned 二进制
  路径是合法运行时设置）。

### 测试命令与结果

```bash
uv run pytest -q -m "not live" tests/harness/test_claude_batch.py   # 9 passed
uv run pytest -q -m "not live" tests/harness/test_codex_batch.py    # 9 passed
uv run pytest -q -m "not live" tests/integration/test_cli_run_backend.py  # 4 passed
uv run pytest -q -m "not live" tests/harness/  # 43 passed（含既有 install/process 回归）
uv run ruff check .  # passed
```

主断言：`test_claude_terminal_missing_and_config_drift`（缺 final/未知
schema → insufficient 不假成功；usage 缺失保持 unknown）与
`test_codex_partial_json_turn_failure_and_unknown_cost`（partial JSON/
turn.failed/exit=0 缺 terminal/费用模型 unknown）。集成：两后端经真实
dispatcher 全链路（fake binary 注入 native_settings.binary）：评分通过、
产物冻结、usage/cost 按 observed 语义（claude cost=0.002、codex
cost=None）、非零退出保留错误证据且评分不过。

### 验收对照

- M4-G06：transport/flag 固定（batch_argv 断言 pinned 形态）。
- M4-G07：settings 隔离文件 + argv_redacted/env_names 记录；宿主自动发现
  不可完全禁用 → reproducibility 语义在 live 记录中单列。
- M4-G08：source_seq/parser_version/coverage。
- M4-G12/A07：exit=0 缺 final → invalid_state/insufficient，评分不过。
- M4-G13/A10：usage/cost unknown 语义（codex 无 cost、无 observed model）。
- M4-A14：parser_version 随结果记录；不同 parser 版本的历史保留由
  ScoringPass 不可变保证（既有 M1 语义）。

### 限制

- 真实 claude/codex 二进制未安装：离线证据经受控 fake binary（真实
  supervisor/parser/平台链路）；live 小任务按 T09 验收包执行后补充。
- Claude stream-json 与 Codex app-server 未作为 batch fallback（按规格
  单独 transport/version）。

## M4-T08 — API/CLI/Web 完整用户链路 + Provider streaming

**状态：完成（API/CLI/Web + 流式契约级离线证据）。**

### 实现路径

- API：`GET /api/v1/runtimes`（目录 + 分层就绪，零模型调用）、
  `POST /api/v1/runtimes/publish`（幂等发布规范版本）、
  `GET /api/v1/runtimes/{name}/readiness`。Runtime Run 经通用
  `POST /api/v1/runs`（manifest.runtime）创建；cancel/retry/report/
  events SSE 沿用既有端点。OpenAPI + schema.d.ts 同提交再生成。
- CLI：`motte runtime list|readiness|publish`（分层旗标 + 逐层原因）。
- Web：`evalTypes/harness/HarnessPages.tsx` + runtimes 套件注册——目录
  表（类型/传输/固定上游/模型控制/三层就绪/发布态）、未就绪原因清单、
  发布动作、交互通道关闭说明；运行页按 manifest.runtime 过滤。沿
  tokens/Radix-free 现有面板与表格样式（无新 token 需求）。
- Provider streaming（总路线横向项）：`transport.post_sse`（不重试、
  close-on-abort）；`BaseHTTPProvider.stream`（text_delta/tool_delta/
  usage/finish 受控类型、usage_reported 诚实、组装 finish 摘要）；
  openai_compatible / openai_responses / anthropic_messages 各自实现
  `normalize_stream_event`（未知协议 fail closed）。

### 测试命令与结果

```bash
uv run pytest -q -m "not live" tests/provider/test_streaming_contracts.py  # 14 passed
pnpm --dir apps/web test    # 226 passed（16 文件，含 HarnessPages 3 用例）
pnpm --dir apps/web build   # 通过
make openapi && make openapi-check  # openapi.json up to date
uv run pytest -q -m "not live" tests/provider/  # 136 passed（既有 provider 回归）
```

## M4-T09 — 支持证据、验收包与 batch review

**状态：完成（离线证据矩阵；live 验收包就绪、未执行）。**

### 实现路径

- `tests/integration/test_external_runtime_slice.py`：分后端独立证据
  （Pi 真实 SDK 任务 + Claude/Codex fake binary 平台链路；usage 语义各
  自断言）；fake 证据不升级 execution_ready；停用单后端（新 Run 拒绝、
  历史可读、其余后端正常）；取消/失败路径。
- 文档：`docs/operations/pi.md`（分层就绪/离线执行/取消恢复/live 验收
  范围/回退）、`docs/operations/cli-harnesses.md`（argv 固定/证据语义/
  认证边界/live 包/回退）、`docs/protocols/runtime-events.md`（v2 协议、
  parser envelope、trace 持久化、与 Provider 流式终态一致性）。
- Live 验收包（未执行，待授权）：见两份操作手册的"Live 验收"节——
  每后端同一受控文件任务 + 取消 + 非零退出；模型/调用上限/费用边界/
  工作目录/凭据 profile/清理范围需逐项确认后执行。

### 测试命令与结果

```bash
uv run pytest -q -m "not live" tests/integration/test_external_runtime_slice.py  # 3 passed
```

### 支持矩阵（离线证据级别）

| backend | 版本锁定 | fixture | integration | live |
|---|---|---|---|---|
| pi-agent@1 | 0.73.1（lockfile） | 协议夹具 + 真实 SDK scripted | dispatcher 全链路（真实 bridge） | pending |
| claude-cli@1 | 2.1.278（registry） | claude-batch-result-v1（synthetic） | fake binary 平台链路 | pending |
| codex-cli@1 | 0.155.1（registry） | codex-exec-events-v1（synthetic） | fake binary 平台链路 | pending |
| codex-app-server@1 | 0.155.1 | app-server-rpc-v1（synthetic） | T10 合成 RPC | pending |

本机实况：claude 2.1.268 / codex 0.148.0 已安装但与 pinned 漂移 →
协议层 fail closed（如实报告，不放宽矩阵）。

## M4-T10 — Codex app-server command consumer

**状态：完成（合成 RPC + API 链路级离线证据；真实 app-server 会话 live 待授权）。**

- 契约：`RunCommandStatus` 增 rejected/expired/delivery_unknown 终态；
  字段补 case_id/session_id/dedupe_key/expires_at/
  expected_session_revision/request_hash/intervention/actor（storage
  transitions 兼容扩展）。
- `motte_sdk/commands.py`：`submit_command`（dedupe_key 去重、批准绑定
  request_hash/session/revision/过期；202=仅持久接收）、`deliver_pending`
  （CAS queued→delivered→acknowledged；错 session/revision → rejected、
  过期 → expired、transport 异常 → delivery_unknown 不重试危险操作）、
  `recover_delivered_without_ack`（重启恢复 → delivery_unknown）、
  `intervention_summary`（干预进入比较条件输入）。
- `motte_harness/codex_app_server.py`：JSON-RPC stdio 传输子集
  （initialize/thread/turn），pinned 版本门（漂移 fail closed）；离线
  语义验证用合成传输。
- API：messages 端点按**注册后端**判定交互能力（manifest 声明不可信：
  未注册声称 → 501 无消费者，不产生假 accepted——保留既有测试语义）；
  交互 run 提交 → 202 + queued 命令持久化；GET /commands 观察真实 ack。

```bash
uv run pytest -q -m "not live" tests/runtime/test_command_delivery.py  # 5 passed
uv run pytest -q -m "not live" tests/api/test_runs.py tests/api/test_events.py  # 回归全绿
make openapi && make openapi-check  # up to date
```

主断言 `test_commands_ack_expiry_and_no_repeat_approval`：去重/过期/
错 session/revision 拒绝/重启不重复批准/干预入 Run 事件。

限制：真实 codex app-server 会话（本机 0.148.0 与 pinned 0.155.1 漂移）
属 live 验收包；合成 RPC 证据级别单独标注。

## M4-T11 — Inspect 原始日志只读导入

**状态：完成（parser + 导入幂等 + API/CLI 级离线证据）。**

- `parsers/inspect.py`（inspect-jsonl-v1）：固定 schema 子集（header +
  sample 行）；未知 schema/超限/aggregate-only 拒绝；解析无任何执行
  路径（无 pickle/插件/命令加载）。
- `inspect.py`：`import_inspect_log`（内容先 hash 冻结；import_id 由
  内容派生 → 同源重导幂等；同身份异内容冲突显式报错；原生分数
  score_source=inspect-native 与平台重评区分；记录落
  var/inspect-imports/）；`InspectHarness.run` 显式
  INSPECT_EXECUTION_UNSUPPORTED（替换 dry-run 占位）。
- API `POST /api/v1/inspect/import`（受控上传文本；422 结构化拒绝）；
  CLI `motte inspect-import <file>`。
- `docs/operations/inspect-import.md`（格式/限制/使用/回退）。

```bash
uv run pytest -q -m "not live" tests/harness/test_inspect_log_import.py  # 8 passed
```

主断言 `test_inspect_import_never_executes_log`：日志内命令/路径仅作
数据；无文件系统副作用。

# 最终回归、Review 与状态声明（2026-09-20）

## 退出门禁执行记录

| 命令 | 结果 | 说明 |
|---|---|---|
| `uv run pytest -q -m "not live" tests/protocol tests/harness tests/runtime tests/storage` | 49 failed / 369 passed / 26 skipped | 49 个失败与主干 `4f5918e` 基线**逐项差集为空**（本机 Windows 既有：sandbox os.open 目录语义、进程组、symlink 特权——M3 记录的通过证据来自 POSIX 环境）；M4 新增测试全部通过 |
| `uv run pytest -q -m "not live" tests/provider/test_streaming_contracts.py tests/integration/test_external_runtime_slice.py` | 17 passed | |
| `pnpm --dir bridges/pi test`（selftest） | ok | 真实 SDK + scripted model 写文件 |
| `pnpm --dir apps/web test` | 226 passed / 16 文件 | 含 HarnessPages 3 用例；一次全量曾现 TerminalBench R15 时序抖动，复跑稳定全绿 |
| `pnpm --dir apps/web build` | 通过 | |
| `make openapi-check` | openapi.json up to date | API 变更均同提交再生成 |
| `make check` | test 段 278 failed / 1407 passed | 278 与主干基线完全一致（同上环境边界），exit 1 由既有失败导致；lint（ruff+mypy contracts+compileall）通过、web 段单独验证通过 |
| `validate_plan.py --allow-code-changes` | 通过 | 137G/77T/119A、依赖无环、755 链接 |
| `uv run ruff check .` | All checks passed | |

## 跨层 review 结论（公共请求→资源解析→冻结→dispatch→启动→事件→持久化→评分→展示）

- 身份链：runtime 引用在 prepare/创建/dispatch 三点核验（published 快照
  + backend 身份一致）；每事件带 run/case/session/operation + 单调 seq，
  写库前 fail closed（协议测试矩阵覆盖错身份/seq 回退/未知事件）。
- 配置链：runtime_profile 经 config_schema allowlist；not-enforced 工具
  边界必须显式确认；usage/cost/model 只取 observed（scripted/codex 缺失
  → unknown，不填 0/请求值）。
- 生命周期：queued cancel 保留计划分母；桥断连/非零退出保留错误证据且
  不自动重放（retry 显式子 Run）；恢复只观察或 needs_review（PID 复用
  拒绝触碰）；202 ≠ 送达（注册后端门 + delivery_unknown 终态）。
- 升级/禁用：backend 注销后新 Run 拒绝、历史与其余后端可读（测试覆盖）。

## P0/P1/P2 修复记录

开发中即时修复（红→绿同包关闭）：bridge 控制消息串行化竞态、
`interrupted` 先于 `finished`、budget 用 `beforeToolCall` 确定性阻断、
`emit is not defined`（T03）；`os.walk` 字符串路径、termination 词表、
contains 规则键、注册表顺序污染（T04）；fixture repr/分支键/f-string
迭代（T06/T07）；循环导入自装（T08）；manifest 声明交互不可信（T10，
保留既有测试语义）。未遗留已知 P0/P1/P2。

## 状态声明

- **代码/离线验证完成**：T01–T11 全部实现并有对应级别离线证据
  （contract/storage/protocol/integration/web）。
- **live 待验收**：Pi 真实模型、Claude/Codex 真实小任务（本机二进制
  与 pinned 漂移：claude 2.1.268/2.1.278、codex 0.148.0/0.155.1 →
  fail closed，须先对齐版本并按 T09 验收包授权执行）、真实 app-server
  会话、PG E2E、POSIX 复验。上述任一完成前，M4 状态为
  **implementation_complete / live_pending**，不宣称完整 M4 完成。
- 分支 `codex/m4-pi-external-harnesses` 保留供 review；未合并、未推送。








