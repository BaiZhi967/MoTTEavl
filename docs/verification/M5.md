# M5 验证记录：场景、Skill 与 Judge

状态：**in_progress / 未完成**。本文只登记实际运行过的命令与结果；子 Agent 的
自述结果单独标注"子包自述、未独立复核"，不当作通过证据。

- 分支：codex/m5-scenarios-skills-judges
- 起始基线：adf468aec4b46ab5f17db7ded56354d93dd2673e（M4 合入点）
- Alembic 起始 head：0010_interactive_sessions；本轮新增 0011、0012、0013
- 环境：Windows，uv Python 3.12，Node 24 / pnpm 9.15
- 实际费用：**0**。未授权、未调用任何付费模型、Judge 或外部 CLI。

## 1. 提交

| 提交 | 内容 | 作者 |
|---|---|---|
| fd890d9 | T01 契约/条件/旧 DSL 转换/workflow_versions + 0011 | 主 Agent |
| f3d5ae9 | T05 身份分层与快照装配 + T08 比较因子 | 主 Agent |
| 522e8a1 / 70f50c1 | T03b 有界引擎 / 受控进程真实停止 | 主 Agent |
| 357e6f1 | T05 可用性事实开关 | 主 Agent |
| eaf1a27 | T06 Skill 三形态与安全导入 | 子包 |
| d527689 | T02 Fixture 所有权/隔离/清理 | 子包 |
| 91baa31 | T09a Judge 契约与零成本预检 | 子包 |
| 015f1d2 | T03a case-owned Builtin session | 子包 |
| b9f0dfc | T04 Workflow 断言指标 | 子包 |
| 19246b5 | T05 执行器/目标 adapter/目标身份 | 主 Agent |
| 79ab229 | T07 Skill 注入与权限交集 | 主 Agent |
| 002df70 | fixtures/skills 三存储资源 + 0013 | 主 Agent |
| 23dbbcd | 本验证记录矩阵 | 主 Agent |
| 8c75be8 | T11 Web 对齐已落地 API 契约（修复路径不一致） | 子包 |
| c951c1c | T10 校准集、逐 criterion 分歧、experimental 状态与人工修订 | 子包 |
| cc724d7 / b353647 / f083afa | T09b 持久作业与账本、原子发布与崩溃窗口验证 | 子包 |
| ca02661 / 30fdb8c / 02d4b63 | PROGRESS 记录、T05 fixture 需求修复、T09a 惰性导入修复 | 主 Agent / 子包 |

## 2. 实际运行并取得结果的命令

| 命令 | 结果 |
|---|---|
| uv run pytest -q tests/contract/test_workflow_spec.py | 45 passed |
| uv run pytest -q tests/scenario/test_workflow_conversion.py | 29 passed |
| uv run pytest -q tests/scenario/test_workflow_engine.py | 18 passed |
| uv run pytest -q tests/scenario/test_target_stop_confirmation.py | 5 passed（真实阻塞子进程被终止，residual_pids 为空） |
| uv run pytest -q tests/scenario/test_scenario_run_vertical.py | 6 passed（含同业务 ID 两 Case 隔离、A02 过程失败、未实现工具具名拒绝） |
| uv run pytest -q tests/scenario/test_scenario_backend_availability.py | 7 passed |
| uv run pytest -q tests/storage/test_workflow_resources.py | 13 passed（memory+sqlite） |
| uv run pytest -q tests/storage/test_scenario_skill_resources.py | 16 passed（memory+sqlite，含 Skill 弃用转换落库） |
| uv run pytest -q tests/storage/test_postgres_repository.py | 11 passed（迁移链含 0011/0012/0013） |
| uv run pytest -q tests/evaluators/test_m5_comparison_factors.py | 14 passed |
| uv run pytest -q tests/skill | 60 passed, 1 skipped（Windows 无符号链接权限） |
| uv run pytest -q tests/storage tests/contract tests/scenario tests/skill + T04/T08 定向 | 661 passed, 32 skipped, 3 failed |
| uv run ruff check . | 主 Agent 提交时点通过；随后子包新增文件出现 1 处 F841（tests/evaluators/test_judge_execution.py） |
| uv run mypy packages/contracts | 子包报告 28 源文件通过（子包自述） |
| **全量非 live 套件（当前分支 HEAD）** | **316 failed / 1998 passed / 54 skipped**（330 秒） |
| **全量非 live 套件（pre-M5 基线 adf468a 的独立 worktree）** | **315 failed / 1625 passed / 52 skipped**（307 秒） |
| 差值 | M5 新增 **+373 通过**、**+1 失败** |
| 失败分类 triage（当前 HEAD，316 条） | **M5 新增测试文件失败数 = 0**（test_workflow* / test_scenario* / test_skill* / test_judge* / test_scoring_jobs / test_m5_comparison* / test_skill_ablation / test_fixture_lifecycle / test_agent_session 全部不在失败列表内）；316 条全部落在既有文件族：harbor benchmark 政策/身份 42+12、terminalbench API 31、M2 review 16+13+12、harbor 集成 11+10、terminalbench 指纹 11、runtime review 10、external job 10、pi 真实 adapter 9 等 |
| 结论与边界 | M5 未使任何**新增**测试失败，属既知 Windows 环境族（目录句柄权限、外部作业子进pend、tmp 工作区权限）。净 +1 落在上述既有族内，**精确 node id 差集尚未产出**，故不宣称"316 条全部为环境问题" |
| uv run ruff check .（当前 HEAD） | All checks passed |
| tests/storage/test_external_job_store.py 基线对照（19246b5 worktree） | 3 项同样失败 → 与本轮改动无关 |

失败项分列（未计入通过）：

- tests/storage/test_external_job_store.py 3 项：在 19246b5 的独立 worktree 中同样失败，
  属既有环境问题（子进程存活判定），非本轮回归。
- tests/evaluators/test_terminalbench_fingerprint.py 11 项：Windows 目录句柄
  （O_DIRECTORY/PermissionError）环境问题，子包以 pristine worktree 对照证明同样失败。
- tests/runtime 48 项：子包以 pristine 副本集合一致对照，属既有环境失败。

未运行：真实 PostgreSQL 迁移/事务、完整门禁、OpenAPI 漂移、Web test/build
（原因见第 5 节）。

## 3. 目标矩阵（M5-G01–G22）

| 目标 | 主责包 | 状态 | 依据 / 缺口 |
|---|---|---|---|
| G01 Scenario 引用已发布 Workflow | T01/T05 | 契约通过、装配已交付、**公开执行仍关闭** | 引用解析/draft 拒绝/快照冻结/身份分层有测试；scoring 投影未接，故 available=False |
| G02 七类步骤、when/expect、有界嵌套 | T01/T03 | **通过** | 契约 45 + 引擎 18 |
| G03 只访问许可字段、无 eval | T01 | **通过** | 受限条件编译器与恶意表达式反例 |
| G04 Fixture 所有权与失败证据 | T02 | 实现完成（子包自述 + 存储层已接） | 20 项子包用例；主 Agent 未独立复核 |
| G05 Case 内保留多轮上下文 | T03a | 实现完成（子包自述） | 19 项子包用例；one-shot 兼容保持 |
| G06 步骤期限能真实中断 | T03b | **通过** | 受控进程被真实终止；未确认停止转 needs_review |
| G07 四种工具 mode 与权限交集 | T05/T07 | 交集**通过**、执行期网关已实现 | injection.intersect_permissions 14 项；执行期 select_for_execution 有测试 |
| G08 过程/状态/副作用可评分 | T04 | 实现完成（子包自述） | 5 种 metric kind + 16 项反例；主 Agent 未独立复核 |
| G09 隐藏断言/gold/checker 不可见 | T02/T04 | 部分通过 | 垂直用例断言工具结果不含隐藏字段；T04 的 owner/hash 校验为子包自述 |
| G10 Skill 三种形态 | T06 | 实现完成（子包自述） | 47 项子包用例（1 skipped） |
| G11 SkillVersion 固定内容/hash/依赖 | T06/T07 | **通过**（存储+注入身份） | 异内容冲突、弃用不改变 hash、顺序改变 plan_hash |
| G12 Skill fixture 三类验证范围 | T07 | 部分 | validation_scope 三分与 native-loader=partial 已实现；executable fixture 执行未接 |
| G13 三臂相同条件比较 | T08 | 契约**通过**、三臂执行未交付 | 14 项比较因子用例 |
| G14 开销/次数/成本不重复计费 | T07/T08 | 部分 | 指令 token 开销单列且标 estimated；三臂执行未接 |
| G15 独立固定 Judge/rubric 输入 | T09 | 实现中（T09a 已提交） | — |
| G16 非成功输出不伪造分数 | T09 | 实现中 | — |
| G17 授权、用途、预算、不重付 | T09 | 实现中（未提交） | scoring_jobs 尚未落盘 |
| G18 至少 30 条人工复核样本 | T10 | **not_run（外部依赖）** | 无真实人工标签，不得用合成标签冒充 |
| G19 新 pass、旧 baseline 不变 | T09/T10 | 实现中 | — |
| G20 代表性业务七类边界 | T05/T12 | 部分 | 引擎级与垂直级覆盖确认/未确认/工具错误/拒绝/超时；公共七类回归未做 |
| G21 API/CLI/Web 可用 | T11 | 实现中（未提交） | — |
| G22 既有套件与恢复无回归 | 全部 | 部分 | 661 passed；完整门禁未运行 |

## 4. 验收矩阵（M5-A01–A18）

| 验收 | 状态 | 证据 / 缺口 |
|---|---|---|
| A01 确认后且只取消一次 | **通过**（引擎级 + 垂直级） | 两轮确认、count == 1、消息序列与最终输出断言 |
| A02 提前取消即过程失败 | **通过**（引擎级 + 垂直级） | 终态 cancelled 但过程断言失败 → failed |
| A03 初始化失败有独立清理证据 | 实现完成（子包自述） | 主 Agent 未独立复核 |
| A04 错误/恢复可见、副作用不重复 | **通过**（引擎级） | evidence-only 降级 + 工具暂态错误恢复 |
| A05 缺字段/恶意表达式明确拒绝 | **通过** | CONDITION_PATH_MISSING；旧 DSL 恶意表达式诊断 |
| A06 步数/时长限制和实际中断 | **通过** | 真实阻塞子进程终止 + 未确认停止 needs_review |
| A07 副作用后崩溃 needs_review 零重放 | 未交付 | 需要 Worker 级 CaseAttempt 恢复路径与公共 Run |
| A08 同业务 ID 不同 Case 不串扰 | **通过** | 引擎级 + 垂直级（两次调用初态独立、实例根不同） |
| A09 Skill 不扩权或提升工具 mode | **通过**（交集层） | 契约拒绝绝对路径/开放网络；mock 不能变 real；未授予工具不可用 |
| A10 纯指令无需入口、验证标签诚实 | **通过** | instruction 无 entrypoint 可发布；scope 三分 |
| A11 恶意资源/未固定依赖拒绝 | 实现完成（子包自述） | 子包 47 项含各类拒绝码 |
| A12 预算或其他条件变更不能归因 Skill | **通过**（比较契约） | SKILL_ATTRIBUTION_UNSAFE |
| A13 未授权零调用 | 实现中（T09a 已提交） | — |
| A14 注入候选无工具权限 | 实现中 | — |
| A15 畸形/拒绝/超时/伪引用不给 pass | 实现中 | — |
| A16 Judge 不确定不重复付费 | 实现中 | — |
| A17 rubric/人工修订新 pass | 实现中 | — |
| A18 所有 GET 与默认 rescore 零 Judge 调用 | 未验证 | — |

## 5. 未运行项与原因（与通过项分开）

| 未运行项 | 原因 |
|---|---|
| 真实 PostgreSQL 迁移/事务（0011–0013 升级、回退、旧数据） | 本机无可用 PG 实例；不把静态 DDL 核对当作数据库验收 |
| 完整门禁（lint + 全量测试 + web build + compose config + openapi 漂移） | 结束时仍有 T09/T10、T11-API、T11-Web 在写同一工作区 |
| OpenAPI 导出与 TS 生成 | 路由集合仍在变动 |
| Web test 与 build | T11-Web 进行中 |
| T09b/T09c、T10、T11 的验收 | 子包未提交/未完成 |
| G18 真实人工校准 | 外部依赖：无真实人工复核标签与费用授权 |
| Judge 真实调用与费用证据 | 未授权付费模型或凭据 |

## 6. 结论与回退

M5 **未完成**。已实现且由本轮实际运行证明的部分见第 2–4 节；T02/T03a/T04/T06
为子包自述，T09b/T09c/T10/T11 仍在进行。

回退：保持 SCENARIO_BACKEND_AVAILABLE=False 并停止相关路由/领取；先确认活动受控
进程停止，再做受控资源清理。保留 Workflow/Fixture/Skill 版本、ScoringPass、job
不确定记录、校准与人工修订、原始证据与导入来源。0011 与 0013 的 downgrade 在存在
版本行或被引用的 Run manifest 时具名拒绝（tests/storage/test_workflow_resources.py、
tests/storage/test_scenario_skill_resources.py 覆盖）。
