# M5 验证记录：场景、Skill 与 Judge

状态：**in_progress / 未完成**。本文只登记本轮**实际运行**过的命令与结果，
不把计划、子 Agent 交接声明或"已有基础设施"当作通过证据。

- 分支：codex/m5-scenarios-skills-judges
- 起始基线：M4 合入点 adf468aec4b46ab5f17db7ded56354d93dd2673e（分支创建时），
  实际分支基点另含交接文档提交 a7b2d96
- Alembic 起始 head：0010_interactive_sessions；本轮新增 0011_workflow_resources
- 环境：Windows 10，项目 uv Python 3.12，Node 24 / pnpm 9.15
- 实际费用：**0**。本轮未授权、未调用任何付费模型、Judge 或外部 CLI。

## 1. 本轮提交

| 提交 | 内容 |
|---|---|
| fd890d9 | T01 Workflow DSL 契约、受限条件、旧 DSL 只读转换、workflow_versions 资源与 0011 迁移 |
| f3d5ae9 | T05 身份分层/快照装配 + T08 比较因子（workflow/fixture/skill/judge/rubric/calibration/budget_policy） |
| 522e8a1 | T03b 有界 Workflow 引擎（全局预算、失败政策、后序证据） |
| 70f50c1 | T03b 受控进程 Target 与真实停止确认 |
| 357e6f1 | T05 可用性事实开关：纵向链路交付前公开执行明确 unavailable |

## 2. 实际运行过并取得结果的命令

| 命令 | 结果 |
|---|---|
| uv run pytest -q tests/contract/test_workflow_spec.py | 45 passed |
| uv run pytest -q tests/scenario/test_workflow_conversion.py | 29 passed |
| uv run pytest -q tests/storage/test_workflow_resources.py | 13 passed（memory + sqlite 双存储参数化） |
| uv run pytest -q tests/scenario/test_workflow_engine.py | 18 passed |
| uv run pytest -q tests/scenario/test_target_stop_confirmation.py | 5 passed（真实阻塞子进程被终止） |
| uv run pytest -q tests/scenario/test_scenario_backend_availability.py | 7 passed |
| uv run pytest -q tests/evaluators/test_m5_comparison_factors.py | 14 passed |
| uv run pytest -q tests/sdk tests/contract tests/trace | 通过，无失败 |
| uv run pytest -q tests/evaluators | 11 failed，全部在 tests/evaluators/test_terminalbench_fingerprint.py |
| uv run ruff check . | All checks passed（最后一次本机检查时点） |
| uv run mypy packages/contracts | Success: no issues found in 27 source files |
| uv run pytest -q tests/benchmarks（两次） | 第一次 70 failed，重跑 0 failed；原因是 Windows 目录句柄在并发测试进程下不可用，与本次改动无关 |

未运行：真实 PostgreSQL 迁移与事务、完整门禁、OpenAPI 漂移检查、Web test 与
build。原因见第 5 节。


## 3. 目标矩阵（M5-G01–G22）

| 目标 | 主责包 | 状态 | 依据 / 缺口 |
|---|---|---|---|
| G01 Scenario 引用已发布 Workflow | T01/T05 | 契约通过，纵向未交付 | 引用解析、draft 拒绝、快照冻结、身份分层有测试；公开 create 在装配层交付前明确 unavailable |
| G02 七类步骤、when/expect、有界嵌套 | T01/T03 | 通过 | tests/contract/test_workflow_spec.py、tests/scenario/test_workflow_engine.py |
| G03 只访问许可字段、无 eval | T01 | 通过 | 受限条件编译器与恶意表达式反例 |
| G04 Fixture 所有权与失败证据 | T02 | 实现完成、待独立复核 | d527689；tests/scenario/test_fixture_lifecycle.py 20 项（子包自述，本轮未独立复核） |
| G05 Case 内保留多轮上下文 | T03a | 实现中 | builtin_react.py + session.py 已改 |
| G06 步骤期限能真实中断 | T03b | 通过 | 受控进程目标在期限后被真实终止，residual_pids 为空 |
| G07 四种工具 mode 与权限交集 | T05/T07 | 未交付 | targets.py 的能力交集已测试；执行期权限网关在 T07 |
| G08 过程/状态/副作用可评分 | T04 | 实现中 | motte_eval/workflow.py 已落盘 |
| G09 隐藏断言/gold/checker 不可见 | T02/T04 | 部分 | PrivateTruth 与 minimal_visible_fields 已实现（T02 自述） |
| G10 Skill 三种形态 | T06 | 实现完成、待独立复核 | eaf1a27；tests/skill 47 项（1 项在 Windows 跳过） |
| G11 SkillVersion 固定内容/hash/依赖 | T06/T07 | 部分 | 版本与内容 hash 由 T06 实现；注入与生效 hash 在 T07（未交付） |
| G12 Skill fixture 三类验证范围 | T07 | 未交付 | — |
| G13 三臂相同条件比较 | T08 | 契约通过、执行未交付 | tests/evaluators/test_m5_comparison_factors.py 14 项 |
| G14 开销/次数/成本不重复计费 | T08/T09 | 未交付 | 比较条件已允许 skill 与 budget_policy 因子 |
| G15 独立固定 Judge/rubric 输入 | T09 | 实现中 | judge.py/rubrics.py 已落盘 |
| G16 非成功输出不伪造分数 | T09 | 实现中 | 同上 |
| G17 授权、用途、预算、不重付 | T09 | 实现中 | scoring_jobs 尚未落盘 |
| G18 至少 30 条人工复核样本 | T10 | not_run（外部依赖） | 无真实人工标签；不得用合成标签冒充 |
| G19 新 pass、旧 baseline 不变 | T09/T10 | 实现中 | — |
| G20 代表性业务七类边界 | T05/T12 | 未交付 | 引擎级边界可用；公共链路未接 |
| G21 API/CLI/Web 可用 | T11 | 实现中 | — |
| G22 既有套件与恢复无回归 | 全部 | 部分 | tests/sdk、tests/contract、tests/trace 通过；完整门禁未运行 |

## 4. 验收矩阵（M5-A01–A18）

| 验收 | 状态 | 证据 / 缺口 |
|---|---|---|
| A01 确认后且只取消一次 | 引擎级通过 | 两轮确认、取消计数为 1、消息序列断言 |
| A02 提前取消即过程失败 | 引擎级通过 | 终态 cancelled 但 before-confirm 断言失败，状态为 failed |
| A03 初始化失败有独立清理证据 | 实现完成待复核 | T02 自述：中途 prepare 失败仍清理并落盘证据 |
| A04 错误/恢复可见、副作用不重复 | 引擎级通过 | continue_for_evidence 只读降级与工具暂态错误恢复 |
| A05 缺字段/恶意表达式明确拒绝 | 通过 | CONDITION_PATH_MISSING 而非 false |
| A06 步数/时长限制和实际中断 | 通过 | 真实阻塞子进程被终止；未确认停止转 needs_review 并保留 residual 证据 |
| A07 副作用后崩溃 needs_review 零重放 | 未交付 | 需要 T05 的 CaseAttempt 恢复路径 |
| A08 同业务 ID 不同 Case 不串扰 | 引擎级通过 | test_case_isolation_between_two_engine_instances |
| A09 Skill 不扩权或提升工具 mode | 未交付 | 能力交集已实现；执行期网关在 T07 |
| A10 纯指令无需入口、验证标签诚实 | 实现完成待复核 | T06 自述：instruction 无 entrypoint 可发布，executable 无入口拒绝 |
| A11 恶意资源/未固定依赖拒绝 | 实现完成待复核 | T06 自述：各拒绝码均有用例 |
| A12 预算或其他条件变更不能归因 Skill | 通过（比较契约） | SKILL_ATTRIBUTION_UNSAFE |
| A13 未授权零调用 | 实现中（T09） | — |
| A14 注入候选无工具权限 | 实现中（T09） | — |
| A15 畸形/拒绝/超时/伪引用不给 pass | 实现中（T09） | — |
| A16 Judge 不确定不重复付费 | 实现中（T09） | — |
| A17 rubric/人工修订新 pass | 实现中（T10） | — |
| A18 所有 GET 与默认 rescore 零 Judge 调用 | 未验证 | 需要 T09/T11 落地后实测 |

## 5. 未运行项与原因（与通过项分开）

| 未运行项 | 原因 |
|---|---|
| 真实 PostgreSQL 迁移与事务（0011 升级、回退、旧数据） | 本机无可用 PG 实例；未把静态 DDL 核对当作数据库验收 |
| 完整门禁（lint + 全量测试 + web build + compose config + openapi 漂移） | 本轮结束时仍有多个子包在写同一工作区，结果不稳定；必须在全部写入停止后重跑 |
| OpenAPI 导出与 TS 生成 | 路由集合仍在变动，生成物必然漂移 |
| Web test 与 build | T11-Web 子包进行中，未取得结果 |
| T03a、T04、T09、T10、T11 的最终验收 | 已委派给隔离子包，本轮结束时仍运行；自述结果未经独立复核，不得据此勾选目标 |
| G18 真实人工校准（至少 30 条） | 外部依赖：需要真实人工复核标签与 rubric/模型/费用授权 |
| Judge 真实调用与费用证据 | 未授权任何付费模型或凭据；不得使用主机个人登录态 |

## 6. 剩余阻断与回退

阻断（按依赖顺序）：

1. T05 装配层缺失：引擎、Fixture、条件与契约都已实现并有测试，但没有把它们接进
   既有 CaseAttempt / InvocationRecord / FrozenObservation / ScoringPass 的装配层。
   scenario@1 以 available=False 注册，创建期返回 EXECUTION_BACKEND_UNAVAILABLE，
   不静默改选其他 backend（357e6f1 锁定）。
2. T07/T08 执行期权限与三臂执行：比较条件已实现，但缺少 Skill 注入编译与权限网关。
3. T09/T10 持久作业与校准：契约与存储正在子包中实现，尚未独立复核。
4. T11 API/CLI/Web：子包进行中。
5. 存储接线未完成：fixtures 与 skills 的资源表（RESOURCE_TABLES、_validate_managed、
   VERSIONED_TABLES）与对应 Alembic 迁移（0012 fixture_versions、0013 skill_versions）
   尚未添加；两个子包均按约定把该步骤留给了主 Agent。
6. packages/scenario-runtime/pyproject.toml 尚未声明 motte-sandbox 依赖（T02 使用
   了 motte_sandbox.workspace 的路径规则）；当前靠根项目依赖与 pytest pythonpath 工作，
   需要补依赖并更新 uv.lock。
7. 旧项目固定源码不可用：docs/migration/workflow-parity.md 第 5 节；转换器按文档化
   语义实现，源码级逐字段对齐仍为 not_run。

回退：保持 SCENARIO_BACKEND_AVAILABLE=False 并停止相关路由/领取；先确认活动受控
进程停止，再做受控资源清理。保留 Workflow 快照、Fixture 版本、Skill 版本、
ScoringPass、job 不确定记录、校准与人工修订、原始证据与导入来源；不得用删除历史
掩盖失败。0011 迁移的 downgrade 在有 Workflow 版本或引用它的 Run manifest 时具名
拒绝（tests/storage/test_workflow_resources.py 覆盖）。

## 7. 结论

M5 **未完成**。已实现并可核验的是：Workflow/Fixture 版本化契约与受限条件、旧 DSL
只读转换与诊断、workflow_versions 三存储资源与迁移、有界 Workflow 引擎（全局预算、
失败政策、后序证据、真实停止确认）、scenario 身份分层与可用性事实开关、M5 比较
因子，以及 T02/T06 两个隔离包的自述实现与其提交。其余工作包仍在实现中或尚未开工。
只有在本页补齐每个目标与验收的可核验依据、并在地写入全部停止后重跑完整门禁
（含真实 PG 与 Web），才能称 M5 交付。
