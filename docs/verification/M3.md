# M3 验证记录（Harbor · Terminal-Bench）

按工作包记录真实命令、环境与结果。`not_run` / `blocked` 表示该验证尚未执行，
不以离线测试替代；skip 不作为通过证据。

- 环境：macOS（darwin 25.3.0 arm64）、Python 3.12（uv）、Node 24 / pnpm 9.15.0；
  Docker **27.4.0**（`linux/arm64` 引擎，本机 `docker version` 实测）。
- 日期：2026-09-20。
- 固定 Runner：`harbor 0.23.0` 装在独立 Python 3.12 venv
  （`/tmp/motte-harbor-runner`，`uv venv` + `uv pip install harbor==0.23.0`），
  依赖锁冻结为 `scripts/runner/harbor-0.23.0-py312.lock`；部署脚本
  `scripts/runner/install-harbor`、入口 `scripts/runner/harbor-entry`。
- 本记录未运行 `make check`（见"阶段收尾"），因此不复制任何门禁通过摘要。

## 真实固定 Runner + 真实 Docker 校准（2026-09-20）

用固定 Harbor 0.23.0 与真实 Docker 跑仓库自有确定性任务（oracle Agent，
**无任何付费模型调用、无成本**）：

| 运行 | 配置 | 结果 |
|---|---|---|
| 通过/失败校准 | 2 Task（`hello-pass` / `hello-fail`）× 2 attempt，`n_attempts=2`、`retry.max_retries=0`、`n_concurrent_trials=2` | `hello-pass` reward **1.0**、`hello-fail` reward **0.0**，两次 attempt 一致；`pass@k` 出现在 Harbor 自己的 job result |
| 错误路径校准 | 各 1 Trial 的 `agent-timeout` / `verifier-timeout` | `AgentTimeoutError`（Verifier 仍产出 reward **0.0**）与 `VerifierTimeoutError`（**无 reward 文件**）分别被解析为有效失败与 Verifier 错误 |

原始输出经 `scripts/runner/capture-harbor-fixtures` 脱敏进仓库：

- 样本：`tests/fixtures/benchmarks/harbor/samples/pass-fail-2x2`、
  `tests/fixtures/benchmarks/harbor/samples/errors-timeout`；
- 绝对宿主路径替换为 `<HOST_PATH>`，traceback 替换为
  `<TRACEBACK_REDACTED_BY_CAPTURE>`，超长日志显式截断（不静默）；
- `SOURCES.json` 记录文件清单、逐文件 sha256 与 `sanitized` 策略
  （本次未记录字节大小；需要大小需重新捕获）。

**边界声明**：捕获的 fixture **不含任何秘密、不含宿主路径**；两次校准只使用
确定性 oracle Agent，未调用任何付费模型。真实固定 Runner + 真实 Docker 的
组合仅在下方矩阵登记的范围内成立（见 `docs/operations/harbor-compatibility.md`）。

## M3-T01：受控任务目录 → TaskIdentity

- 范围：路径规范化、内容 hash、来源/revision 身份、只读准备限额与越界拒绝。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_task_identity.py` | **13 passed** |

- 覆盖点：同名不同路径不合并、同字节准备幂等、内容变化 = 新身份、manifest
  漂移检测、路径归一化/逃逸拒绝、symlink 拒绝、限额与非法候选显式登记、
  未钉住 revision 拒绝、**prepare 不执行任何东西**、仓库夹具准备、环境 digest
  随安全 extra 变化。

## M3-T02：TaskPlan → TrialPlan / Trial 存储

- 范围：Trial 契约、CaseAttempt 的 trial 维度、Memory/SQLite/PG 同语义存储、
  migration `0008_trials`、旧读兼容。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/storage/test_trial_store.py` | **15 passed, 1 skipped**（`test_postgres_trials_roundtrip` 需 `MOTTE_PG_DSN`，本机未设 → **not_run**，CI postgres service 执行） |

- 覆盖点：多 Trial 不覆盖 Case、重复创建 identical/冲突显式、结果导入幂等且
  终态不被覆盖、传输重试不产生新 Trial、attempt 冲突域收窄到 Trial、旧
  attempt/Case 语义不变、SQLite 旧库就地补列、ScoreSet 复用 trial 维度。

## M3-T03：Profile → 原生配置

- 范围：Harbor 原生 JobConfig 转换的 allowlist、Agent/环境放行、重试不叠加、
  凭据仅引用、配置快照与 hash。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_config.py` | **7 passed** |

- 覆盖点：未知字段拒绝且默认值明确、未验证 Agent/环境拒绝、原生 retry 不叠加、
  凭据引用不泄漏、计划与任务集合必须一致、换 Agent/环境改变冻结身份、
  相对任务路径只在 Runner 侧投影为绝对路径。

## M3-T04：冻结原始产物 → TrialResult

- 范围：纯 Parser（只消费已冻结字节）、reward 四态、终止分类、证据完整度、
  同名任务拒绝合并、unmapped/unplanned 显式登记。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_parser.py` | **13 passed**（对真实捕获 fixture 运行） |

- 覆盖点：reward 0/缺失/畸形/Verifier 错误四态、Verifier 错误不是质量失败、
  全部计划 Trial 都有 disposition、repeat 分配确定且稳定、同 basename 不合并、
  unmapped/unplanned 上报、证据完整度诚实降级、时长分相、用量 unknown 不填 0、
  取消不覆盖已确定结果、证据 hash 冲突不静默覆盖、Parser 不读文件系统。

## M3-T05：Task/环境 → preflight

- 范围：Docker/平台/磁盘/镜像/依赖、凭据与宿主暴露、网络策略、Verifier
  可见性、自定义 Profile 指纹；fail-closed 且零模型调用。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_preflight.py` | **4 passed** |

- 覆盖点：资源/可见性不满足即拒绝、任务容器不得获得宿主秘密与 Docker socket、
  自定义安全 Profile 改变指纹、探测报告读取严格。

## M3-T06/T07：ExternalJob → 真实 Harbor；取消/崩溃 → 安全收尾

- 范围：Harbor adapter（配置落盘零执行、受控读取产物、纯 Parser 采集）、
  Runner 桥接（原生 JobConfig 构造校验、job-location、原子完成标记）、
  资源账本与恢复只观察。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/integration/test_harbor_job_fixture.py` | **7 passed, 1 skipped**（真实 Runner 用例需 `MOTTE_HARBOR_RUNNER_PYTHON`，未设即 skip；skip 不作为通过证据） |
  | `scripts/runner/verify-harbor-local /path/to/runner/bin/python` | 独立 live 校准入口（内部运行 `tests/integration/test_harbor_calibration.py`）；本记录**未执行**（not_run），真实 Docker 校准证据见本文首节 |

- 覆盖点：一 Job 两 Task 各两 Trial 只启动一次、全部计划单元有 disposition、
  结果幂等导入 Trial 存储、导入中断后恢复不重启、证据冲突阻断且不覆盖、
  资源账本 owner token/容器标签、清理报告 clean/residual、Verifier 可见性与
  未知用量如实记录、Agent 超时与 Verifier 超时区分。

## M3-T08：Trial evidence → ScoreSet

- 范围：Trial 层分数行、Task 聚合（`first-trial` / `mean-success`）、覆盖门禁、
  成本与时长视图、重评分条件。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/evaluators/test_harbor_metrics.py` | **9 passed** |

- 已复验数值：有效 Trial 通过率 **1/2**、有效覆盖 **2/3**、完整覆盖门禁
  **不通过**（不把出错 Trial 删掉后只报 50%）；reward=0 是有效失败；缺证据
  既不是通过也不是 0 分；计划分母覆盖"从未上报"的单元；成本未知保持 `null`、
  零成功单位成本不适用；Agent/Verifier/环境时长分相；重评分条件不含任务字节。

## M3-T09：公共 API / CLI / Web 工作区

- 范围：SDK 门面 `motte_sdk.terminalbench`、API 端点、CLI `terminal-bench`
  子命令、Web 五页；三端同一事实源，执行只入队。
- 命令与结果（本机，2026-09-20）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/api/test_terminalbench_flow.py` | **6 passed** |
  | `pnpm --dir apps/web test -- tests/TerminalBenchPages.test.tsx` | **12 passed** |

- 覆盖点：prepare 只读且回报身份、预检给出可执行原因且不启动任何东西、
  创建需 adapter 连接且预检通过、Task/Trial 下钻保留错误与 pending、概览报告
  Runner 连接、计划 Trial ID 跨请求稳定。

## M3-T10：校准、完整 review 与交付

- 范围：本组文档（操作指南、兼容矩阵、身份协议、清理 runbook、本记录）、
  真实 Docker 校准与脱敏 fixture、`make check`。
- 状态：文档与 fixture 已交付；真实 Docker 校准结果见本文首节；
  独立 live 校准入口 `scripts/runner/verify-harbor-local`（内部运行
  `tests/integration/test_harbor_calibration.py`）本记录**未执行**；
  `make check` **not_run_yet**（本次未运行，最终门禁结果记录在收尾提交）。

## 追踪表（18 目标 / 10 工作包 / 15 验收场景）

| ID | 状态 | 证据/命令 | 残余限制 |
|---|---|---|---|
| G01 | 已实现 | `tests/benchmarks/test_harbor_task_identity.py`（13 passed：来源/revision/内容 hash 身份）；`test_harbor_config.py`（Harbor 0.23.0 与数据 revision 冻结） | 上游 89/10 集的实机 revision 未运行 |
| G02 | 部分 | `test_harbor_task_identity.py::test_prepare_executes_nothing`、`::test_repository_calibration_fixtures_prepare_cleanly`（prepare 只读、不执行任务包脚本） | 固定来源 tarball 的下载/解包与 89/10 集实机准备 **not_run** |
| G03 | 已实现 | `test_harbor_task_identity.py::test_task_path_identity_not_basename`；`test_harbor_parser.py::test_same_basename_is_never_merged`（`HARBOR_TASK_NAME_AMBIGUOUS`） | 无 |
| G04 | 已实现 | `test_harbor_config.py::test_native_retries_are_not_stacked`；`tests/storage/test_trial_store.py::test_transport_retry_does_not_create_a_trial` | 无 |
| G05 | 已实现 | `tests/storage/test_trial_store.py`（15 passed：计划/结果/attempt trial 维度/ScoreSet 复用/旧语义） | PG 实机往返 **not_run**（`MOTTE_PG_DSN` 门控，CI 执行） |
| G06 | 已实现 | `test_harbor_config.py::test_harbor_config_rejects_unknown_and_pins_defaults`、`::test_change_of_agent_or_environment_changes_the_frozen_identity`；`test_harbor_task_identity.py::test_environment_digest_changes_with_security_extra` | 真实模型身份/用量由 Runner 观测，平台侧记 unknown（设计边界） |
| G07 | 已实现 | `tests/integration/test_harbor_job_fixture.py`（7 passed, 1 skipped：一 Job 多 Task/Trial、一次启动、全计划单元处置） | 真实模型层 **blocked** |
| G08 | 已实现 | `test_harbor_parser.py::test_reward_zero_missing_invalid_and_verifier_error`、`::test_verifier_error_is_not_a_quality_failure` | 无 |
| G09 | 已实现 | 同上；`tests/evaluators/test_harbor_metrics.py::test_reward_zero_is_a_valid_failure` | 无 |
| G10 | 已实现 | `test_harbor_metrics.py::test_planned_denominator_covers_units_that_never_reported`；`test_harbor_parser.py::test_parse_keeps_every_planned_trial_and_all_dispositions` | 无 |
| G11 | 部分 | `test_harbor_job_fixture.py::test_adapter_records_owner_tokens_and_residual_state`；`docs/operations/harbor-cleanup.md` | 真实运行中的取消与容器级残留核查 **not_run** |
| G12 | 部分 | `tests/runtime/test_external_job_lifecycle.py`（M2 supervisor）；`test_harbor_job_fixture.py::test_recovery_observes_only_and_never_restarts`；Runner 入口同名 Job 目录只复用 | 真实崩溃/句柄不确定场景演练 **not_run** |
| G13 | 已实现 | `test_harbor_parser.py::test_evidence_completeness_degrades_honestly`、`::test_unknown_usage_is_unknown_not_zero`、`::test_candidate_evidence_hash_prevents_silent_overwrite` | 真实 Agent 非空轨迹未观测（oracle 轨迹为空，已如实降级） |
| G14 | 已实现 | `test_harbor_preflight.py::test_task_container_never_gets_host_secrets_or_docker_socket`、`::test_custom_security_profile_changes_fingerprint`；冻结配置 `security` 段 | 上游 Verifier 隔离不可观测时按文档标注限制 |
| G15 | 已实现 | `tests/evaluators/test_harbor_metrics.py`（9 passed：分母/单位/覆盖/成本 null/零成功不适用/时长分相） | 真实模型成本不可观测（unknown） |
| G16 | 已实现 | `apps/web/tests/TerminalBenchPages.test.tsx`（12 passed）；`apps/web/src/evalTypes/terminalbench/TerminalBenchPages.tsx`（操作/任务/监控/结果/比较） | 真实数据下钻未执行（**blocked**） |
| G17 | 部分 | 层 1 离线已验证；层 2 真实 Docker oracle 校准已验证（本文首节：4 Trial，2 通过 / 2 失败） | 层 3 用户授权真实 Agent 小批次 **blocked** |
| G18 | 已实现 | `docs/operations/harbor-compatibility.md`（逐 dataset×agent×environment 登记；未验证 Agent 配置层拒绝） | 新组合需实机证据才能转正 |
| T01 | 已复验 | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_task_identity.py` → 13 passed | 无 |
| T02 | 已复验 | `uv run pytest -q -m "not live" tests/storage/test_trial_store.py` → 15 passed, 1 skipped | PG 实机 **not_run** |
| T03 | 已复验 | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_config.py` → 7 passed | 无 |
| T04 | 已复验 | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_parser.py` → 13 passed（真实捕获 fixture） | 无 |
| T05 | 已复验 | `uv run pytest -q -m "not live" tests/benchmarks/test_harbor_preflight.py` → 4 passed | 无 |
| T06 | 已复验 | `uv run pytest -q -m "not live" tests/integration/test_harbor_job_fixture.py` → 7 passed, 1 skipped | 真实 Runner 用例需 `MOTTE_HARBOR_RUNNER_PYTHON`；真实 Docker 校准另见 T10 |
| T07 | 已复验 | 同上（资源账本、清理报告、恢复只观察） | 真实取消/崩溃演练 **not_run** |
| T08 | 已复验 | `uv run pytest -q -m "not live" tests/evaluators/test_harbor_metrics.py` → 9 passed（1/2、2/3、门禁拒绝） | 无 |
| T09 | 已复验 | `uv run pytest -q -m "not live" tests/api/test_terminalbench_flow.py` → 6 passed；`pnpm --dir apps/web test -- tests/TerminalBenchPages.test.tsx` → 12 passed | 无 |
| T10 | 部分 | 本文 + `docs/operations/terminal-bench.md`、`harbor-compatibility.md`、`harbor-cleanup.md`、`docs/protocols/task-trial-identity.md`；真实 Docker 校准（本文首节）；`scripts/runner/capture-harbor-fixtures` | `make check` 与独立 live 校准入口的结果见文末「收尾复验」（均已执行）；上游完整任务集 **not_run**；真实模型 live 层 **blocked** |
| A01 | 已复验 | `test_harbor_parser.py` + `test_harbor_metrics.py`；真实 Docker oracle 运行（`hello-pass` 1.0 / `hello-fail` 0.0） | 无 |
| A02 | 已复验 | `test_harbor_task_identity.py::test_task_path_identity_not_basename`；`test_harbor_parser.py::test_same_basename_is_never_merged` | 无 |
| A03 | 已复验 | `tests/storage/test_trial_store.py::test_planned_trials_do_not_overwrite_case`；`test_harbor_metrics.py::test_aggregation_policy_is_fixed_upfront` | 无 |
| A04 | 已复验 | `test_harbor_parser.py::test_reward_zero_missing_invalid_and_verifier_error` | 无 |
| A05 | 部分 | `test_harbor_preflight.py::test_preflight_resource_and_visibility_fail_closed`（环境不可用即拒绝、零启动） | 真实环境启动失败的产物分类 **not_run** |
| A06 | 已复验 | `test_harbor_parser.py` 对 `samples/errors-timeout`（真实 Docker 产物）：Agent 超时 vs Verifier 超时 | 无 |
| A07 | 部分 | `test_harbor_parser.py::test_parse_keeps_every_planned_trial_and_all_dispositions`、`::test_unmapped_and_unplanned_trials_are_reported` | 真实 Runner 非零退出演练 **not_run** |
| A08 | 部分 | M2 进程树中断（`tests/runtime/test_external_job_lifecycle.py`）；`test_harbor_job_fixture.py::test_adapter_records_owner_tokens_and_residual_state`；`docs/operations/harbor-cleanup.md` | 真实运行中的取消与残留核查 **not_run** |
| A09 | 已复验 | `test_harbor_job_fixture.py::test_recovery_observes_only_and_never_restarts`；Runner 入口同名 Job 目录只登记不重跑 | 真实 wrapper 崩溃演练 **not_run** |
| A10 | 已复验 | `tests/storage/test_trial_store.py::test_result_import_is_idempotent_and_never_overwrites`；`test_harbor_job_fixture.py::test_conflicting_trial_evidence_blocks_without_overwriting` | 无 |
| A11 | 已复验 | `test_harbor_parser.py::test_evidence_completeness_degrades_honestly` | 真实长轨迹截断未观测 |
| A12 | 已复验 | `test_harbor_metrics.py::test_cost_keeps_unknown_null_and_zero_success_na`；`test_harbor_parser.py::test_unknown_usage_is_unknown_not_zero` | 真实模型计量不可观测（unknown） |
| A13 | 已复验 | `test_harbor_preflight.py::test_task_container_never_gets_host_secrets_or_docker_socket`、`::test_custom_security_profile_changes_fingerprint` | 无 |
| A14 | 已复验 | `test_harbor_config.py::test_change_of_agent_or_environment_changes_the_frozen_identity`；`test_harbor_task_identity.py::test_environment_digest_changes_with_security_extra` | 真实镜像 digest 未采集（`image_digest=None`） |
| A15 | 部分 | `test_harbor_metrics.py::test_rescore_identity_does_not_include_task_bytes`；不可变 ScoringPass 语义沿用 M1/M2 | M3 未新增重评分端到端演练 |

## 分层状态

| 层 | 状态 | 说明 |
|---|---|---|
| 层 1 离线（脱敏真实产物 + 纯 Parser/存储/API/Web） | 已验证 | 各 T 段命令；skip 不作为通过证据 |
| 层 2 真实固定 Harbor + 真实 Docker + 确定性 oracle Agent | 已验证（本机） | 4 Trial，2 通过 / 2 失败；无模型调用、无成本 |
| 层 3 真实模型/Agent 小批次 | **blocked** | 需用户显式授权：可审阅任务清单、模型、Profile、重复数、预算、期限 |
| 上游完整任务集（89 题 `terminal-bench 2.0` / 10 题 `terminal-bench-sample 2.0`） | **not_run** | 未下载运行；兼容矩阵逐条登记 |

## 阶段收尾

- **not_run / blocked 清单（如实保留）**：
  1. 真实模型/Agent live 层 — **blocked**（授权清单如上）；
  2. 上游完整任务集 89/10 — **not_run**；
  3. PG 实机（`MOTTE_PG_DSN` 门控的 trials 往返）— **not_run**，CI postgres service 执行；
  4. linux/amd64 宿主、受限网络策略、Verifier 可见性变体 — **not_run**；
  5. 真实运行中的取消与容器级残留核查 — **not_run**；
  6. `make check` — **not_run_yet**（本文未运行；最终门禁结果记录在收尾提交）。
- 已复验层（层 1 + 层 2）不构成 M3 整体验收完成：真实模型小批次与上游完整
  任务集未收口，兼容矩阵只登记本机实际跑过的组合。

## 收尾复验（实现提交后补记）

本节记录收尾阶段实际执行的门禁与真实环境证据，覆盖上文标为
`not_run_yet` / `not_run` 的条目；未执行的项继续保持未执行状态。

### 1. 阶段门禁 `make check`

```bash
make check      # 退出码 0
```

- Ruff、contracts mypy、compileall 通过。
- Python：**1333 passed / 33 skipped**（默认无 `MOTTE_PG_DSN`、无固定 Runner）。
- Web：**15 files / 201 passed**；Web build 通过；Pi bridge selftest 通过。
- `docker compose config -q` 与 `scripts/check_openapi.py`（`openapi.json is up to date`）通过。
- 门禁日志：`/tmp/m3-final-check.log`。

### 2. 真实 PostgreSQL 16 两连接复验

```bash
docker run -d --name motte-m3-pg -e POSTGRES_PASSWORD=motte -e POSTGRES_USER=motte \
  -e POSTGRES_DB=motte -p 127.0.0.1:55432:5432 postgres:16
MOTTE_PG_DSN="postgresql://motte:motte@127.0.0.1:55432/motte" \
  uv run pytest -q -m "not live" tests/
docker rm -f motte-m3-pg
```

- 结果：**1346 passed / 18 skipped**（PG 门控用例全部实机执行）。
- `tests/storage/test_trial_store.py::test_postgres_trials_roundtrip_and_two_connection_race`：
  两个独立连接各写同一批计划 → 先写 6×`created`、后写 6×`identical`；同
  `trial_id` 异内容 → `conflict` 且保留先写入的计划；`put_result` 跨连接
  `stored` / `identical`；跨 Run 隔离。
- 迁移链在真实 PG 上 up/down/再 up 通过（`0001 → 0008_trials`）。
- 该容器与卷为本轮临时创建，验证后已删除；未接触用户现有数据库。

### 3. 真实固定 Harbor + 真实 Docker 校准（层 2，经公共创建入口）

```bash
scripts/runner/install-harbor /tmp/motte-harbor-runner
scripts/runner/verify-harbor-local /tmp/motte-harbor-runner/bin/python
```

- 环境：macOS arm64 宿主 + Docker 27.4.0（引擎 `linux/arm64`）+ Python 3.12.11 +
  `harbor==0.23.0`（`scripts/runner/harbor-0.23.0-py312.lock`，89 个包）。
- 结果：**10 passed，58.90s**。其中
  `tests/integration/test_harbor_calibration.py::test_production_chain_api_to_report`
  走完整生产链路：`POST /benchmarks/terminal-bench/prepare` → 预检通过
  （`platform_custom_profile: false`）→ `POST .../runs` → 同一 Dispatcher 的
  `claim` / `execute_claimed` → 固定 Runner wrapper → 真实 Harbor Job（真实
  Docker、oracle Agent、`n_attempts=1`、`retry.max_retries=0`）→ 冻结证据 →
  纯 Parser → Trial 导入 → ScoringPass → `GET /runs/{id}/report`。
- 实际断言值：`valid_trial_pass_rate = 0.5`、`valid_trial_coverage = 1.0`、
  完整覆盖 Gate `passed = true`、`cost.known_cost_usd = null`（oracle 无
  provider 成本，未填 0）、`verifier_environment_mode = "shared"`。
- 重评分：`POST /runs/{id}/rescore` 产生新的 ScoringPass（旧 pass 保留），
  覆盖与通过率不变——只读冻结证据，未重新执行任务。
- 清理：运行后 `docker ps -aq --filter label=motte.job` 为空（无本 Job 残留容器）。
- 未调用任何付费模型：仅确定性 oracle Agent。
- 修正：`scripts/runner/verify-harbor-local` 现同时指向
  `tests/integration/test_harbor_job_fixture.py` 与
  `tests/integration/test_harbor_calibration.py`（后者含离线自洽校准与真实
  链路两个用例）。

### 4. 本节仍未收口（保持 not_run / blocked）

1. 真实模型/Agent 小批次 — **blocked**（需授权：任务清单、模型、Profile、
   重复数、预算、期限）；
2. 上游完整任务集 `terminal-bench 2.0`（89 题 / commit
   `69671fbaac6d67a7ef0dfec016cc38a64ef7a77c`）与 `terminal-bench-sample 2.0`
   （10 题 / commit `7e917f35c281188532772312d4ad91ca9274febc`）— **not_run**；
3. linux/amd64 宿主、受限网络策略、Verifier 可见性变体 — **not_run**。

（原第 4 条"真实运行中的中途取消与容器级残留核查"已在
[review round-1 修复](#6-review-round-1-修复复验2026-09-20)中实跑收口。）

**结论：代码检查通过（`make check` 退出码 0），层 1/层 2 真实验证通过；
阶段验收未完成**——真实模型小批次与上游完整任务集仍未收口。

### 5. 收尾 review 修复清单

| ID | 严重度 | 现象 | 修复 | 复验 |
|---|---|---|---|---|
| M3-CL-01 | P0 | 一个 Task 的多个 Trial 共用同一外部 Job 导入键（`stable_case_key=task_key`），第二个 Trial 被判"同键异内容"冲突，Job 终态 `failed` | 导入键改为计划 Trial 身份（`task_key#repeat_index`）；逻辑 Case 身份由 payload 的 `task_key` 表达，service 按 payload 映射 | `tests/integration/test_harbor_job_fixture.py::test_harbor_one_job_many_tasks_trials` 等 8 项 |
| M3-CL-02 | P1 | 直接注入 `artifacts` 的 runner 未接上证据冻结通道，解析前完整字节不落盘 | `DurableExternalJobRunner.__init__` 在提供 artifacts 时立即接线 | 证据 bundle 断言 + 全量回归 |
| M3-CL-03 | P1 | Harbor 形态 `runner_config`（`trials` 而非 `cases`）被 M2 校验器拒绝 | 校验器接受二者之一 | `tests/api/test_terminalbench_flow.py` + 全量回归 |
| M3-CL-04 | P1 | 计划 Trial 与最终 Run 身份可能不一致（`create_run` 内部生成 run_id） | `create_run` 接受调用方冻结的 `run_id`；API/CLI/SDK 先冻结再创建 | `test_planned_trial_ids_are_stable_across_requests` + 生产链路用例 |
| M3-CL-05 | P1 | 首个 Trial 完成即写任务级 CaseRun，会把整个 Task 写死 | Trial 维度的 CaseAttempt 禁止写任务级 CaseRun（三后端一致） | `tests/storage/test_trial_store.py::test_attempt_conflict_domain_is_trial_scoped` |
| M3-CL-06 | P2 | 计划里有、但完全没有产出的 Task 不出现在 Task 视图 | `task_aggregate` 为每个计划 Task 生成行（`observed_trials=0`、`task_pass=None`、原因 `no_valid_trial`） | `test_planned_denominator_covers_units_that_never_reported` + API 下钻用例 |
| M3-CL-07 | P2 | 迁移文件名与 revision id 不一致时 PG e2e 夹具按文件名加载失败；`0006/0007` 缺 `DOWN_STATEMENTS` | 夹具按模块内 `revision` 索引；补齐 0006/0007/0008 的 `DOWN_STATEMENTS` | 真实 PG 上 `test_postgres_e2e.py` + 全量 1346 passed |
| M3-CL-08 | P2 | `prepare` 持久化含 `prepared_at`，同字节重复准备被判"同 revision 异内容" | 准备时间不参与身份（持久化剔除，由存储 `created_at` 表达） | `test_prepare_is_read_only_and_reports_identity` |
| M3-CL-09 | P2 | 冻结证据把 job 根层文件也放进 `harbor/trials/`，与 Trial 目录语义混淆 | job 根层文件冻结为 `harbor/job/...`，Trial 文件保持 `harbor/trials/<trial>/...` | 解析与集成用例 |
| M3-CL-10 | P2 | 真实 Runner 未拿到 `MOTTE_TASK_ROOT`，先报"任务目录缺失"并以退出码 5 结束 | `HarborJobAdapter` 把受控任务根注入 Runner 环境 | 真实 Docker 生产链路用例 |

## 6. Review round-1 修复复验（2026-09-20）

逐项修复记录（26 项：失败反例 → 修复 → 同一反例复验）见
[M3 Review Round 1 修复记录](M3-review-round1-fixes-2026-09-20.md)。
本节记录该轮修复后的**门禁与真实环境复验**，用于替换上文对应的
`not_run` / `not_run_yet` 条目。

### 6.1 阶段门禁 `make check`（修复后）

```bash
make check    # 退出码 0
```

- Ruff `All checks passed!`；contracts mypy 通过；compileall 通过。
- Python：**1436 passed / 43 skipped**（默认无 `MOTTE_PG_DSN`、无固定 Runner）。
- Web：**15 files / 216 passed**；Web build 通过；Pi bridge selftest 通过；
  `docker compose config -q` 通过；`openapi.json is up to date`
  （本轮新增路由已重新生成 `make openapi`，并同步 `apps/web/src/api/schema.d.ts`）。

### 6.2 真实 PostgreSQL 16（一次性容器，已删除）

```bash
docker run -d --name motte-m3-r1-pg -e POSTGRES_HOST_AUTH_METHOD=trust \
  -p 127.0.0.1:55433:5432 postgres:16-alpine
MOTTE_PG_DSN="postgresql://postgres@127.0.0.1:55433/postgres" uv run pytest -q -m "not live"
docker rm -f motte-m3-r1-pg
```

- 全量：**1454 passed / 25 skipped**（PG 门控用例实机执行）。
- 本轮修复的 PG 用例：两连接同步屏障的并发首写（同 payload → `identical`，
  异 payload → `conflict` 且保留先写入者）、attempt 身份不可改写、
  `put_result` 身份错配拒绝、Trial 往返与跨连接幂等 → 4 passed。
- 迁移降级守护在真实 PG 上实跑（升级 → 有数据拒绝降级 → 空库降级 → 回到 head）。
- 诚实记录：`tests/storage` 单独批次的**首次**运行出现 11 个 `test_postgres_e2e.py`
  夹具 error（清库与既有 schema 的竞态），立即重跑为 **147 passed / 9 skipped /
  0 error**；未改动任何产品代码，`make check` 与全量 DSN 运行均无此现象。

### 6.3 真实固定 Harbor + 真实 Docker（层 2，修复后）

```bash
scripts/runner/install-harbor /tmp/motte-runner-verify
MOTTE_HARBOR_RUNNER_PYTHON=/tmp/motte-runner-verify/bin/python \
  uv run pytest -q tests/integration -m "not live"
```

- 结果：**46 passed / 1 skipped，120.46s**。相对上一轮新增/加强的真实用例：
  - `test_real_harbor_docker_calibration`：真实 Job 全流程（不可变任务副本复验、
    冻结证据、Parser、disposition）；
  - `test_production_chain_api_to_report`：API → Dispatcher → 真实 Runner →
    评分 → 报告；
  - `test_real_in_flight_cancel_stops_containers_and_keeps_decoy`：运行中取消 →
    本 Job 容器停止、诱饵容器保留；
  - `test_real_wrapper_crash_leaves_locatable_residue`：SIGKILL 后定位文件仍在；
  - `test_real_job_deadline_cancels_and_keeps_evidence`：长睡任务 + `job_sec=25`
    → 真实期限中断、部分证据保留；
  - `test_harbor_agent_native.py`（3 个）：真实 Harbor 的 `JobConfig` 与
    `ClaudeCodeOptions` 接受 `claude-code` 冻结配置（`cli_version=2.0.30`）、
    oracle 零模型零凭据、缺凭据环境变量时退出码 5。
- 运行后 `docker ps -aq --filter label=motte.job` 为空（无残留容器）。
- 未调用任何付费模型：全部使用确定性 oracle Agent 或只校验不执行的
  `--validate-config` 模式。

### 6.4 本轮修复期间的两次真实事故（如实记录）

1. **误启真实 Job**：校验命令最初漏传 `--validate-config`（wrapper 不转发调用方
   参数），以合成凭据起了一次真实 Job（`job-native`）：两个任务容器进入 Agent
   安装阶段后失败退出（`_setup_agent` 异常），**没有任何模型调用成功**。已用平台
   自己的所有权视图清理（`ContainerOwnership.stop_owned(remove=True)` →
   `state: clean`，无关容器未被动过），并修复 wrapper 转发 `"$@"`。
2. **自己引入的退出码 5**：`validate_agent_config` 最初用
   `AgentFactory.create_agent_from_config` 构造 Agent，`OracleAgent` 因需要
   `task_dir`/`trial_paths` 直接失败 → 全部真实 Run 退出码 5。真实链路复验发现后
   改为用 Agent 的 options 模型入口校验（不构造 Agent）。

### 6.5 本节仍未收口

1. 真实模型/Agent 小批次 — **blocked**（`claude-code` 实现与离线原生配置校验已完成，
   真实调用需单独授权）；`docs/operations/harbor-compatibility.md` 已按此登记，
   不把"实现完成"写成"组合已验证"。
2. 上游完整任务集 89 / 10 题 — **not_run**。
3. linux/amd64 宿主、受限网络策略、Verifier 可见性变体 — **not_run**。
4. 二进制证据 `/bytes` 下载路由返回原始字节、不做脱敏（响应头
   `X-Motte-Artifact-Redacted: false` 显式标注）；若后续要求原始字节也脱敏，
   需要额外的内容扫描层。
