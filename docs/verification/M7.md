# M7 验证记录：SDK、历史迁移、备份恢复、安全与稳定发布

> 当前状态：`planned / not_run`。本文只登记 M7 实际运行过的命令、外部操作和证据，不把路线图、历史 M7 计划、M6 merge message 或静态代码阅读当作 M7 通过。每轮追加记录，不覆盖历史证据。

## 1. Current summary

- 开工：2026-09-22，分支 `codex/m7-sdk-migration-release`，worktree `.worktree/m7-sdk-migration-release`，起始 HEAD `9940821`（= main；M6 merge `aee31a9` 的后续文档提交）。worktree 开工时 clean（仅 3 份未跟踪规划文档，与主工作区逐字节一致，经 sha256 比对）。
- T00 审计（2026-09-22，主 Agent + 4 个只读探索子 Agent）：Alembic head `0014_experiments_and_gates`（PG 链；SQLite 走 `create_and_upgrade` 内联 schema）；uv 0.12.6 / Python 3.12（venv）、Node 24.18.0 / pnpm 9.15.0；**本机无 docker 可执行文件、MOTTE_PG_DSN 未设置**（真实 Docker/PG → blocked/not_run，如实登记）。
- 代码缺口（T00 确认）：`import motte_sdk` 零副作用（良好）；无 HTTP client（client.py/client_errors.py/client_types.py 不存在）；`POST /runs` 无幂等键、无请求注册表（Experiment 仅进程内 request_key；ScoringJob 有持久 request_key）；无 `/api/v1/capabilities`；SSE 有 after/Last-Event-ID/心跳/终态关闭但无 gap/partial、无 JSON 持久查询端点、非法 Last-Event-ID 会 500；CLI 全部直连本地 DB、无 run list/get/cancel/events/wait 命令、无 local/server 分流；13 个包全部无 `[build-system]`/extras/wheel 构建；无导入账本/迁移模块；`maintenance.py` 备份无屏障/无 artifact hash/恢复无条件覆盖；cleanup 仅 mtime、无 pin 保护/tombstone；API 无 Host/Origin/auth/CORS 防护；CI 无 wheel 构建。
- M6 输入：RunReportRef、ReportSnapshot、ScoringPass、Baseline、GateResult、退出码 0–6、JSON/JUnit exporter v1 和 Experiment/API/CLI/Web 契约；以当前 `docs/verification/M6.md` 重核对为准。
- M6 继承限制：真实 PG、Docker/Harbor、Judge 人工校准以及部分 Web 遗留债务需明确纳入 M7 支持矩阵，不得自动标 stable。
- 实际外部操作：截至 T00 未导入真实旧数据、未覆盖生产数据库、未执行旧平台切换、未创建正式 release；live 费用 0。
- 主工作区 dirty 文件（`docs/roadmap/README.md` 修改、`docs/prompts/M7-development-agent.md` 未跟踪）与 `.worktree/m4`、`.worktree/platform-integrity` 未触碰。

## 2. 证据层级

| 层级 | 可证明内容 | 当前状态 |
|---|---|---|
| contract/unit | SDK 类型/错误、幂等、SSE、CLI、迁移策略、脱敏和 Gate/export 映射 | not_run |
| storage/ops | SQLite/PG maintenance、checkpoint、restore、pin/GC、upgrade/rollback | not_run |
| packaging | wheel、clean venv、Web artifact、Compose build/up、依赖隔离 | not_run |
| integration | client/CLI → API → Run/Worker → M6 report/Gate → events/export | not_run |
| migration | synthetic plan/apply/resume/rollback；真实 source counts/hash/license | synthetic pending / real not_run |
| security | loopback/Origin/Host/CSRF/CORS/auth/TLS/redaction/supply chain | not_run |
| external acceptance | 真实 Provider/Runner/PG/Docker/旧导出/三类替代场景 | pending / not_run |

`skip`、ImportError、collection error、compose config、静态页面、wheel 文件存在、子 Agent 自述都不构成通过证据。

## 3. 分母账本

| 范围 | 分母 | 当前状态 |
|---|---:|---|
| M7-G01–G23 | 23 | not_run |
| M7-T01–T12 | 12 | not_run |
| M7-A01–A21 | 21 | not_run |
| 非编号协议/运维/发布/回退要求 | 全部原始章节 | not_run |

每项最终必须填写：状态、实现路径、公共消费者、测试 node/命令、环境/版本、commit、证据 hash、外部条件和限制。

## 4. 工作包账本

| 包 | 状态 | 实现 commit | focused/相邻验证 | 外部证据 | 限制 |
|---|---|---|---|---|---|
| T00 审计/契约冻结 | complete | 887fe23 | 4 个只读探索子 Agent 地图 + 主 Agent 复核；`docs/protocols/sdk-and-migration.md` frozen@1 落库；tests/contract 可收集 | - | 协议修订须递增版本 |
| 服务端共享契约（T01/T02/T09/T10 的服务端半边） | complete | da62c68 | `uv run pytest -q tests/api/test_m7_platform.py`（15 passed）；相邻 tests/api/test_runs.py+test_events.py（30 passed）、tests/storage/test_run_store.py、tests/runtime/test_worker_loop.py（17 passed）；`make openapi` 重生成无漂移提交 | - | PG 侧 0015 由链断言登记（7d93629），真实 PG 未跑（无 DSN） |
| T02（Web 半边）SSE 一致性 | complete | 9da4cf6 | `pnpm --dir apps/web test` 286 passed（含 3 项新 hook 测试：去重/gap 快照补齐/快照失败保持 partial） | - | 组件消费接口向后兼容（新增 partial 字段） |
| T09 一致备份/staging restore | complete（子 Agent ec9be0a + 主 Agent 复审复跑） | ec9be0a | `uv run pytest -q tests/storage/test_maintenance.py tests/integration/test_backup_restore_consistency.py`：14 passed / 1 skipped（PG live 仅 MOTTE_PG_DSN 存在时运行）；主 Agent 独立复跑同结果；相邻 tests/api/test_m7_platform.py + tests/security/ 27 passed | PG 侧 not_run（无 DSN/无 pg_dump 环境） | 引用收集为宽口径启发式（fail-closed，代码内注明）；快照冻结维护标志问题已由 restore 前清除+守卫承担 |
| T10 安全/retention/GC（存储+API 层） | complete | 8c8cf3d | `uv run pytest -q tests/security/` 12 passed（GC 4 + 边界 8） | - | trace 事件无时间戳，DB 行裁剪未实现（plan 如实报告）；GC CLI 命令在 T03 批次接线 |
| T05 clean packaging | complete（子 Agent e4d54bd + 主审复跑） | e4d54bd | `uv run pytest -q tests/packaging/` 8 passed / 0 skipped（checkout 外 venv、6 wheel、G09 依赖闭包断言）；主审独立复跑同结果；相邻 tests/contract 263 passed、check_openapi 无漂移、tests/cli 冒烟 7 passed | CI packaging job（Linux）合并后回填 run 链接 | pyyaml 经 cel-python 传递进入解析闭包（声明级断言 G09，代码内注明）；motte_cli main.py --help 对无 provider 安装加守卫（越界编辑已主审，理由成立）；Compose build 本机无 docker |
| T06-T08 导入管道 | complete（子 Agent 148e26a + 主审复跑） | 148e26a | `uv run pytest -q tests/migration` 19 passed / 1 skipped（Windows symlink 权限，Linux CI 激活）；主审独立复跑同结果；`uv run mypy packages/contracts` 33 文件零错误；`uv run ruff check .` 全仓通过；相邻 41 passed | 真实旧导出 not_run（无授权来源） | rollback 无物理 run 行删除（存储层无 delete API，按协议 §5.4 备选：整批 rolled_back + CAS 注记保留审计）；配置类记录为账本引用映射不发布为可执行资源 |
| T01 SDK 类型/错误/幂等 | complete（子 Agent 8389659/1db0a54 + 主审复跑） | 8389659/1db0a54/133a2d5 | `uv run pytest -q tests/sdk/test_client_contract.py tests/sdk/test_wait_and_events.py` 27 passed（主审独立复跑同结果）；相邻 tests/api/test_m7_platform.py（16 passed，含内存幂等回归）+ tests/storage/test_run_store.py | - | 子 Agent 发现的内存后端平台仓库逐次新建缺陷已由主 Agent 修复（133a2d5：按 store 实例缓存 + 回归测试）；协议勘误：带 key POST 传输失败默认上抛由调用方重送 |
| T02 wait/SSE/事件（SDK 半边） | complete（同上提交） | 8389659/1db0a54 | 同上 test_wait_and_events.py 11 项：去重/断线重连/gap+snapshot 补齐/终态对账/needs_review 终态/wait 超时不 cancel/cancel_event | - | stream_events 以 sse_cursor+events_snapshot 为能力门 |
| T04 pytest/Trace/exporter v1 扩展 | complete（子 Agent 287a3ca + 主审复跑） | 287a3ca | `uv run pytest -q tests/sdk/test_pytest_and_exports.py` 26 passed；主审复跑 + 全量 tests/sdk + tests/cli/test_m6_cli.py 397 passed/1 skipped；EXPORTER_VERSION 保持 gate-exporter@1 | - | 插件在 collection 末加载（单次）；报告读取与真实执行 fixture 分界固化 |
| T03 CLI local/server | complete（子 Agent c4da983 + 主审复跑） | c4da983 | `uv run pytest -q tests/cli` 97 passed / 2 failed（terminalbench 2 项在 main 基线 9940821 同样失败，Windows O_DIRECTORY 环境族，非 M7 引入）；新增 test_remote_parity.py 18 项（A04 断连→TRANSPORT+零本地副作用/错误码 parity/等待超时零 cancel/双模式幂等键一致）；docs/operations/sdk-and-cli.md 落库 | - | local `run` 未知场景改返回 SCENARIO_NOT_FOUND（为双模式错误码 parity，无既有测试断言旧码）；`baseline default` server 读取走 client._get（无公共方法，仍是 HTTP-only） |
| T12 cutover readiness 门禁 | complete（软件侧） | 9c9d894 | tests/integration/test_cutover_readiness.py 4 passed（三场景 + 文档完整性门，矩阵回填后全绿） | 真实切换 not_run（需单独授权） | Windows O_DIRECTORY 环境族影响 Harbor trusted-root 节点，已换本机可复核节点并登记 |
| T02 wait/SSE/事件 | not_run | - | - | - | - |
| T03 CLI local/server | not_run | - | - | - | - |
| T04 pytest/Trace/export | not_run | - | - | - | - |
| T05 clean packaging | not_run | - | - | - | - |
| T06 ImportManifest/dry-run | not_run | - | - | - | - |
| T07 历史资源/只读 Run 导入 | not_run | - | - | - | - |
| T08 resume/conflict/rollback | not_run | - | - | - | - |
| T09 backup/restore/upgrade | not_run | - | - | - | - |
| T10 security/retention/supply chain | not_run | - | - | - | - |
| T11 release candidate/support matrix | not_run | - | - | - | - |
| T12 cutover/readiness/final review | not_run | - | - | - | - |

## 5. 目标与验收矩阵

详细责任映射见 [`2026-09-22-m7-execution.md`](../superpowers/plans/2026-09-22-m7-execution.md)。实现期间在下表补充测试 node、操作记录和证据：

| ID | 状态 | 测试/操作证据 | 证据级别 | 限制 |
|---|---|---|---|---|
| G01-G23 | not_run | - | - | - |
| A01-A21 | not_run | - | - | - |

## 6. 命令账本

| 日期 | worktree/SHA | 命令或操作 | 结果 | passed/failed/skipped/blocked/not_run | 证据 |
|---|---|---|---|---|---|
| 2026-09-22 | m7 worktree @9940821 | git rev-parse HEAD / status / worktree list；alembic head（编程入口 + 0014 文件核对）；uv/node/pnpm --version；docker/pg 探测；规划文档 sha256 与主工作区比对 | worktree=main 起点、clean；head=0014；无 docker、无 PG DSN | 环境事实登记 | 会话记录 |
| 2026-09-22 | m7 worktree @9940821 | uv run pytest -q tests/contract --co -q | 收集成功 | 48 项可收集（3+45） | 会话记录 |
| 2026-09-22 | m7 worktree @9940821 | pnpm install --frozen-lockfile | exit 0（web 依赖就绪） | - | 会话记录 |
| 2026-09-22 | m7 worktree @da62c68 | uv run pytest -q tests/api/test_m7_platform.py tests/api/test_runs.py tests/api/test_events.py tests/runtime/test_worker_loop.py tests/storage/test_run_store.py | 62 passed | 0 failed / 0 skipped | 会话记录 |
| 2026-09-22 | m7 worktree @da62c68 | make openapi（新增 capabilities/events-snapshot/maintenance 端点重生成 openapi.json + schema.d.ts） | 无漂移提交 | - | api/openapi.json |
| 2026-09-22 | m7 worktree @9da4cf6 | pnpm --dir apps/web test | 286 passed（21 文件） | 0 failed | - |
| 2026-09-22 | m7 worktree @8c8cf3d | uv run pytest -q tests/security/ | 12 passed | 0 failed | - |
| 2026-09-22 | m7 worktree @7d93629 | uv run pytest -q tests/storage/test_postgres_repository.py | 12 passed（0015 链断言登记） | 0 failed | - |

最低验证命令按实际路径调整：

```bash
uv run pytest -q -m "not live" tests/sdk tests/cli tests/migration tests/security tests/storage/test_maintenance.py
uv run pytest -q -m "not live" tests/integration/test_backup_restore_consistency.py tests/integration/test_release_smoke.py tests/integration/test_cutover_readiness.py
uv run ruff check .
uv run mypy packages/contracts
make openapi
make openapi-check
pnpm --dir apps/web test
pnpm --dir apps/web build
make check
```

## 7. Review ledger

| R-ID | 严重度 | 工作包 | 复现 | 修复 commit | 复验命令/结果 | 状态 |
|---|---|---|---|---|---|---|
| - | - | - | - | - | - | not_run |

P0/P1 必须关闭；P2/P3 只能在记录不影响退出门的理由后保留。基线/head 必须按精确 node-id 或明确的构建/迁移差异核对，不能按文件族猜环境归因。

## 8. 迁移、恢复与外部操作记录

### Synthetic migration

- source fixture/hash：-
- ImportManifest/importer/transform version：-
- dry-run counts/hash：-
- apply/resume/rollback：not_run
- Worker claim guard：not_run

### Backup/restore rehearsal

- DB backend/version：-
- Artifact root/hash manifest：-
- maintenance barrier：not_run
- staging restore：not_run
- unresolved Run handling：not_run

### External/cutover authorization

真实旧导出、许可证、凭据重绑定、PG/Docker/Harbor、正式 Provider/Runner、生产备份恢复和旧平台切换必须记录授权人、时间、范围、版本、费用、清理和 rollback window。未授权项保持 `blocked` 或 `not_run`。

## 9. 阶段结论规则

- `implementation_complete`：代码和公共消费者完成，但仍可能有 offline/external 证据缺口；
- `offline_verified`：合成/本地/声明环境测试、打包、迁移机制、恢复和安全证据齐备；
- `external_pending`：真实来源、环境、许可证、live 或切换证据仍缺失；
- `stable_supported`：支持矩阵中每个声明组合均有实际证据，skip/not_run 不计入；
- `cutover_ready`：三类替代场景、历史迁移对账、备份恢复、回退窗口和功能/迁移双线验收均通过。

没有同时满足对应证据时，不得将阶段写成 stable 或 ready。
