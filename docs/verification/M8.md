# M8 verification ledger

## Baseline and provenance

- Start SHA: `0ad766fd8020e852b61b88ef82e92d010c553584` (`origin/main`, fetched 2026-09-23). Assessment SHA `fc1e7d56c47d36952fb338bf9cb3b1f8c0fbfb3d`; document-parent SHA `807f84c7d7e42f880f24d2e4ffe5916509d59d12`.
- Isolated worktree: `codex/m8-delivery-closure`; source checkout `main` had untracked `skills-lock.json`, untouched.
- Environment: Windows 11 PowerShell; uv selected CPython 3.12.10; Node 24.18.0; pnpm 9.15.0; local Docker executable and `MOTTE_PG_DSN` unavailable. `uv sync` exit 0; `pnpm install --frozen-lockfile` exit 0.
- Current main CI: [run 35811711912](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35811711912), SHA above, Python failed: 3 failed, 2938 passed, 30 skipped, 1 deselected; Web and packaging succeeded. Failure nodes: `tests/scenario/test_fixture_lifecycle.py::test_prepare_materializes_owned_state_and_marker`, `tests/storage/test_scoring_jobs.py::test_postgres_scoring_job_transactions`, `tests/storage/test_trials_downgrade_guard.py::test_postgres_trials_downgrade_guard`. Python audit/Trivy/OpenAPI CI steps were skipped after `make check` failed. These are current CI facts, superseding the assessment's old run count.
- Superseding historical review: `docs/review/2026-09-22-m1-m7-code-feature-review.md` §9 and `docs/review/2026-09-22-m1-m7-test-report.md` §12. Previously fixed security/operational findings remain resolved at their recorded verification layer; no current live promotion is inferred.

## Package results

| Package | Implementation | Local verification | Remaining gate |
|---|---|---|---|
| T00 | 137-goal source crosswalk, no implied current test receipts | generator enforces 137 unique IDs; current index: 18 scoped offline rows, 2 scoped integration rows, 117 `not_reverified` | remaining rows need consumer/test/receipt evidence; counts are not a completion percentage |
| T01 | ControlledRoot create/reopen test aligned; owner/run fixture fixed; PG tests use task-owned random DB, explicit Alembic DSN now wins over cluster env | Linux CI `35815300827` at `f5c4374`: Python `make check`, Web and packaging all success | Windows symlink privilege and absent local Docker remain local limitations; no live PG recovery claim |
| T02 | Direct accepts only model_profile/reasoning_level; prompt/runtime/skill rejected in preview/create | `tests/sdk/test_m6_experiments.py` 21 passed at `bbbf335`; fake Provider captured reasoning request | extend accepted factors only with published consumers; current Direct scope explicit |
| T03 | common read-only resource/budget preflight for preview/create/allocate; unsupported strict policy rejected; persistent request registry; completed replay tolerates later resource drift without allocation. Continuation: database-scoped create/allocate lock protects active `allocating` claims | Prior 10×2 cap 5 and two SQLite service receipts remain; red test entered `_create_cell_run` twice before lock; `ac05062` dedicated two-process PG test passed twice in task-owned databases | preview is advisory if resources change before create; broader mutable-resource race combinations remain |
| T04 | Direct/GSM8K pages consume fixed ReportSnapshot + ComparisonService, no local quality/gate; cost currencies separated and unknown retained | Web 22 files/299 tests pass at `dc7f8d6`; targeted page after late-response test 80/80; SDK+HTTP 23 passed; real `create_app` HTTP proves case set/scorer/old pass; build passed | visual screenshot not reverified; other compare consumers to audit |
| T05 | GSM8K and Agent Tasks (`legacy-json`, model selector only) assemblers reuse managed `prepare_run`; Agent budget multiplies Cases × Cells × frozen `max_steps` × Provider retry cap. GSM8K now rejects experiment output caps other than its fixed 1024 preset before persistence | Synthetic standalone parity retained; `test_unassembled_suite_preview_and_create_fail_closed` covers C-Eval external, Scenario and Harbor; `test_agent_runtime_factor_cannot_silently_use_legacy_json` covers native/runtime selector; GSM8K cap red/green test plus adjacent set 50 passed locally | C-Eval ExternalJob/Runner freeze, native Agent controls, Workflow/Fixture/Skill snapshots and Harbor Task/Trial plan need distinct assemblers before support |
| T06 | fixed-pass paired Case/Task statistics remain. Continuation adds Terminal-Bench Task-internal planned Trial pass@k, excludes unplanned retry rows, rejects missing/invalid or reused upstream Trial, and exposes `k`/qualifications in SDK, HTTP, CLI JSON export and Web. Compare now freezes Pass IDs once and exposes them to Web/CLI | `9413db2` full Linux CI green: Python 2986 passed, Web 304 passed; review race red/green and fixed-ref HTTP/CLI/Web tests pass | separately persisted published statistical report and live Trial independence validation remain; T06 not fully accepted |
| T09/T10 | synthetic migration/SQLite restore and task-owned PG parity rechecked. Guarded `restore_postgres_staging` accepts empty targets, validates manifest/dump/artifact before restore, sets guard and checks count/ref after restore | Synthetic 34 passed/2 skipped at prior SHA; `ac05062` manual PG parity and `e96407e` guarded restore both passed twice in dedicated Linux CI; corrupt dump and DSN red/green tests pass | Compose build/up, in-flight write and GC/pin counterexamples, private old export and production recovery remain open |
| T07/T08 | live Provider/Harness/Judge and human calibration require bounded authorization and real labels | no paid/live operation or invented calibration sample | see external execution cards |
| T11/T12 | support matrix and cutover runbook keep RC/cutover gates explicit; synthetic readiness, release smoke and pytest/export paths rechecked | `uv run pytest -q tests/integration/test_cutover_readiness.py tests/integration/test_release_smoke.py tests/sdk/test_pytest_and_exports.py`: exit 0, 34 passed; final code SHA CI all jobs passed | three real replacement scenarios, PG recovery, old export and production authorization absent; no `stable_supported` or `cutover_ready` |

## Finding status at M8 candidate

| Finding | Current assessment | Next proof |
|---|---|---|
| F-01 | Direct accepted factors now consumed or rejected; other suite factor surface remains partial | T02 fake request and refusal tests, `bbbf335` |
| F-02 | partially closed: GSM8K and constrained Agent Tasks joined Direct with suite-specific guards; C-Eval, Agent native/runtime, Scenario/Skill, Harbor absent | T05 parity tests, `f932f17` and current Agent slice |
| F-03 | original budget defect closed offline; active-claim race fixed and scoped PG two-process test passed; full mutable-resource race surface still partial | T03 HTTP test `4b2943e`; PG dedicated CI `ac05062` / `35829305820`, `35829308936` |
| F-04 | Direct/GSM8K entries closed offline for fixed quality/pass/cost; all consumer inventory and screenshot remain | T04 Web+HTTP tests, `dc7f8d6` |
| F-05 | persistent request key wired; fixed-pass Case/Task and planned Trial pass@k have SDK/HTTP/CLI JSON/Web consumers; separately persisted statistical publication remains | T03 reconstruction; T06 fixed-pass/Trial/CLI/Web tests and `ac05062` CI |
| F-06 | guarded task-owned PG staging restore now integration-scoped; Compose build/up, private export, live support and RC/cutover remain open | T00 index, T09 dedicated CI `e96407e`, T11/T12 cards |

## Candidate CI and negative evidence

- `3495e23` CI `35812425055`: failure at the two PG guard tests because isolated fixture used a keyword DSN that migration code rejects. Corrected to URI at `9bf2f5f`.
- `bbbf335` CI `35812567085`: same earlier DSN failure while the later fix was not yet on that SHA; this run is not evidence for `9bf2f5f`.
- `4b2943e` CI `35813101028`: 4 failed, 2952 passed, 30 skipped, 1 deselected. The new CLI preview failures came from fixture `probe` model not seeded in its DB after strict preflight. Two PG failures came from Alembic env preferring cluster `MOTTE_PG_DSN` over explicit temporary DB. `fcb85e2` seeds the proper CLI fixture and gives programmatic DSN precedence. Local focused 3/3 passed; PG needs new CI receipt.
- `df9f067` CI [35814628752](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35814628752) **success**: Python `make check` 2963 passed, 30 skipped, 1 deselected; ruff, contracts mypy (33 files), compileall, Web build, Web 299/299, Compose config and OpenAPI check passed. Packaging built six source/wheel pairs; separate Web job also passed 299/299. This proves the T01 temporary PG migration target and owner/downgrade tests on Linux at that SHA, not the later commits.
- `f5c4374` CI [35815300827](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35815300827) and [35815297785](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35815297785) **success**: Python `make check`, pip audit, Trivy, OpenAPI drift, Web build/tests/audit/type drift, packaging wheel plus clean external-venv smoke all passed on Linux. This is an earlier candidate; later exact-SHA receipts supersede it.
- T06 local red/green: `pnpm --dir apps/web test -- M6.test.tsx` initially exited 1 (1 failed, 13 passed; missing `compare-statistics` UI), then exited 0 (14 passed) after consumer wiring. `uv run pytest -q tests/sdk/test_m6_comparison_service.py tests/integration/test_experiment_gate_slice.py tests/cli/test_m6_cli.py` exited 0 (44 passed). `pnpm --dir apps/web test` exited 0 (22 files, 301 passed); `pnpm --dir apps/web build` exit 0; `make openapi` and `make openapi-check` exit 0; changed-file Ruff exit 0. These are Windows synthetic/offline receipts, not the post-push Linux CI receipt.
- T05 Agent Tasks local red/green: `test_agent_tasks_experiment_matches_standalone_and_counts_steps` initially exited 1 at `SUITE_UNSUPPORTED`, then passed after guarded assembly. `uv run pytest -q tests/sdk/test_m6_experiments.py tests/api/test_m8_experiment_contract.py tests/integration/test_experiment_gate_slice.py` exited 0 (41 passed); `uv run ruff check .` and `git diff --check` exited 0. No Provider calls or external jobs were executed.
- T03 two-service SQLite concurrency: `uv run pytest -q tests/sdk/test_m6_experiments.py -k two_sqlite_services` exited 0 (1 passed, 35 deselected). The services used separate store/resource instances with the same task-owned SQLite file and request key; this does not prove multi-process PostgreSQL behavior.
- The T09/T10 synthetic migration, SQLite backup/restore and clean install set was rerun on `91c167c` with `uv run pytest -q tests/migration tests/integration/test_backup_restore_consistency.py tests/packaging/test_clean_install.py`: exit 0, 34 passed, 2 skipped. The two skips are environment/privilege-bound; no private old export or PG dump/restore was exercised.
- `1e3d179` CI [35816014006](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35816014006) and [35816010230](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35816010230) failed the same Web test node `tests/evalTypes.test.tsx > Gsm8kOperate > 勾选多个模型发起后跳转批次过程页` (300 passed, 1 failed in Python job Web step; separate Web job failed likewise). Root cause: the test waited only for the second API call count, which increments before its Promise resolves and `navigate()` commits; the immediate route assertion raced the UI. The test now waits for the router state itself; no product behavior, assertion, case, or negative test was removed. Recheck succeeded at `91c167c` below.
- `91c167cbb6f22417bb4342194ea73af266e4c178` CI [35816644485](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35816644485) and [35816641694](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35816641694) **success** on Linux. Python `make check`: 2970 passed, 30 skipped, 1 deselected, 2 warnings; Ruff, contracts mypy (33 source files), compileall, Web build, Web test, Compose config and OpenAPI drift passed. Separate Web job: 22 files/301 tests passed, build/audit/generated TS drift passed. Packaging wheel and clean external-venv smoke, pip audit and Trivy passed. This exact SHA includes the T05 Agent Tasks slice, T06 fixed-pass statistics and the async test repair. The 30 skips and local Windows limitations are not promoted to live coverage.
- `98252fc` CI [35826040658](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35826040658) and [35826044892](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35826044892) **failed** at the new PG process test: 1 failed, 2976 passed, 30 skipped, 1 deselected, 3 warnings in the first run. The first child did not signal entry into `_create_cell_run` within 15 s; a fork warning appeared but the test masked the child's actual exception. `bc9dfa3` used `spawn` and surfaced the exception: [35826843588](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35826843588) and [35826847372](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35826847372) each failed **1 node**, with 2983 passed, 30 skipped, 1 deselected, 2 warnings in the first run. The test payload omitted required `ExperimentSpec.created_by` and `reason`; this is the confirmed cause of the pre-claim failure, not evidence that PostgreSQL locking failed. The fixture now supplies both fields. Packaging and Web passed at those SHAs; Python audit/Trivy/OpenAPI were skipped after `make check`. Neither SHA is a green release gate.
- A local ad hoc `pnpm --dir apps/web exec tsc --noEmit` returned 1 with pre-existing type errors in `client.ts`, Agent/External/Resources pages and Semi dependency declarations; this is not the repository Web build gate. Web `pnpm --dir apps/web test` 299/299 and Vite build succeeded. No changed-file TS error appeared in that output.
- Candidate local checks: `make openapi-check` exit 0 (`openapi.json is up to date`), `uv run mypy packages/contracts` exit 0 (33 files), `uv run python -m compileall -q packages apps` exit 0, `pnpm --dir apps/web build` exit 0, `git diff --check` exit 0. `make compose-config` exit 2 because Windows host lacks the `docker` executable. `make check` is therefore not locally complete; Linux CI is the exact-SHA gate.
- `uv run pytest -q -m "not live"` on Windows exited 1: **2881 passed, 14 failed, 97 skipped, 1 deselected**, 682.79 s. Thirteen failures occur at `Path.symlink_to`/`os.symlink` before the security assertion (`WinError 1314`, local privilege unavailable):
  `tests/benchmarks/test_ceval_parser_parity.py::test_parser_input_limits_and_path_safety`, `tests/benchmarks/test_harbor_task_compose_policy.py::test_env_file_requires_bounded_regular_utf8_file[symlink]`, `tests/benchmarks/test_harbor_task_identity.py::test_symlink_escape_and_swap_are_refused`, `tests/integration/test_harbor_evidence_io.py::{test_outer_config_symlink_is_refused_not_followed,test_location_file_symlink_does_not_escape}`, `tests/review/test_m2_review_fixes.py::{test_r06_outputs_root_symlink_cannot_become_trusted_root,test_r06_parent_chain_symlink_inside_boundary_rejected}`, `tests/review/test_m2_round3_fixes.py::{test_r3_04_root_outputs_symlink_cannot_become_trusted,test_r3_04_symlink_component_rejected_by_fd_walk}`, `tests/review/test_m2_round4_runner.py::test_r4_snapshot_rejects_symlink_and_oversize`, `tests/runtime/test_review_round3_fixes.py::{test_workspace_symlink_chain_refused_and_victim_survives,test_cleanup_refuses_workspace_swapped_to_symlink}`, `tests/runtime/test_review_round4_fixes.py::test_workspace_creation_has_no_side_effect_in_victim`. The negative tests remain intact for Linux CI.
- The remaining local failure is `tests/integration/test_harbor_job_fixture.py::test_adapter_records_owner_tokens_and_residual_state`: it expected `residual` but got `unknown`, because Harbor cleanup correctly prioritizes `containers.state == unknown` when no local Docker daemon can be queried. `unknown` was not relabelled as `residual` or `clean`. Linux CI must establish the candidate result.

## Continuation receipts and suite decisions (2026-09-23)

- **T03 root cause and red/green:** `ExperimentService._allocate_one` treated any committed `allocating` cell without a Run as a crashed owner. A concurrent service could reset an active claim and enter `_create_cell_run` again. `tests/sdk/test_m6_experiments.py::test_active_sqlite_allocation_is_not_reset_by_second_service` failed before the fix (`calls == ['first', 'second']`) and passed after database-scoped locking. The lock is held from create/preflight through cell completion; SQLite uses a database-adjacent OS file lock, PostgreSQL a session advisory lock keyed by experiment/version. `test_resource_ceiling_drift_after_preview_rejects_before_persistence` passed: a draft model cap lowered from 4096 to 256 after preview rejects create with zero specs/cells/runs; restoring the cap permits the previously rejected key. Existing half-allocation recovery tests still pass. `tests/storage/test_m8_experiment_pg_concurrency.py::test_two_postgres_processes_do_not_reclaim_active_cell` uses a task-owned disposable PG database and two independent processes; Windows without `MOTTE_PG_DSN` skipped it. The Linux CI result must be recorded against the committed SHA before calling PG verified. These receipts are separate from the earlier two-service SQLite test.
- **T05 suite-by-suite ruling:** standalone C-Eval/CMMLU creation goes through `apps/api/app/main.py::_external_run_create`, with frozen ExternalJob/Runner config and case expectations; the current experiment manifest has none of them. Agent Tasks standalone can select native-tool/runtime, while this assembler fixes `legacy-json` and accepts only `model_profile`. Scenario/Skill requires published Workflow, Fixture, Target and Skill snapshots via `prepare_run`; `ExperimentService._validate_task_resource` currently requires a managed benchmark plugin and cannot assemble it. Harbor standalone calls `terminalbench.build_run_inputs` to freeze Task-specific TrialPlan and native Job identity; Experiment Cell repeat is not Harbor Trial repeat. Preview/create therefore both reject these combinations before persistence. The tests named in the T05 row exercise the refusal. Adding a suite name to the allowlist would change standalone semantics, so no unsupported suite was enabled.
- **T05 GSM8K fixed output cap counterexample:** A bounded live preflight offered `controlled_conditions.max_output_tokens=128`, but the GSM8K suite replaced that request with its frozen 1024 preset. A model ceiling of 128 made creation fail; a larger ceiling silently used 1024. `test_gsm8k_experiment_rejects_output_cap_below_fixed_suite_preset` failed first because preview accepted 128, then passed when preview/create rejected all non-1024 caps before persistence. The adjacent suite initially caught a diagnostic precedence regression (`SUITE_MISMATCH` was hidden by the cap error); validation now checks suite identity before the cap. Local command `uv run pytest -q tests/sdk/test_m6_experiments.py tests/api/test_m8_experiment_contract.py tests/integration/test_experiment_gate_slice.py` exited 0: **50 passed, 2 warnings**. This does not relax standalone GSM8K's fixed preset.
- **T04 currency loss counterexample:** The first bounded live DeepSeek receipts used official CNY peak rates but `PriceTable` lacked a currency field; Provider cost omitted currency and `ComparisonService` would default those historical numeric values to USD. Those historical cost labels are insufficient and remain unchanged. Provider now validates an explicit three-letter currency (legacy default USD), emits it in cost evidence, and the Worker/live-smoke consumers retain it. Red tests reproduced missing CNY in the envelope/log and missing Worker currency; green focused provider/runtime/comparison/API set: **194 passed, 2 warnings**, plus CLI smoke red/green. Later live receipts must use an explicit currency and verify the fixed path; this code change does not verify gateway billing rates.
- **T06 fixed Trial report:** `ComparisonService.paired_statistics` now groups fixed ScoreSet Trial rows by each Run's frozen TrialPlan and Task. A valid Task requires unique planned `trial_id`/`repeat_index`, all matching valid scored rows, and no reused nonempty `source_trial_id`; unplanned ScoreSet rows are counted as excluded and never increase n. `pass_at_k` is called only for complete Task groups. The response carries fixed RunReportRefs, policy ID/hash, method/seed/iterations, `k`, per-Task planned/valid/passed counts, excluded rows, and precise inapplicability. HTTP `GET /api/v1/comparisons/statistics?k=`, CLI `motte compare --statistics --k N --json FILE`, and the existing Web comparison page consume that same result. This is a fixed-input JSON comparison export; there is no separately persisted published statistical report yet. SDK tests first failed for missing `k`, then passed; Web test first failed for absent Trial field, then passed. `test_terminal_statistics_reject_reused_upstream_trial_identity` failed before the independence guard and passed after it. No real Harbor Agent run or live independence claim follows from these synthetic rows.
- **Review fix, fixed Pass identity:** A read-only independent reviewer reproduced a current-pointer race: the response could name the old Pass while Trial statistics read the new Pass. `test_terminal_statistics_pin_current_once_when_pass_changes_mid_read` failed with old baseline ref and new-pass pass@2, then passed after `ComparisonService.compare` pinned both Pass IDs once and passed them through comparison, manifest, cost and statistics reads. The comparison HTTP response and CLI JSON now expose those exact refs; Web sends them into its statistics request and declines statistics if the comparison response lacks refs. Web red/green covered both the missing-ref refusal and pinning. A separate adjacent CLI test caught and fixed the original `RUN_NOT_FOUND` error being replaced by `NO_SCORING_EVIDENCE` during the first refactor.
- **Local continuation commands (Windows 11 / Python 3.12 / Node 24; worktree dirty base SHA `bea03e6`):** `uv run pytest -q tests/sdk/test_m6_experiments.py tests/api/test_m8_experiment_contract.py tests/storage/test_m8_experiment_pg_concurrency.py` exit 0, 44 passed/1 skipped; `uv run pytest -q tests/sdk/test_m6_comparison_service.py tests/integration/test_experiment_gate_slice.py tests/cli/test_m6_cli.py` exit 0, 49 passed before the invalid-`k` addition; `pnpm --dir apps/web test -- M6.test.tsx` exit 0, 15 passed; `pnpm --dir apps/web build`, `make openapi`, `make openapi-check`, changed-file Ruff and `git diff --check` exit 0. These are pre-commit local receipts and do not substitute for exact-SHA CI.
- **Final T06 local adjacent set (Windows 11 / dirty worktree after `98252fc`):** `uv run pytest -q tests/sdk/test_m6_comparison_service.py tests/integration/test_experiment_gate_slice.py tests/cli/test_m6_cli.py tests/migration tests/integration/test_backup_restore_consistency.py tests/packaging/test_clean_install.py tests/integration/test_cutover_readiness.py tests/integration/test_release_smoke.py tests/sdk/test_pytest_and_exports.py` exit 0, **118 passed, 2 skipped, 2 warnings**. This includes current synthetic T09/T10 migration, SQLite recovery, packaging and T11/T12 readiness checks; the skips are environment-bound and this is not PG dump/restore, private old-export or live cutover evidence. `pnpm --dir apps/web test` exit 0, **22 files/302 passed**; `pnpm --dir apps/web build` exit 0. `uv run ruff check .`, `uv run mypy packages/contracts` (33 source files), `uv run python -m compileall -q packages apps`, `make openapi-check` and `git diff --check` all exit 0. No post-build screenshot/asset/__BUILD_ID__ receipt was taken, so UI visual verification is `not_reverified`.
- **Local full Makefile gate:** `make check` on the dirty continuation worktree exited 1 at Python pytest: **14 failed, 2900 passed, 98 skipped, 1 deselected, 2 warnings** in 678.76 s. Thirteen failures are the same `WinError 1314` symlink fixture nodes listed above; `tests/integration/test_harbor_job_fixture.py::test_adapter_records_owner_tokens_and_residual_state` remains the no-Docker `unknown` versus expected `residual` observation. No new M8 node failed. Ruff, contracts mypy (33 files), and compileall passed before pytest; Web/Compose stages did not run inside this stopped `make check`. The one additional skip versus the prior local run is the new disposable PG process test (`MOTTE_PG_DSN` unset). This failed Windows gate cannot be marked passed; exact-SHA Linux CI is still required.
- **T09 initial isolated PG exercise:** `tests/integration/test_m8_pg_restore.py::test_pg_dump_restore_preserves_run_trial_pass_baseline_and_artifact` created two random databases only on a loopback PG test cluster, backed up the source, manually restored into the separate target and compared Run, TrialPlan, ScoringPass, ScoreSet, Baseline and Artifact content. It verified the queued Run remained blocked by an explicitly set `restore_guard_active`. This original shape passed twice on Linux at `ac05062`; it was a manual parity receipt, not yet an automated staging restore or production recovery receipt.
- **T09 guarded staging implementation:** `restore_postgres_staging` accepts a separate empty PostgreSQL target and empty Artifact destination. It validates manifest, dump and artifact bytes before connecting to the target, refuses existing user objects, restores without `--clean` in one transaction, sets the guard before clearing inherited maintenance, then checks revision, counts and references. The damaged dump test failed first for the missing helper, then passed with zero target access; the report credential test also ran red/green. Local targeted set: **17 passed, 2 PG skips**; Ruff, contracts mypy (33 files), compileall and diff check exit 0. The helper's real PG parity node passed twice at `e96407e` as recorded below. This does not replace Compose build/up, production recovery or cutover.
- **T09 helper candidate Windows Makefile gate:** `make check` exited 1 at Python pytest: **14 failed, 2905 passed, 99 skipped, 1 deselected, 2 warnings** in 662.88 s. The failing nodes match the 13 WinError 1314 symlink fixtures and one no-Docker Harbor cleanup `unknown`/`residual` expectation listed earlier; no new helper node failed. Ruff, contracts mypy and compileall passed; Web/Compose did not run inside the stopped local `make check`. The helper's PostgreSQL path remains skipped locally and requires its own exact-SHA Linux receipt.
- **Review fix, disposable PG safety:** The reviewer reproduced that a URL query `dbname=` can override a rewritten path and direct a supposedly disposable restore at the original database. Both the older T01 temporary-DB fixture and the new T09 pair now remove query-level database overrides, reject effective non-loopback `host`/`hostaddr`/service settings, and verify `current_database()` equals each generated name before migration or writes. The old `isolated_database_uri` test failed on the override then passed. The T09 test also clears the inherited maintenance flag under its explicit restore guard and asserts `restore_guard_active`, so maintenance alone cannot mask a missing guard.
- **Post-review local focused gates (Windows 11, dirty continuation after `bc9dfa3`):** `uv run pytest -q tests/sdk/test_m6_comparison_service.py tests/integration/test_experiment_gate_slice.py tests/cli/test_m6_cli.py tests/storage/test_trials_downgrade_guard.py tests/storage/test_m8_experiment_pg_concurrency.py tests/integration/test_m8_pg_restore.py tests/evaluators/test_comparability.py tests/evaluators/test_m6_comparability_levels.py tests/evaluators/test_terminalbench_comparison.py tests/evaluators/test_terminalbench_fingerprint.py` exit 0, **100 passed, 3 skipped, 2 warnings**. The three skips are PG-dependent on this host. `pnpm --dir apps/web test` exit 0, **22 files/304 passed**; Web build exit 0. Ruff, contracts mypy (33 files), compileall, OpenAPI check and diff check exit 0. This still is not a post-build screenshot receipt.
- **`9413db27cf5ca1342757ad5dffb8add92db25fd2` Linux CI:** [35828281757](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35828281757) and [35828284887](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35828284887) both succeeded. Python `make check`: **2986 passed, 30 skipped, 1 deselected, 2 warnings** in 408.07 s / 393.59 s; Ruff, contracts mypy (33 files), compileall, Web build/tests, Compose config and OpenAPI check passed. Separate Web job: 22 files/**304 passed**, build, pnpm audit and generated TS drift passed. Packaging wheel plus clean external-venv install, pip audit, Trivy config scan and OpenAPI drift passed. The full `-q` output does not name passing PG nodes; a dedicated `-vv` CI step is being added for the two task-owned PG concurrency/restore nodes, so this full-green SHA is not presented as a per-node PG transcript.
- **`ac05062eeb55c0ac0f25b14afb19af4adf82c4d6` exact-SHA CI:** [35829305820](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35829305820) and [35829308936](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35829308936) both succeeded on `ubuntu-latest` with PostgreSQL 16 service and Python 3.12. Python `make check`: **2986 passed, 30 skipped, 1 deselected, 2 warnings** in 401.65 s / 394.14 s; Ruff, contracts mypy (33 files), compileall, Web build/tests, Compose config and OpenAPI check passed. Dedicated command `uv run pytest -vv tests/storage/test_m8_experiment_pg_concurrency.py::test_two_postgres_processes_do_not_reclaim_active_cell tests/integration/test_m8_pg_restore.py::test_pg_dump_restore_preserves_run_trial_pass_baseline_and_artifact` exited 0 in both runs: both named nodes **PASSED**, **2 passed** in 7.34 s / 7.86 s. Separate Web job: 22 files/**304 passed**, build, audit and generated TS drift passed. Packaging wheel/clean-venv smoke, pip audit, Trivy config scan and OpenAPI drift succeeded. These are task-owned PG process/dump/restore integration receipts, not Compose build/up or production recovery certification.
- **`e96407e1daca7496520e56057b3bd0519033a5fc` guarded-restore CI:** [35832658716](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35832658716) and [35832665229](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35832665229) both succeeded on `ubuntu-latest` with PostgreSQL 16 service and Python 3.12. Python `make check`: **2988 passed, 30 skipped, 1 deselected, 2 warnings** in 397.67 s / 405.95 s; Ruff, contracts mypy (33 files), compileall, Web build/tests, Compose config and OpenAPI check passed. Dedicated `-vv` step again printed both M8 PG nodes as **PASSED**, **2 passed** in 6.92 s / 7.50 s; the restore node now executes `restore_postgres_staging`, validates the returned database name and explicit guard, and rejects reuse of a nonempty target. Separate Web job: 22 files/**304 passed**, build/audit/generated TS drift. Packaging wheel/clean-venv smoke, pip audit, Trivy config scan and OpenAPI drift succeeded. This proves only the task-owned supported combination, not Compose build/up or production recovery.
- **Clean `9413db2` Windows Makefile gate:** `make check` exited 1 at Python pytest: **14 failed, 2903 passed, 99 skipped, 1 deselected, 2 warnings** in 661.97 s. The same 13 symlink fixture nodes fail at `WinError 1314`; `tests/integration/test_harbor_job_fixture.py::test_adapter_records_owner_tokens_and_residual_state` sees conservative `unknown` without Docker. Ruff, mypy (33 files) and compileall passed before pytest; `make check` did not reach its Web/Compose stages locally. The extra PG skips reflect no local DSN/client tools; no negative test was deleted or relabelled.

## External execution cards

- **T07 Provider/Harness/Judge live**: obtain explicit account/credential reference (not secret text), fixed model/runtime/data/scorer versions, per-path call/token/cost caps, stop condition and artifact target before calling. DeepSeek V4.1 Flash availability does not itself grant paid-call authorization.
- **T08 calibration**: obtain a real, authorized human-labelled sample set (at least the project's 30-item minimum) and Judge spend bound; synthetic labels remain offline only.
- **T09 PG recovery**: task-owned manual PG parity at `ac05062` and guarded empty-staging helper at `e96407e` each passed twice on Linux; corrupted dump refusal passed offline. In-flight write and GC/pin counterexamples, Compose build/up and any production target remain open. Local Docker/DSN remain unavailable.
- **T10 old export**: source owner/format/path authorization plus isolated target and rollback plan. No private export was opened.
- **T11/T12 RC/cutover**: recheck support rows against exact candidate SHA, three replacement scenarios, build hash, rollback material and authorized production window. No tag, Release, production switch or merge performed.

## Authorization boundary

No real Provider/Harness/Judge calls, private old export, production migration/recovery/GC, Release or cutover were performed in this ledger. Existing credentials and DSNs are not authorization. Such tasks remain blocked individually until an execution card names the account/reference, fixed versions, maximum calls/tokens/cost, source/target, stop and cleanup rules.
