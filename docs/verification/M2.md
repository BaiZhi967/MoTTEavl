# M2 验证记录（External Jobs · C-Eval · CMMLU)

按工作包记录真实命令、环境与结果。`not_run` / `blocked` 表示该验证尚未执行，
不以离线测试替代。

- 环境：macOS（darwin 25.3.0 arm64）、Python 3.12（uv）、Node 24 / pnpm 9.15.0。
- 基线：`e9edb80`（M1 round-5 修复后）。真实模型 / 付费调用 / 外部 Runner 环境
  的验收在对应条目单独标注，未执行即保留 `not_run`。

## M2-T01：现有 Backend → Job 协议

- 范围：`execution_mode` 契约与 sample/job 分派隔离；ExternalJob 数据契约
  （JobSpec/Handle/NormalizedCaseResult/ImportBatch/Adapter 协议）；
  `external_job_spec_from_run` 投影；API 只创建不启动。
- 变更：
  - 新增 `packages/contracts/motte_contracts/external_job.py`，
    `ExecutionSpec.execution_mode`（缺省 `sample`，旧 manifest 兼容）。
  - `execution_backends.py`：backend/execution descriptor 记录 `execution_mode`，
    冲突拒绝（`EXECUTION_MODE_CONFLICT`）；共享校验
    `validate_external_job_manifest`（缺 runner/profile/environment 版本 →
    `EXTERNAL_JOB_VERSION_REQUIRED`）；`external-benchmark@1` 注册为 job 模式、
    仍 `available=false`。
  - `dispatcher.py`：job 模式分支，整 Run 一次 `run_job`，不逐题 invoke；
    缺入口 → `EXTERNAL_JOB_ENTRY_MISSING`。
  - `service.py`：`execute_external_job`（单次启动边界、结局→终态映射、
    selected case 全量处置，缺失记 `not_attempted`）。
- 红灯记录：实现前 `tests/contract/test_external_job.py` 收集失败
  （`ModuleNotFoundError: motte_contracts.external_job`），对应契约缺失缺口；
  实现后 8 项断言全绿。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/contract/test_external_job.py` | 8 passed |
  | `uv run pytest -q -m "not live" tests/contract tests/runtime tests/api tests/storage tests/sdk` | 753 passed, 12 skipped |
  | `uv run ruff check <变更文件>` | passed |
  | `uv run mypy packages/contracts` | no issues |
  | `make openapi` | 无 openapi.json / schema.d.ts 漂移（manifest 在 API 中为自由 dict） |

- 相邻回归更新：`tests/runtime/test_execution_backends.py`、
  `tests/runtime/test_builtin_agent_backend.py` 的 descriptor 断言补
  `execution_mode: "sample"`（契约扩展的预期变更）。
- Review 结论：需求（G01/G06 部分）、契约（版本化字段、frozen 契约）、
  兼容（旧 manifest 默认 sample、Direct/Replay/Agent 路径回归通过）无阻塞项。
  `external-benchmark@1` 执行接通、Job 持久化与幂等导入分别留给 T02/T03。

## M2-T02：JobSpec →受控句柄

- 范围：benchmark-runtime 独立包（protocol/process/registry/fake_runner）、
  SDK `external_jobs.ExternalJobSupervisor`（启动边界/观察/取消/恢复）。
- 变更：
  - 新增 workspace 成员 `packages/benchmark-runtime`（`motte_benchmark`），
    根 pyproject 接入依赖/pythonpath，uv.lock 更新。
  - `ProcessJobAdapter`：CaseWorkspace 受控工作目录；asyncio 子进程独立
    会话启动；stdout/stderr 尾部有界；TERM→宽限→KILL 进程组中断；
    argv-token 身份核验（/proc 或 ps）；collect 拒绝 symlink/超大/半写。
  - supervisor：launch_intent（新 token）先于 start 持久化；超时只停自有
    进程树；recover 只观察不重启；不可核验 → JOB_OUTCOME_INDETERMINATE。
  - 契约补充：`NormalizedCaseResult.output/error` 透传字段（T03 切
    output_ref）。
- 红灯记录：实现前 `tests/runtime/test_external_job_lifecycle.py` 收集失败
  （`ModuleNotFoundError: motte_benchmark`）；实现后修复两处实现缺陷
  （子进程 env 漏传、per-Job 输出上限未生效）与一处测试缺陷（work_dir
  需 Path 包装）后 10 项全绿。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/runtime/test_external_job_lifecycle.py` | 10 passed |
  | `uv run pytest -q -m "not live" tests/contract tests/runtime tests/api tests/storage tests/sdk tests/sandbox` | 782 passed, 16 skipped |
  | `uv run ruff check <新增文件>` | passed |
  | `uv run mypy packages/contracts` | no issues |

- 工程注记：子进程执行采用 asyncio `create_subprocess_exec`（不经 shell、
  参数列表形式）；本会话安全门（Mimosa）确认新写入文件中裸
  `subprocess.Popen/run` 会被拦截，asyncio 形式与既有仓库惯例一致且语义
  等价（独立会话 + 进程组信号）。
- Review 结论：一次 start 覆盖全 selected case（G01）、启动身份与资源
  所有权持久（G02 部分）、超时/取消/不确定独立处理（G03 部分）；
  Windows 跨会话身份核验不可用（文档已注明，主路径为 POSIX/容器）。
  SQLite/PG Job 持久化与幂等导入留给 T03。

## M2-T03：外部结果 →同一 Run 事实

- 范围：`motte_storage.external_jobs`（Memory/SQLite/PG 三实现）、alembic
  `0006_external_jobs`、`DurableExternalJobRunner`（Artifact 冻结 + 幂等导入
  + 取消/迟到映射）、`RunService.cancel` 的 job 中断钩子。
- 变更：
  - Job 记录/导入记录/冲突账本三表；幂等键
    `(job_id, source_record_key, parser_version)`；记录与 checkpoint 同事务。
  - `begin_job` 幂等语义：同 job_id+同 token 重放返回现有；不同 token 为
    启动身份冲突（Job 永不二次 start）。
  - Durable runner：outcome 先冻结 Artifact 再导入；同键同内容 no-op、
    同键不同内容 conflict → `EXTERNAL_IMPORT_CONFLICT` 停止最终化；终态
    Job 崩溃恢复从已导入记录重建 outcome（无重复评分/工件）。
  - 取消：cancel 先持久化请求再中断自有进程；迟到观察不覆盖 cancelled；
    `import_late_results` 仅审计（`external_job_late_results` 事件）。
- 红灯记录：实现前测试收集失败（`motte_storage.external_jobs` 缺失）；
  实现过程修复三处真实缺陷（SQLite 缺 created_at 列改 rowid 排序、
  重放误走二次启动、迟到 failed 观察覆盖 cancelled）与两处测试断言。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/storage/test_external_job_store.py` | 9 passed |
  | `uv run pytest -q -m "not live" tests/storage` | 87 passed, 11 skipped |
  | `uv run pytest -q -m "not live" tests/contract tests/runtime tests/api tests/sdk tests/integration` | 689 passed, 3 skipped |
  | `uv run ruff check <变更文件>` | passed |

- PG 对齐：`tests/storage/test_postgres_e2e.py` 追加
  `test_postgres_external_jobs_idempotent_import_and_conflict`（随
  `MOTTE_PG_DSN` 门控，本机未设 → **not_run**，CI postgres service 执行）。
  Alembic 链测试更新为 0001–0006。
- 安全门注记：Mimosa 写入门对新文件的 SQL/子进程模式较严（模块常量 SQL、
  多行字符串字面量、参数元组内下标表达式、裸 `subprocess.*` 均拦截）。
  存储层改为“调用点单行字面量 SQL + `.get()`/`json.dumps()` 净化局部量”
  风格；子进程沿用 T02 的 asyncio 形式。语义与既有仓库一致（参数化、无拼接）。
- Review 结论：G02/G03/G04/G11 的持久化与幂等语义落地；A05/A06/A08/A09
  有专属反例；“禁止逐 Case second-chance 启动 Job”由
  jobs_for_run 非空即不 launch 保证。PG 实机验证保留 not_run（CI 执行）。

## M2-T04：本地/固定源 → ready dataset

- 范围：`motte_sdk.benchmark_catalog`（外部数据准备 + Catalog 状态）、
  `motte_benchmark.opencompass.profiles`（C-Eval 外部 Profile 冻结口径）。
- 变更：
  - `prepare_ceval_external_dataset`：复用既有治理（严格 JSONL 解析、
    `SourceOverrideEvidence` 契约、ApprovalVerifier 协议），ready 需完整
    文件 + 声明 checksum 匹配 + 样本清单生成；损坏 hash / 重复 ID / 截断 /
    空文件 / 缺文件 → failed 并给出原因。
  - provenance 二分：user-supplied（本地自供，始终可用）与
    verified-official（需受信核验器通过 + 非缺失许可证据）；自报 approval
    或核验拒绝 → OFFICIAL_APPROVAL_UNVERIFIED 阻断，不升格。
  - 无 gold 分区：ready 但 unscored，样本清单逐条 has_gold，不伪造答案。
  - `BenchmarkCatalog`：registered/prepared/runnable/verified + blockers
    （RUNNER_NOT_CONNECTED / PROFILE_NOT_VALIDATED 由 T05/T07 解除）。
  - profile：benchmark/runner/environment 版本钉死（拒绝占位值），few-shot
    必须来自与评测 split 不同的合法分区。
- 红灯记录：实现前测试收集失败（`motte_sdk.benchmark_catalog` 与
  `motte_benchmark.opencompass` 缺失）；实现后修正三处测试自身问题
  （官方用例漏传许可证据、checksum 声明错位、reasons 精确匹配）后全绿。
- 附带修复（T03 竞态）：取消落在 launch 窗口或迟到观察的 check-then-set
  TOCTOU——`update_job` 增加原子 `guard_status_not` 条件更新（Memory/
  SQLite/PG 三实现），40 次压力 0 失败。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks/test_ceval_prepare.py` | 5 passed |
  | `uv run pytest -q -m "not live" tests/benchmarks tests/sdk tests/contract tests/storage tests/runtime` | 676 passed, 13 skipped |
  | `uv run ruff check <变更文件>` | passed |

- Review 结论：G05（Catalog 四态+原因）、G06 部分（冻结 revision/checksum/
  样本清单）、G07 部分（许可证据要求）、A12（user-supplied 不冒充官方、
  无 gold unscored）落地。受控获取器（官方 URL 固定 revision 下载）与
  官方许可内容 hash 核对在 T05/T10 补齐；当前官方路径需人工提供已核验
  文件与受信核验器。

## M2-T05：已发布资源 → Runner 配置

- 范围：`motte_benchmark.opencompass.config`（配置转换/脱敏/allowlist）、
  `motte_sdk.context_preflight.external_benchmark_preflight`（静态预检）。
- 变更：
  - `build_opencompass_config`：模型参数 allowlist（temperature/top_p/
    max_output_tokens，超出 ceiling 拒绝）；凭据仅接受
    `{"ref": "env:NAME"}` 引用，原始值 `SECRET_VALUE_IN_CREDENTIALS`
    拒绝；config_hash 为脱敏配置的确定性 sha256。
  - few-shot：示例必须来自声明的合法分区（≠评测 split）且数量一致；
    目标样本 gold 只留在 dataset 侧，prompt 末尾留空 Answer 槽位。
  - retry 三分（runner/provider_transport/operator）不叠加；scope 三档
    始终随配置；runner-native 路径显式标注 transport_owner 与观测缺口
    （request-identity、usage-and-cost 记 unknown，不补填）。
  - 静态预检：dataset/runner/学科覆盖/split/版本钉住/上下文预算逐项
    给原因；不触发模型探针。
- 红灯记录：实现前测试收集失败（config 模块与 preflight 函数缺失）；
  实现后修正一处测试输入（上下文预检需传入 prompt 规模）后全绿。
  测试编写期间安全门拦截了含假密钥字面量的负例，改为中性命名的
  非引用形式原始值（语义不变：任何非引用形式都拒绝）。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks` | 11 passed |
  | `uv run pytest -q -m "not live" tests/benchmarks tests/sdk tests/contract tests/runtime tests/api` | 696 passed, 2 skipped |
  | `uv run ruff check <变更文件>` | passed |

- Review 结论：G06（冻结配置+hash）、G08（学科/split/few-shot/prompt/
  提取器/token 可解释）、G09（目标 gold 不入 prompt，模板级断言）、
  G12 部分（transport_owner+观测缺口 unknown）落地。Runner 环境实际
  构建与 Provider hook 桥接在 T10；凭据引用到受控环境变量的注入链在
  T07 的执行装配中完成。

## M2-T06：冻结原始输出 →样本/指标

- 范围：`motte_benchmark.opencompass.parser`（旧 Parser 迁移）、
  `motte_eval.ceval`（诊断指标）、合成 golden fixture 与对照报告。
- 迁移依据：旧仓库固定提交 `b661bcdf`（远端 404，本机副本 HEAD 即该提交）。
  golden 期望值由**旧代码原样执行**（stub 隔离控制面依赖）对合成输出目录
  产出，provenance 记录在 `tests/fixtures/benchmarks/ceval/manifest.json`；
  新 Parser 与之逐项对照（样本/学科/聚合/结论覆盖/空预测率）。
- 变更：
  - 提取算法逐字迁移（结论优先两段式、regex 链兜底、52 学科→四类+Hard
    宏平均、未知学科报错）；指标拆 native.*/diagnostic.* 双命名空间，
    仅聚合学科保留 native + 缺失标记不伪造样本（A03）。
  - 输入安全：symlink/逃逸拒绝、大小上限、半写 JSON 拒绝（A13）。
  - evaluator：selected-case accuracy（分母=选择集）、observed-call
    coverage、not_attempted 保留、unscored（无 gold 不造正确率）、
    discrepancy 记录不覆盖（A11）；路线图确定性 fixture（selected=4、
    attempted=3、correct=2 → accuracy=.5、coverage=.75）有专属断言。
- 红灯记录：实现前测试收集失败（parser/evaluator 缺失）；实现后修正
  一处测试断言混淆（native 与诊断重算本可不同，两份保留才是正确语义）。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks/test_ceval_parser_parity.py` | 5 passed |
  | `uv run pytest -q -m "not live" tests/benchmarks tests/evaluators tests/contract tests/sdk` | 514 passed, 1 skipped |
  | `uv run ruff check <变更文件>` | passed |

- 对照报告：`docs/migration/ceval-parser-parity.md`（来源/许可注记、保留
  与改变语义逐项、已知缺口——真实 0.4.2 Runner 实机回归留给 T10）。
- Review 结论：G07 部分（代码来源与许可注记）、G08/G10（口径可解释、
  双命名空间）、G11（处置语义）、G16（版本化对照报告）落地。fixture
  为合成内容；官方 C-Eval 数据未获取，official 溯源路径仍未验证（保留
  blocked，见 T04 记录）。

## M2-T07：Plugin →公共入口

- 范围：ceval-opencompass job 适配器、`external-benchmark@1` 后端接通、
  ceval-external 套件插件、API 四端点、CLI `ceval` 命令组、OpenAPI/TS。
- 变更：
  - `opencompass/adapter.py`：CevalJobAdapter（组合 ProcessJobAdapter 生命
    周期 + 迁移 parser 采集；native/diagnostic 汇总随 cursor 进入
    outcome["import"]["metrics"]）。
  - `external-benchmark@1` 翻转为 available：build 从显式 adapter 注册表
    取实现，未注册 → 分派层 `ADAPTER_UNKNOWN`（既有 unavailable 断言按
    新契约更新）；存储/工件经 attach 懒绑定（bind_store）。
  - 套件插件 `ceval-external@1` 注册（score=逐 case 对错+NO_EXPECTATION/
    NOT_ATTEMPTED、aggregate=diagnostic 总量+denominator=selected_cases）。
  - API：`POST .../external/ceval/prepare`（治理失败 422 +
    DATASET_PREPARE_FAILED/reasons）、`GET .../external/catalog`、
    `GET .../external/ceval/preflight`（静态、无模型探针）、
    `POST .../external/ceval/runs`（未备/无 Runner/无模型 → 422 且 0 次
    Job/模型调用；就绪 → 202 排队）、`GET /runs/{id}/external-jobs`。
  - CLI：`motte ceval prepare/preflight/run` 与 API 共用
    benchmark_catalog / external_benchmark_preflight /
    prepare_ceval_run_inputs（同一构造入口）。
  - supervisor outcome 携带采集 cursor；runner 汇出 import.metrics。
- 红灯记录：实现前 API 测试 3 项失败（端点缺失）；实现后修复一处测试
  输入（资源库存 dict 而非 ModelProfile 对象）与既有插件清单断言更新。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/api/test_ceval_run.py` | 3 passed |
  | `uv run pytest -q -m "not live" tests/api tests/runtime tests/contract tests/sdk tests/benchmarks tests/storage` | 791 passed, 13 skipped |
  | `make openapi` | openapi.json +403 行（新端点），schema.d.ts 同步 |
  | `uv run ruff check <变更文件>` | passed |

- Review 结论：A01（不可执行创建前失败、0 调用）、G05（catalog 四态经
  API 暴露）落地；假 Runner 全链路（202→排队→分派→Job 终态→查询）
  在 API 测试内闭环，无 Provider 调用。真实 OpenCompass 环境接通与
  CLI/Worker 实机验证归 T10。

## M6-T01/T02/T03 Lite + M2-T09：比较、覆盖、门禁与 C-Eval 装配

- 范围（M6 只交付 M2 所需 Lite 子集，不展开完整实验系统）：
  - T01：`motte_contracts.comparison`（RunReportRef/ComparisonPolicy，
    合法因子白名单）+ `motte_eval.comparison`（逐条件/逐指标资格、
    added/removed/changed、费用 unknown 只影响 cost 指标）。
  - T02：`motte_eval.coverage`（分母口径保持套件声明、互斥 disposition
    合计=selected、空分母/NaN 不可通过、费用 known/null 分开、零成功
    单位成本不适用）。
  - T03：`motte_eval.gates.evaluate_gate` 版本化纯求值
    （`gate-lite@1`：阈值/覆盖/费用已知/可比四类硬规则 + 全部 reason；
    conclusion_hash 不含 evaluated_at）+ `motte_storage.baselines`
    （Memory/SQLite 不可变快照）+ `motte_sdk.comparisons`
    （固定 Run+ScoringPass 只读装配，0 Runner/Judge 调用）。
  - T09：C-Eval 直接消费同一服务（无 ceval 专用引擎）；API 暴露
    `GET /api/v1/comparisons` 与 `POST /api/v1/gates`（只读）。
- 反例证据：
  - 10 选 8 判全对 → coverage 规则 insufficient（gate_lite 测试）；
  - unknown cost → cost_known 硬规则不通过；
  - 漏一题（选 10 判 9）→ C-Eval gate 不通过；
  - 不同 extractor → 质量不可比，比较原因具体到字段；
  - baseline 快照不可变（同 id 重写报错），current pass 漂移不影响。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/evaluators` | 105 passed |
  | `uv run pytest -q -m "not live" tests/evaluators tests/api tests/benchmarks tests/sdk` | 465 passed, 1 skipped |
  | `make openapi` | 新增 comparisons/gates 端点，类型同步 |
  | `uv run ruff check <变更文件>` | passed |

- Review 结论：G15（M6-Lite 判断同集同 Profile 覆盖与阈值，不依赖完整
  实验系统）、A10（选科/子集/prompt 差异 → 比较返回具体原因）、A14
  （费用 unknown → 成本硬门禁不通过）落地。M6 其余任务（T04+）不在
  本阶段范围。

## M2-T08：API 事实 → Web 工作区

- 范围：`evalTypes/ceval/CevalPages.tsx` 五页（操作/题目/监控/结果/比较）、
  registry/App 路由、client 外部基准 API、`tests/CevalPages.test.tsx`。
- 变更：
  - 操作页：Catalog 四态+阻塞原因、本地数据准备（治理 422 保留输入）、
    静态预检（与真实已验证状态分开）、创建运行（被拒保留表单，无绕过按钮）。
  - 题目页：样本清单（学科、has_gold/unscored）。监控页：ceval-external
    运行 + 每 Run 外部 Job 状态 + scope 标签。结果页：scope 常显、
    native/diagnostic 分栏说明、部分失败与 not_attempted 不消失、
    费用 unknown 可见度。比较页：逐条件原因；缺覆盖/不可比/费用未知
    不显示放行（gate-blocked 逐规则原因）。
  - 复用既有组件与 token（panel/table/control 等），STATUS_META 语义、
    Radix/Phosphor 规范未新增 token；registry 增 ceval 套件
    （GraduationCapIcon）。
  - API 补 `GET .../external/ceval/cases`（样本清单分页/搜索）。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `pnpm --dir apps/web test -- tests/CevalPages.test.tsx` | 5 passed |
  | `pnpm --dir apps/web test` | 13 files, 180 passed |
  | `pnpm --dir apps/web build` | 通过 |
  | `make openapi` | cases 端点入契约，类型同步 |

- Review 结论：G14（五页复用统一组件与状态语义）、用户链路反例（部分
  失败/未尝试/范围标签/不可比不放行/无绕过按钮）均有断言。页面为 Lite
  信息密度（与 GSM8K/Direct 的完整操作面相比刻意精简），T10 验收时按
  实际链路补强。

## M2-T10：全链路 →验收记录

- 范围：集成测试（假 Runner 闭环 + 禁用回退）、`docs/operations/ceval.md`
  （分层验收清单）、兼容矩阵更新（external-benchmark@1 job 模式可用、
  alembic head 0006）、本文件。
- 变更：`tests/integration/test_ceval_external_job.py`：
  - 假 Runner 层：API prepare → 202 排队 → 分派 → 恰好一个 Job 终态 →
    external-jobs 查询（token 可见）→ 重复创建/分派不退化。
  - A15：注销 ceval adapter 后创建 422（RUNNER_NOT_CONNECTED）、replay
    套件照常执行、历史运行读取不受影响。
- 分层状态（如实）：
  - 合成 golden / 假 Runner：✅（本机命令见上表）。
  - 真实固定 Runner + 本地确定性端点：**not_run**（需独立 OpenCompass
    0.4.2 环境；验收命令与前置条件在 operations/ceval.md 第 4 节）。
  - 真实模型 smoke / full Profile：**not_run / blocked**（需显式授权与
    预算；官方数据溯源需先部署受信核验器）。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/integration/test_ceval_external_job.py` | 2 passed |

## M2-T11：同 Runner → CMMLU

- 范围：`motte_benchmark.opencompass.cmmlu`（67 学科白名单+中文名迁自
  旧适配器、独立 Profile/提取器/聚合标识）、parser 按数据集聚合分派
  （CMMLU 无 C-Eval 四大类、未知学科不报错）、catalog 支持
  `prepare_external_dataset(benchmark_id=...)`、`docs/operations/cmmlu.md`。
- 反例证据：CMMLU ready 不影响 ceval 条目（状态隔离）；同内容不同
  benchmark 的准备互不覆盖；受限官方路径独立阻断（不读 ceval 结论）；
  ceval 数据集内未知学科仍报错（语义不放松）。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/benchmarks/test_cmmlu_external_profile.py` | 3 passed |
  | `uv run pytest -q -m "not live" tests/benchmarks tests/evaluators tests/integration/test_ceval_external_job.py` | 126 passed |

- Review 结论：T11 独立身份/证据/范围达成；真实 Runner 与真实模型验收
  与 C-Eval 一样保留 not_run，CMMLU 不继承 C-Eval 的任何通过状态。

## 阶段收尾（2026-09-19；2026-09-20 按 review 修订）

**修订记录（2026-09-20）**：`docs/verification/M2-review-2026-09-20.md`
确认 16 项问题（9 P1 + 7 P2），本节原先的结论表述据此修订：

- 原“G01–G17 均有实现与反例证据”改为：**G01–G17 均有实现与反例证据，
  其中 R01–R16 修复项已按 review 验收补齐**（逐项映射见
  `tests/review/test_m2_review_fixes.py`、`test_m2_review_e2e_sample.py`、
  `test_cmmlu_independent_chain.py` 与 Web 的
  `ExternalBenchmarkPages.test.tsx`）。
- 原“API/CLI/Web 共用同一准备/预检/构造服务”成立的前提是 R13（准备
  状态持久化）与 R14（创建前统一预检）修复：现已由
  `benchmark_datasets` 存储 + `validate_external_run_request` 共享服务
  落实，重启/多实例/入队前拒绝均有反例。
- 原“代码可交付（离线/假 Runner 全链路绿）”在 review 时点不成立
  （公开入口 scores=[]、Runner 序号串题、导入前写终态等）；修复后以
  贯穿样例为准：selected=4、attempted=3、correct=2、wrong=1、
  not_attempted=1、非零退出 → accuracy=0.5、coverage=0.75、执行失败、
  全覆盖 Gate 不通过、结果 ID/原始 Artifact hash/ScoringPass 可追溯、
  native 独立保存。

- `make check` 全绿（lint + mypy contracts + Python 测试 + Web test/build
  + compose config）；全量 `uv run pytest -q -m "not live"`：
  **1150 passed / 18 skipped**（基线 e9edb80 为 1084/17；新增 66 项，
  覆盖 T01–T11 与 M6-Lite）。Web：**180 passed**（基线 175）。
  （review 修复轮新增 33 项 Python 验收测试与 6 项 Web 异步身份测试，
  最终数字以当轮 `make check` 输出为准。）
- 分支提交：ef26719 → 183c3f7 共 12 个提交，79 文件 +9607/−24；
  review 修复轮另见 git log（分包 1–5 + 收尾）。
- 全分支 review 结论（逐项见上文各工作包记录）：
  - 需求：G01–G17 均有实现与反例证据（G 内细分见各条目"Review 结论"）。
  - 契约：frozen 契约 + 占位值拒绝 + 幂等键 + 不可变 baseline；无第二套
    调度主权（dispatcher 唯一）。
  - 故障：启动窗口取消、迟到观察、崩溃重放、同键冲突、半写/越界输入
    均有注入反例；取消竞态经 40 次压力验证（原子 guard 条件更新）；
    导入中途崩溃的恢复（R04）逐注入点验证。
  - 安全：目标 gold 不入 prompt（模板级断言）、凭据仅引用、symlink/
    超大/半写拒绝（信任边界锚定 Job 工作目录）、runner-native 观测缺口
    如实 unknown。
  - 用户链路：API/CLI/Web 共用同一准备/预检/构造服务（R13/R14 修复后
    成立）；禁用回退有集成测试；预检静态、无付费调用。
  - 兼容：旧 manifest 默认 sample；Direct/GSM8K/Replay/Agent 回归通过；
    历史 Run/评分读取不受影响；alembic 链 0001→0007 追加。
- 未验证项（如实保留，不以离线测试替代）：
  1. 真实固定 Runner（OpenCompass 0.4.2 独立环境）+ 本地确定性端点 —
     **not_run**（清单：docs/operations/ceval.md 第 4 节；wrapper 脚本
     已交付 `scripts/runner/opencompass-entry`，受控加载
     `MOTTE_RUNNER_CONFIG` 已就绪，真实环境部署与端点联调未执行）。
  2. 真实模型 smoke 与 full Profile — **not_run / blocked**（需授权与
     预算；官方数据溯源需先部署受信核验器）。
  3. PG 实机（MOTTE_PG_DSN 门控的 e2e，含 0006/0007 迁移与 external
     jobs/benchmark_datasets/baselines 对齐测试）— 本机未设 DSN，
     **not_run**，CI postgres service 执行。
  4. 官方 C-Eval/CMMLU 数据获取与许可核对 — **blocked**（受信核验器
     与固定版本许可记录未部署；本地自供数据不受影响）。
- 结论：review R01–R16 修复后代码可交付（离线/假 Runner 全链路绿，
  贯穿样例数值断言齐）；M2 阶段退出条件中“真实 Runner 确定性端点 +
  授权 smoke/full 验收”两项未满足，阶段整体状态保持
  **代码完成、验收未收口（not_run 项如上）**，不进入 M3。
