# M2 验证记录（External Jobs · C-Eval · CMMLU)

按工作包记录真实命令、环境与结果。`not_run` / `blocked` 表示该验证尚未执行，
不以离线测试替代。

- 环境：macOS（darwin 25.3.0 arm64）、Python 3.12（uv）、Node 24 / pnpm 9.15.0。
- 基线：`e9edb80`（M1 round-5 修复后）。真实模型 / 付费调用 / 外部 Runner 环境
  的验收在对应条目单独标注，未执行即保留 `not_run`。

## M2-T01：现有 Backend → Job 协议

- 范围：`execution_mode` 契约与 sample/job 分派隔离；ExternalJob 数据契约
  （JobSpec/Handle/NormalizedCaseResult/ImportBatch/Adapter 协议）；
  `external_job_spec_from_run` 投影；API 只创建不启动。
- 变更：
  - 新增 `packages/contracts/motte_contracts/external_job.py`，
    `ExecutionSpec.execution_mode`（缺省 `sample`，旧 manifest 兼容）。
  - `execution_backends.py`：backend/execution descriptor 记录 `execution_mode`，
    冲突拒绝（`EXECUTION_MODE_CONFLICT`）；共享校验
    `validate_external_job_manifest`（缺 runner/profile/environment 版本 →
    `EXTERNAL_JOB_VERSION_REQUIRED`）；`external-benchmark@1` 注册为 job 模式、
    仍 `available=false`。
  - `dispatcher.py`：job 模式分支，整 Run 一次 `run_job`，不逐题 invoke；
    缺入口 → `EXTERNAL_JOB_ENTRY_MISSING`。
  - `service.py`：`execute_external_job`（单次启动边界、结局→终态映射、
    selected case 全量处置，缺失记 `not_attempted`）。
- 红灯记录：实现前 `tests/contract/test_external_job.py` 收集失败
  （`ModuleNotFoundError: motte_contracts.external_job`），对应契约缺失缺口；
  实现后 8 项断言全绿。
- 命令与结果（本机，2026-09-19）：

  | 命令 | 结果 |
  |---|---|
  | `uv run pytest -q -m "not live" tests/contract/test_external_job.py` | 8 passed |
  | `uv run pytest -q -m "not live" tests/contract tests/runtime tests/api tests/storage tests/sdk` | 753 passed, 12 skipped |
  | `uv run ruff check <变更文件>` | passed |
  | `uv run mypy packages/contracts` | no issues |
  | `make openapi` | 无 openapi.json / schema.d.ts 漂移（manifest 在 API 中为自由 dict） |

- 相邻回归更新：`tests/runtime/test_execution_backends.py`、
  `tests/runtime/test_builtin_agent_backend.py` 的 descriptor 断言补
  `execution_mode: "sample"`（契约扩展的预期变更）。
- Review 结论：需求（G01/G06 部分）、契约（版本化字段、frozen 契约）、
  兼容（旧 manifest 默认 sample、Direct/Replay/Agent 路径回归通过）无阻塞项。
  `external-benchmark@1` 执行接通、Job 持久化与幂等导入分别留给 T02/T03。
