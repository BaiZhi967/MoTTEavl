# M2 外部 Job、C-Eval 与 CMMLU Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. 本次只完成规划与计划 review；后续逐工作包实现，不将本文件作为付费调用、生产切换或自动合并的授权。

**Goal:** 完整交付本阶段需求文档中的全部目标、工作包及验收场景，包括明确列出的追加/增强项。

**Architecture:** 复用现有 RunDispatcher、CaseAttempt、不可变资源和 ScoringPass。契约由责任阶段维护，后续阶段增量消费；错误、缺失、取消和不确定结果分别保留。

**Tech Stack:** Python 3.12 / uv / FastAPI / Pydantic；React / Vite / pnpm 9.15.0 / Node 24；SQLite / PostgreSQL；按任务需要使用受控 Docker 或独立 Runner。

**Spec:** [M2-llm-benchmarks-and-ceval.md](../../../roadmap/M2-llm-benchmarks-and-ceval.md) 全文；[执行总安排](README.md)与[需求索引](../../../roadmap/README.md)。

**核对基线:** `b6c402b62f0a19f79cde719af60c8bb73672c877`；计划状态 `planned`，实施验证 `not_run`。当前仓库事实是静态代码核对，不是本次 runtime 测试通过证明。

## Global Constraints

- “保持单用户、API-first、Python/FastAPI/Pydantic、React/Vite、现有 uv/pnpm workspace。”
- “Run、Case、Trial、传输重试、操作员 retry 和评分 pass 的身份分开。”
- “已发布资源与终态评分不可覆盖。”
- “MetricResult 是评估视图，持久化仍进入既有 ScoreSet。”
- “默认测试不使用真实模型或个人订阅。” subject、Judge、Harness 的 live 分别授权和计量。
- 遵守 [AGENTS.md](../../../../AGENTS.md) 与 [DESIGN.md](../../../../apps/web/DESIGN.md)。运行状态由 STATUS_META 派生；Radix / Phosphor Icon 后缀；新 token 先登记 DESIGN.md 再加 :root。
- 各任务的路径是仓库相对路径；“修改”表示当前存在，“新增计划”表示尚不存在，最终落位在开工时重新核对。新 schema 沿实际 migration head 追加，不预占 revision。
- 下文测试命令为将来执行步骤，缺文件/ImportError 只说明尚未实现，不能作为业务红灯或通过记录。先写可收集的断言测试，红灯必须对应列出的行为缺口。

## Review Focus

- 现存 restricted 来源被新 Catalog 绕过，T04 断言不可信 approval 无效。
- Job 启动成功而句柄丢失，T02 验证 token 恢复观察不再 start。
- 同采集 key 内容改变，T03 验证 conflict 与原始证据保留。
- 空输出或只有 aggregate，T06 保留缺失且不伪造样本/native score。
- M6-Lite 与 M2-T09 形成循环依赖，T09 仅装配已就绪的公共服务。

## 1. 进入条件与分批安排

M1-T01/T03/T07 接口；现有 SourceSpec 治理、Direct LLM v2 转换器作为复用基础。

1. T01 → T02 → T03：外部 Job 所有权、持久化与幂等导入。
2. T04 → T05 → T06 → T07：数据/Profile、配置、Parser 和公共入口。
3. 先完成 M6-T01/T02/T03，再做 T09 → T08 → T10；T11 追加独立 CMMLU 验收。

工程量估算 27–46 人日，包含实现、focused 回归和工作包 review，不包含等待许可证据、预算批准、外部环境与人工标注。它是初步排期范围，不是交付承诺；完成前两包后按实际吞吐重新估计。每个工作包一般拆 1–3 个可独立测试的提交，固定“一项主功能 + 一项验证”的在制品限制。

## 2. 工作包实施卡

接口字段、错误与不变量以本阶段 Spec 第 3 节及后续契约章节为输入；下方“消费 → 产出”明确交接边界。远期 Python 私有函数签名不预先锁死；实际开工时先核对已合入公共契约并补可执行单测，再实现。每张卡都须完成所有步骤，不能用最后一个集成测试替代前置反例。

<a id="m2-t01"></a>
### M2-T01：现有 Backend → Job 协议

**依赖：** M1-T01, M1-T03。**估算：** 2-3 人日。**范围：** 增加 execution_mode 和 ExternalJob 数据契约，保留旧 sample 模式。

**需求关联：** M2-G01；**主责验收：** 使用本卡专属反例与阶段集成验收，不借用其他工作包的通过状态。

**文件与职责落位：**

- 新增计划 `packages/contracts/motte_contracts/external_job.py`。
- 修改 `packages/sdk-python/motte_sdk/execution_backends.py`。
- 修改 `packages/sdk-python/motte_sdk/dispatcher.py`。

**测试：** `tests/contract/test_external_job.py`；主断言名 `test_sample_and_job_dispatch_are_distinct`（Web 使用同义描述）。

- [ ] 准备反例和期望：旧 sample handle 可用；缺 runner/profile/environment 版本拒绝；Job Run 不逐题 invoke。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/contract/test_external_job.py
```

- [ ] 以显式 execution_mode 分支扩展 handle，保持 Direct/Replay 兼容。
- [ ] 定义 JobSpec、JobHandle、NormalizedCaseResult、ImportBatch 与 prepare/start/poll/interrupt/collect/cleanup 协议。
- [ ] 固定 launch_token、selected_case_ids、retry policy 与环境 hash，API 只创建不启动。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t02"></a>
### M2-T02：JobSpec →受控句柄

**依赖：** M2-T01, M1-T07。**估算：** 3-5 人日。**范围：** start/poll/interrupt/cleanup wrapper、launch_token、资源所有权。

**需求关联：** M2-G01, M2-G02, M2-G03；**主责验收：** M2-A07。

**文件与职责落位：**

- 新增计划 `packages/benchmark-runtime/motte_benchmark/protocol.py`。
- 新增计划 `packages/benchmark-runtime/motte_benchmark/process.py`。
- 新增计划 `packages/benchmark-runtime/motte_benchmark/registry.py`。
- 新增计划 `packages/sdk-python/motte_sdk/external_jobs.py`。
- 新增计划 `packages/benchmark-runtime/pyproject.toml`。

**测试：** `tests/runtime/test_external_job_lifecycle.py`；主断言名 `test_launch_once_and_pid_reuse`（Web 使用同义描述）。

- [ ] 准备反例和期望：四样本同 Job 的 start 计数 1；PID 被无关进程复用时不误杀；超时/取消只停自有进程树。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/runtime/test_external_job_lifecycle.py
```

- [ ] 先持久启动意图后调用 wrapper，launch_token 传入进程与资源标签。
- [ ] 实现有界 stdout/stderr 消费、poll、interrupt、cleanup 与残留报告，prepare 无模型执行。
- [ ] 启动是否成功无法证明时隔离 needs_review，有 token 可核验时仅恢复观察。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t03"></a>
### M2-T03：外部结果 →同一 Run 事实

**依赖：** M2-T02。**估算：** 3-5 人日。**范围：** Job repository、采集检查点、幂等导入、needs_review 与取消映射。

**需求关联：** M2-G02, M2-G03, M2-G04, M2-G11；**主责验收：** M2-A05, M2-A06, M2-A08, M2-A09。

**文件与职责落位：**

- 新增计划 `packages/storage/motte_storage/external_jobs.py`。
- 修改 `packages/storage/motte_storage/run_store.py`。
- 修改 `packages/storage/motte_storage/postgres.py`。
- 新增计划 `packages/sdk-python/motte_sdk/external_jobs.py`。
- 修改 `packages/sdk-python/motte_sdk/service.py`。

**测试：** `tests/storage/test_external_job_store.py`；主断言名 `test_idempotent_collect_crash_and_conflict`（Web 使用同义描述）。

- [ ] 准备反例和期望：相同 key+hash 重复采集无新增；不同内容 conflict；采集前后崩溃无重复评分；取消后迟到结果不复活。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/storage/test_external_job_store.py
```

- [ ] 实现 Job/导入 key/checkpoint 的 SQLite PG InMemory 存储及事务。
- [ ] 冻结 raw Artifact 后解析，结果与 checkpoint 同事务提交，所有 selected Case 生成 disposition。
- [ ] 将外部状态映射既有 Run、CaseAttempt、ScoringPass，禁止借用逐 Case second-chance 启动 Job。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t04"></a>
### M2-T04：本地/固定源 → ready dataset

**依赖：** M2-T01。**估算：** 3-5 人日。**范围：** 校验、来源、许可、split、Case 清单、受控获取与断点处理。

**需求关联：** M2-G05, M2-G06, M2-G07；**主责验收：** M2-A12。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/benchmark_catalog.py`。
- 修改 `packages/sdk-python/motte_sdk/dataset_sources.py`。
- 修改 `packages/sdk-python/motte_sdk/managed_sources.py`。
- 修改 `packages/sdk-python/motte_sdk/adapters/restricted_chinese.py`。
- 修改 `packages/contracts/motte_contracts/dataset_sources.py`。
- 新增计划 `packages/benchmark-runtime/motte_benchmark/opencompass/profiles.py`。

**测试：** `tests/benchmarks/test_ceval_prepare.py`；主断言名 `test_dataset_prepare_respects_source_governance`（Web 使用同义描述）。

- [ ] 准备反例和期望：损坏 hash、重复 ID、半下载均不 ready；受限来源自报 approval 不解锁；无 gold 输出 unscored。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/benchmarks/test_ceval_prepare.py
```

- [ ] 复用 SourceSpec/revision/checksum/license 和已存在纯转换器，不复制数据治理服务。
- [ ] 增加外部 Benchmark 的 catalog/profile 投影、准备状态与样本清单，区分合法本地自供与已核验官方来源。
- [ ] 将当前 C-Eval restricted 状态设为真实准备前置：受信证据核验器与固定版本许可记录未就绪保持阻断，合成数据可继续开发。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t05"></a>
### M2-T05：已发布资源 → Runner 配置

**依赖：** M2-T04。**估算：** 2-4 人日。**范围：** Profile、few-shot、学科、模型参数 allowlist、脱敏配置。

**需求关联：** M2-G06, M2-G08, M2-G09, M2-G12；**主责验收：** 使用本卡专属反例与阶段集成验收，不借用其他工作包的通过状态。

**文件与职责落位：**

- 新增计划 `packages/benchmark-runtime/motte_benchmark/opencompass/config.py`。
- 新增计划 `packages/benchmark-runtime/motte_benchmark/opencompass/profiles.py`。
- 修改 `packages/sdk-python/motte_sdk/context_preflight.py`。

**测试：** `tests/benchmarks/test_opencompass_config.py`；主断言名 `test_profile_no_gold_or_secret_and_no_retry_stacking`（Web 使用同义描述）。

- [ ] 准备反例和期望：固定四题模板只有合法 few-shot gold；目标答案和密钥不入配置；未知参数拒绝；原生/Provider/操作员重试分别记录。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/benchmarks/test_opencompass_config.py
```

- [ ] 锁定 Runner 与环境依赖，校验学科/split/顺序/seed/few-shot/prompt/extractor/aggregation。
- [ ] 通过 allowlist 转换 ModelProfile，选择可核验 Provider hook 或标明 runner-native 与观测缺口。
- [ ] 临时引用凭据并生成脱敏配置 hash，scope=smoke/custom-subset/full 始终随报告。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t06"></a>
### M2-T06：冻结原始输出 →样本/指标

**依赖：** M2-T04。**估算：** 3-5 人日。**范围：** 迁旧 Parser、提取器和聚合，保留 native/diagnostic。

**需求关联：** M2-G07, M2-G08, M2-G10, M2-G11, M2-G16；**主责验收：** M2-A03, M2-A04, M2-A11, M2-A13。

**文件与职责落位：**

- 新增计划 `packages/benchmark-runtime/motte_benchmark/opencompass/parser.py`。
- 新增计划 `packages/evaluators/motte_eval/ceval.py`。
- 新增计划 `tests/fixtures/benchmarks/ceval/manifest.json`。

**测试：** `tests/benchmarks/test_ceval_parser_parity.py`；主断言名 `test_parser_native_diagnostic_parity`（Web 使用同义描述）。

- [ ] 准备反例和期望：selected=4、3 条回答中 2 对，selected accuracy=.5、coverage=.75；无 aggregate 不捏造 native；同源新旧逐条对照。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/benchmarks/test_ceval_parser_parity.py
```

- [ ] 从固定旧提交提取 Parser/提取算法及合法 fixture，记录来源/许可/删去控制面依赖。
- [ ] 分开保存 native.* 与 diagnostic.*，解释学科宏平均和样本加权差异。
- [ ] 限制 JSON/CSV/压缩输入、拒绝路径逃逸、半写文件；差异报告按 parser/scorer version 记录。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t07"></a>
### M2-T07：Plugin →公共入口

**依赖：** M2-T03, M2-T05, M2-T06。**估算：** 2-4 人日。**范围：** 注册 C-Eval，Catalog/预检，API/CLI 调用同一准备服务。

**需求关联：** M2-G05；**主责验收：** M2-A01。

**文件与职责落位：**

- 修改 `packages/sdk-python/motte_sdk/benchmark_plugins.py`。
- 新增计划 `packages/sdk-python/motte_sdk/benchmark_catalog.py`。
- 修改 `apps/api/app/main.py`。
- 修改 `apps/api/app/schemas.py`。
- 修改 `packages/cli/motte_cli/main.py`。
- 新增计划 `packages/benchmark-runtime/motte_benchmark/opencompass/adapter.py`。

**测试：** `tests/api/test_ceval_run.py`；主断言名 `test_ceval_preflight_and_enqueue`（Web 使用同义描述）。

- [ ] 准备反例和期望：Runner 不存在时 422 且 0 调用；合法合成 Profile 创建 202；重启继续采集；静态预检不触发模型探针。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/api/test_ceval_run.py
```

- [ ] 将 C-Eval 注册为正式 job-based suite，连接 prepare/validate/preview/run/report 与 artifact 查询。
- [ ] CLI 与 API 共用资源准备和预检服务，状态 registered/prepared/runnable/verified 单独显示。
- [ ] 生成 API/TS 契约并保留 Direct LLM 转换数据的独立 suite/Profile 名称，禁止自动升格官方成绩。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t08"></a>
### M2-T08：API 事实 → Web 工作区

**依赖：** M2-T07, M2-T09。**估算：** 3-4 人日。**范围：** 操作、题目、监控、结果、比较；共享模型与状态组件。

**需求关联：** M2-G14；**主责验收：** 使用本卡专属反例与阶段集成验收，不借用其他工作包的通过状态。

**文件与职责落位：**

- 新增计划 `apps/web/src/evalTypes/ceval/CevalPages.tsx`。
- 修改 `apps/web/src/evalTypes/registry.ts`。
- 修改 `apps/web/src/api/client.ts`。
- 修改 `apps/web/src/App.tsx`。

**测试：** `apps/web/tests/CevalPages.test.tsx`；主断言名 `test_ceval_scope_partial_and_comparability`（Web 使用同义描述）。

- [ ] 准备反例和期望：五页可导航；空预测/部分失败/版本差异有说明；缺覆盖的比较不显示放行；restricted 无绕过按钮。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
pnpm --dir apps/web test -- tests/CevalPages.test.tsx
```

- [ ] 实现操作、题目、监控、结果、比较页面并复用模型与状态组件。
- [ ] 展示学科/split/few-shot/Profile/scope、native/diagnostic、not_attempted 和成本 unknown。
- [ ] 静态能力与真实已验证状态分开，预检失败保留配置，键盘和长日志用既有交互规范。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t09"></a>
### M2-T09：两个报告 →最低质量结论

**依赖：** M2-T07, M6-T01, M6-T02, M6-T03。**估算：** 1-2 人日。**范围：** 交付 M6-Lite 同一比较/门禁服务的首批子集。

**需求关联：** M2-G15；**主责验收：** M2-A10, M2-A14。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/benchmark_catalog.py`。
- 修改 `apps/api/app/main.py`。
- 新增计划 `tests/evaluators/test_benchmark_gate_lite.py`。

**测试：** `tests/evaluators/test_benchmark_gate_lite.py`；主断言名 `test_ceval_uses_shared_gate`（Web 使用同义描述）。

- [ ] 准备反例和期望：同集同 Profile 可比；漏一题 coverage Gate 不通过；不同 extractor 的质量指标不能直接比较。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/evaluators/test_benchmark_gate_lite.py
```

- [ ] 只装配 M6-T01/T02/T03 的同一 Comparison/Gate 服务，禁止新增 ceval_gate 引擎。
- [ ] 以固定 Run+ScoringPass 为输入暴露比较与门禁，显示逐规则不足原因。
- [ ] 编写 C-Eval 适配契约回归与两报告示例，所有读取不执行 Runner/Judge。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t10"></a>
### M2-T10：全链路 →验收记录

**依赖：** M2-T08, M2-T09。**估算：** 3-5 人日。**范围：** 假 Runner、真实 Runner 确定性端点、显式小样本 live、部署和兼容文档。

**需求关联：** M2-G13, M2-G17；**主责验收：** M2-A02, M2-A15。

**文件与职责落位：**

- 新增计划 `tests/integration/test_ceval_external_job.py`。
- 新增计划 `docs/operations/ceval.md`。
- 新增计划 `docs/protocols/external-jobs.md`。
- 新增计划 `docs/verification/M2.md`。

**测试：** `tests/integration/test_ceval_external_job.py`；主断言名 `test_ceval_fixture_real_runner_and_live_layers`（Web 使用同义描述）。

- [ ] 准备反例和期望：假 Runner 一次 Job 闭环；真实固定 Runner 对本地确定性端点产出实际工件；授权 smoke 与 full Profile 分开记录。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/integration/test_ceval_external_job.py
```

- [ ] 完成 fake Runner 故障矩阵，再用独立 Runner 环境和假 HTTP 验证真实命令/Parser。
- [ ] 准备授权小样本及完整 Profile 的两档验收清单，固定版本/范围/预算/原始 hash 与旧新差异报告。
- [ ] review 全部 G/A、支持/禁用回退、许可和来源，任何真实验证缺项保留 blocked/not_run。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m2-t11"></a>
### M2-T11：同 Runner → CMMLU

**依赖：** M2-T10。**估算：** 2-4 人日。**范围：** 独立数据/Profile/Parser fixture 与页面声明。

**需求关联：** 本任务属于追加/支撑范围，仍须独立验收；**主责验收：** 使用本卡专属反例与阶段集成验收，不借用其他工作包的通过状态。

**文件与职责落位：**

- 新增计划 `packages/benchmark-runtime/motte_benchmark/opencompass/cmmlu.py`。
- 新增计划 `packages/benchmark-runtime/motte_benchmark/opencompass/profiles.py`。
- 修改 `packages/sdk-python/motte_sdk/adapters/restricted_chinese.py`。
- 新增计划 `tests/benchmarks/test_cmmlu_external_profile.py`。

**测试：** `tests/benchmarks/test_cmmlu_external_profile.py`；主断言名 `test_cmmlu_has_own_identity_and_evidence`（Web 使用同义描述）。

- [ ] 准备反例和期望：CMMLU 使用独立 revision/Profile/样本 hash，不能读 C-Eval 验证勾选；受限来源未核验时不执行。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/benchmarks/test_cmmlu_external_profile.py
```

- [ ] 复用 Job supervisor 与纯转换基础，独立配置学科/split/prompt/extractor/aggregation。
- [ ] 建 CMMLU 专用 native/diagnostic golden、来源和支持记录、UI 范围声明。
- [ ] 独立确定性 Runner/授权 live 验收及 full/smoke 标识，追加状态不影响 C-Eval Core 但不得计为已完成。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M2.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

## 3. 全部目标到任务的覆盖表

目标复选框保留未完成；该表证明规划覆盖，不证明功能已实现。

| 目标 | 原始要求 | 责任工作包 |
|---|---|---|
| M2-G01 | 一个 job-based Run 只启动一个明确的外部 Job，不逐题重启 Runner。 | [M2-T01](M2.md#m2-t01)、[M2-T02](M2.md#m2-t02) |
| M2-G02 | Job 句柄、配置哈希、工作目录、进程/容器所有权和采集进度持久化。 | [M2-T02](M2.md#m2-t02)、[M2-T03](M2.md#m2-t03) |
| M2-G03 | 启动不确定、非零退出、缺结果、部分结果、取消有独立处理。 | [M2-T02](M2.md#m2-t02)、[M2-T03](M2.md#m2-t03) |
| M2-G04 | 重复采集幂等；同键不同内容必须冲突报警，不能静默覆盖。 | [M2-T03](M2.md#m2-t03) |
| M2-G05 | Catalog 区分 registered、prepared、runnable、verified 和不支持原因。 | [M2-T04](M2.md#m2-t04)、[M2-T07](M2.md#m2-t07) |
| M2-G06 | Dataset/Profile/Runner/scorer 及选中 Case 集合冻结；没有占位 revision/digest。 | [M2-T04](M2.md#m2-t04)、[M2-T05](M2.md#m2-t05) |
| M2-G07 | 数据与代码许可、获取方式、来源、校验和分别记录。 | [M2-T04](M2.md#m2-t04)、[M2-T06](M2.md#m2-t06) |
| M2-G08 | C-Eval 学科、split、few-shot、prompt、提取策略和 token 设置可解释。 | [M2-T05](M2.md#m2-t05)、[M2-T06](M2.md#m2-t06) |
| M2-G09 | 输入仅包含任务要求和合法示例，不泄漏目标样本 gold。 | [M2-T05](M2.md#m2-t05) |
| M2-G10 | Runner 原始指标与平台诊断指标分开存储、命名和显示。 | [M2-T06](M2.md#m2-t06) |
| M2-G11 | 每个 selected Case 有明确 disposition；未尝试不消失。 | [M2-T03](M2.md#m2-t03)、[M2-T06](M2.md#m2-t06) |
| M2-G12 | 调用路径、身份可见度、usage/cost 完整度和重试设置进入证据。 | [M2-T05](M2.md#m2-t05) |
| M2-G13 | 假 Runner E2E、真实 Runner 确定性集成、显式 live 分层验收。 | [M2-T10](M2.md#m2-t10) |
| M2-G14 | C-Eval 操作/题目/监控/结果/比较页面复用统一组件。 | [M2-T08](M2.md#m2-t08) |
| M2-G15 | M6-Lite 可以判断同集同 Profile 的覆盖与阈值，不依赖完整实验系统。 | [M2-T09](M2.md#m2-t09) |
| M2-G16 | 旧新 Parser 对照报告存在，所有差异有版本化解释。 | [M2-T06](M2.md#m2-t06) |
| M2-G17 | 关闭外部 adapter 不影响 Direct/GSM8K/Replay，也不破坏历史读取。 | [M2-T10](M2.md#m2-t10) |

## 4. 全部验收场景的执行责任

每行必须在实现阶段记录真实测试/操作证据；矩阵中主责包需写反例，最终交付包汇总复核。跨端/真实环境场景不能由纯函数测试替代。

| 验收 ID | 注入条件 | 必须观察 | 主责工作包 |
|---|---|---|---|
| M2-A01 | 未准备数据/Runner 不存在 | 创建前失败，模型调用 0 | [M2-T07](M2.md#m2-t07) |
| M2-A02 | 正常完整 Job | start 一次；所有 Case 和原始 Artifact 可查询 | [M2-T10](M2.md#m2-t10) |
| M2-A03 | 原始目录缺 details，仅有聚合 | 保留聚合与缺失标记；不伪造样本 | [M2-T06](M2.md#m2-t06) |
| M2-A04 | 空输出/全空预测 | 0 条结果或未作答按 Profile 处理，不能 completed+通过 | [M2-T06](M2.md#m2-t06) |
| M2-A05 | Runner 非零退出且有部分结果 | 部分结果保留，全部 selected Case 有处置状态 | [M2-T03](M2.md#m2-t03) |
| M2-A06 | 同键结果重复/内容改变 | 相同 no-op；改变 conflict，禁止覆盖 | [M2-T03](M2.md#m2-t03) |
| M2-A07 | start 后句柄未落库时崩溃 | 可核验 token 时重新观察，否则 needs_review，不重启 Job | [M2-T02](M2.md#m2-t02) |
| M2-A08 | 采集落库前后崩溃 | 幂等恢复采集，无重复 Score/Artifact 关联 | [M2-T03](M2.md#m2-t03) |
| M2-A09 | 取消时 Runner 仍输出 | 终态不复活，迟到结果仅按审计政策接收 | [M2-T03](M2.md#m2-t03) |
| M2-A10 | 选科/子集/不同 prompt | manifest 有差异，比较返回具体原因 | [M2-T09](M2.md#m2-t09) |
| M2-A11 | 原始分数和平台诊断分数不同 | 两份保留且说明 extractor/version | [M2-T06](M2.md#m2-t06) |
| M2-A12 | 无 gold 或来源未确认 | unscored/user-supplied，不冒充完整官方分数 | [M2-T04](M2.md#m2-t04) |
| M2-A13 | 密钥/路径泄漏、超大 CSV、symlink | 拒绝或脱敏且记录，不能读取任意宿主文件 | [M2-T06](M2.md#m2-t06) |
| M2-A14 | 费用未知 | unknown+coverage，成本硬门禁不能通过 | [M2-T09](M2.md#m2-t09) |
| M2-A15 | 新 adapter 禁用 | 旧 Direct/GSM8K/Replay 与历史报告不受影响 | [M2-T10](M2.md#m2-t10) |

## 5. 阶段验证、review 与退出门

focused 全绿后执行相邻回归；这些路径在核对基线存在：

```bash
uv run pytest -q -m "not live" tests/sdk/test_dataset_sources.py tests/sdk/test_managed_source_integrity.py tests/sdk/test_restricted_chinese_adapters.py tests/runtime/test_execution_backends.py tests/api/test_suite_isolation.py
uv run ruff check .
uv run mypy packages/contracts
pnpm --dir apps/web test
pnpm --dir apps/web build
make check
```

API 变化还要 `make openapi` 并审阅 `api/openapi.json` / `apps/web/src/api/schema.d.ts` 差异；生成结果应与代码同提交。`make check` 已含 `pnpm -r test`，CI 还含依赖审计、配置扫描和生成类型漂移检查，不能声称两者完全等价。PG/Docker/live 依赖缺失记 blocked 或 not_run，skip 不计通过。

阶段退出条件：C-Eval 全公共链路、真实 Runner 确定性端点、授权 smoke 与选定完整 Profile 验收；CMMLU 自有来源/范围/证据独立收口。

- [ ] 需求 review：第 3/4/6 节每项有对应代码、测试与证据，所有新增/增强项保留状态。
- [ ] 契约 review：上游输入和下游消费一致；版本/缺失/身份/状态/评分不可变，无重复注册表或新调度主权。
- [ ] 故障 review：创建、dispatch、外部完成、采集、评分提交、取消与恢复边界都有注入，调用次数/资源所有权可核验。
- [ ] 安全 review：gold/秘密隔离、输入限额、路径/权限/费用政策及不可观察范围有具体断言。
- [ ] 用户链路 review：API/CLI/Web 对同一 Run/pass 一致，禁用与错误原因、键盘/空态/长日志可用。
- [ ] 交付 review：文档/兼容矩阵/操作命令/回退与代码一致；未验证外部组合不宣传 supported。
- [ ] 将 review 缺陷登记为 R-ID、严重度、复现、责任包、修复 commit、复验命令和结果。P0/P1/P2 修复后重跑受影响 focused、相邻回归及阶段门禁；P3 也应关闭或明确记录不影响退出条件的理由。未满足任一必备退出项不得通过。

实现时新增/更新的文档：

- `docs/operations/ceval.md`
- `docs/operations/cmmlu.md`
- `docs/protocols/external-jobs.md`
- `docs/migration/ceval-parser-parity.md`
- `docs/protocols/provider-compatibility.md`
- `docs/verification/M2.md`

回退遵循需求文档：先禁止新能力创建并确认存量执行/评分状态，再停用对应 backend/adapter/policy；保留 schema、原始证据和不可变历史，不删除未知活跃工作目录。涉及导入/恢复时仅操作明确归属资源。

下游交接：M3 复用 ExternalJob/token/checkpoint；M6 消费 native/diagnostic 指标与固定 Profile。

## 6. 非编号要求覆盖与计划 review

不能只核对 G/T/A 数量。下表把原需求的所有二级章节交给责任工作包；章节内的字段、不变量、接口、样例、运行约束、验证和回退均属于这些工作包的验收范围。原 Spec 随本计划一起交付，完整内容不被缩减成表格摘要。

| 原需求章节 | 责任工作包 | review 重点 |
|---|---|---|
| 第 1 节：起点与范围 | [M2-T01](M2.md#m2-t01)、[M2-T04](M2.md#m2-t04)、[M2-T10](M2.md#m2-t10)、[M2-T11](M2.md#m2-t11) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 2 节：最终具体目标清单 | [M2-T01](M2.md#m2-t01)、[M2-T02](M2.md#m2-t02)、[M2-T03](M2.md#m2-t03)、[M2-T04](M2.md#m2-t04)、[M2-T05](M2.md#m2-t05)、[M2-T06](M2.md#m2-t06)、[M2-T07](M2.md#m2-t07)、[M2-T08](M2.md#m2-t08)、[M2-T09](M2.md#m2-t09)、[M2-T10](M2.md#m2-t10)、[M2-T11](M2.md#m2-t11) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 3 节：模块、文件与责任边界 | [M2-T01](M2.md#m2-t01)、[M2-T03](M2.md#m2-t03)、[M2-T04](M2.md#m2-t04)、[M2-T06](M2.md#m2-t06)、[M2-T07](M2.md#m2-t07)、[M2-T08](M2.md#m2-t08)、[M2-T09](M2.md#m2-t09) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 4 节：job-based 契约 | [M2-T01](M2.md#m2-t01)、[M2-T02](M2.md#m2-t02)、[M2-T03](M2.md#m2-t03) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 5 节：数据、Profile 与 C-Eval 口径 | [M2-T04](M2.md#m2-t04)、[M2-T05](M2.md#m2-t05)、[M2-T06](M2.md#m2-t06) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 6 节：模型、费用与环境边界 | [M2-T02](M2.md#m2-t02)、[M2-T05](M2.md#m2-t05)、[M2-T10](M2.md#m2-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 7 节：工作包与执行次序 | [M2-T01](M2.md#m2-t01)、[M2-T02](M2.md#m2-t02)、[M2-T03](M2.md#m2-t03)、[M2-T04](M2.md#m2-t04)、[M2-T05](M2.md#m2-t05)、[M2-T06](M2.md#m2-t06)、[M2-T07](M2.md#m2-t07)、[M2-T08](M2.md#m2-t08)、[M2-T09](M2.md#m2-t09)、[M2-T10](M2.md#m2-t10)、[M2-T11](M2.md#m2-t11) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 8 节：验收矩阵 | [M2-T10](M2.md#m2-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 9 节：用户入口与交付物 | [M2-T07](M2.md#m2-t07)、[M2-T08](M2.md#m2-t08) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 10 节：验证命令与回退 | [M2-T03](M2.md#m2-t03)、[M2-T10](M2.md#m2-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 11 节：固定来源与交接 | [M2-T06](M2.md#m2-t06)、[M2-T10](M2.md#m2-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |

计划 review 结论及修订见 [REVIEW.md](REVIEW.md)。本次只完成文档审阅，以上所有实现步骤与阶段退出门保持未勾选。
