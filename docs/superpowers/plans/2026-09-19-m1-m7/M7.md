# M7 SDK、迁移、备份、安全与发布切换 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. 本次只完成规划与计划 review；后续逐工作包实现，不将本文件作为付费调用、生产切换或自动合并的授权。

**Goal:** 完整交付本阶段需求文档中的全部目标、工作包及验收场景，包括明确列出的追加/增强项。

**Architecture:** 复用现有 RunDispatcher、CaseAttempt、不可变资源和 ScoringPass。契约由责任阶段维护，后续阶段增量消费；错误、缺失、取消和不确定结果分别保留。

**Tech Stack:** Python 3.12 / uv / FastAPI / Pydantic；React / Vite / pnpm 9.15.0 / Node 24；SQLite / PostgreSQL；按任务需要使用受控 Docker 或独立 Runner。

**Spec:** [M7-sdk-migration-and-release.md](../../../roadmap/M7-sdk-migration-and-release.md) 全文；[执行总安排](README.md)与[需求索引](../../../roadmap/README.md)。

**核对基线:** `b6c402b62f0a19f79cde719af60c8bb73672c877`；计划状态 `planned`，实施验证 `not_run`。当前仓库事实是静态代码核对，不是本次 runtime 测试通过证明。

## Global Constraints

- “保持单用户、API-first、Python/FastAPI/Pydantic、React/Vite、现有 uv/pnpm workspace。”
- “Run、Case、Trial、传输重试、操作员 retry 和评分 pass 的身份分开。”
- “已发布资源与终态评分不可覆盖。”
- “MetricResult 是评估视图，持久化仍进入既有 ScoreSet。”
- “默认测试不使用真实模型或个人订阅。” subject、Judge、Harness 的 live 分别授权和计量。
- 遵守 [AGENTS.md](../../../../AGENTS.md) 与 [DESIGN.md](../../../../apps/web/DESIGN.md)。运行状态由 STATUS_META 派生；Radix / Phosphor Icon 后缀；新 token 先登记 DESIGN.md 再加 :root。
- 各任务的路径是仓库相对路径；“修改”表示当前存在，“新增计划”表示尚不存在，最终落位在开工时重新核对。新 schema 沿实际 migration head 追加，不预占 revision。
- 下文测试命令为将来执行步骤，缺文件/ImportError 只说明尚未实现，不能作为业务红灯或通过记录。先写可收集的断言测试，红灯必须对应列出的行为缺口。

## Review Focus

- 创建响应丢失导致重复付费 Run，T01 服务端幂等事务。
- 远端断连回退本地或等待超时擅自取消，T02/T03 固定语义。
- 导入只有总分却伪造明细，T07 保留 legacy summary 与 unknown。
- 备份忽略评分/发布/GC 写入，T09 测全部写入口与 staging 恢复。
- TTL 删除 pinned 工件或恢复自动重跑，T09/T10 保护引用与未决状态。

## 1. 进入条件与分批安排

SDK/打包/备份安全从前期预备；最终 RC 以 M1–M6 实际证据为门。

1. T01 → T02 → T03 → T04 → T05：同步客户端、远程 CLI、导出、干净打包。
2. T06 → T07 → T08：合成旧数据规划/导入/恢复；真实来源 apply 另走操作审阅。
3. T09 → T10：一致快照、staging 恢复、认证与引用感知 GC；设计与测试从 M1 开始。
4. T11 → T12：支持环境 RC、三替代场景、全77工作包核对与切换记录。

工程量估算 37–59 人日，包含实现、focused 回归和工作包 review，不包含等待许可证据、预算批准、外部环境与人工标注。它是初步排期范围，不是交付承诺；完成前两包后按实际吞吐重新估计。每个工作包一般拆 1–3 个可独立测试的提交，固定“一项主功能 + 一项验证”的在制品限制。

## 2. 工作包实施卡

接口字段、错误与不变量以本阶段 Spec 第 3 节及后续契约章节为输入；下方“消费 → 产出”明确交接边界。远期 Python 私有函数签名不预先锁死；实际开工时先核对已合入公共契约并补可执行单测，再实现。每张卡都须完成所有步骤，不能用最后一个集成测试替代前置反例。

<a id="m7-t01"></a>
### M7-T01：公共 API →SDK 类型/错误

**依赖：** M1-T03, M6-T03, M6-T07。**估算：** 3-5 人日。**范围：** 同步客户端、超时、兼容协商、安全重试与幂等请求。

**需求关联：** M7-G01, M7-G02, M7-G03；**主责验收：** M7-A02, M7-A03。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/client.py`。
- 新增计划 `packages/sdk-python/motte_sdk/client_errors.py`。
- 新增计划 `packages/sdk-python/motte_sdk/client_types.py`。
- 修改 `apps/api/app/main.py`。
- 修改 `apps/api/app/schemas.py`。
- 新增计划 `packages/storage/motte_storage/requests.py`。

**测试：** `tests/sdk/test_client_contract.py`；主断言名 `test_client_typed_errors_and_idempotent_post`（Web 使用同义描述）。

- [ ] 准备反例和期望：422 不重试；服务已建 Run 响应丢失后同 key/body 返回同 Run；不同 body 冲突；import SDK 不启动 DB。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/sdk/test_client_contract.py
```

- [ ] 同步 HTTP client 类型来自公共契约，区分 timeout/auth/config/server 与 request ID。
- [ ] 在服务端公共创建入口事务绑定 idempotency key+请求 hash，所有客户端共享；默认只安全查询重试。
- [ ] 实现资源读取/发布、Run submit/get/list/cancel/retry、指定 pass/rescore 和 compare/Gate 能力协商。
- [ ] 增加 Experiment preview/create/get/cancel、Baseline create/list/select 以及 Artifact 元数据读取；服务端能力未就绪返回类型化 unsupported，不在客户端模拟实现。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t02"></a>
### M7-T02：Run/事件 →等待与流

**依赖：** M7-T01。**估算：** 2-4 人日。**范围：** wait、needs_review、取消、游标、终态与断线恢复。

**需求关联：** M7-G02, M7-G05；**主责验收：** M7-A05。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/client.py`。
- 新增计划 `packages/sdk-python/motte_sdk/client_types.py`。
- 修改 `apps/api/app/main.py`。
- 修改 `apps/web/src/hooks/useRunEvents.ts`。

**测试：** `tests/sdk/test_wait_and_events.py`；主断言名 `test_wait_timeout_and_sse_gap_recovery`（Web 使用同义描述）。

- [ ] 准备反例和期望：等待超时不取消远端；needs_review 直接返回；重复/断流/终态事件去重，缺口无法补齐标 partial。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/sdk/test_wait_and_events.py
```

- [ ] 增加总期限、取消本地等待、poll/SSE 游标与响应大小限制。
- [ ] Last-Event-ID/after 重连后用持久事件补齐，终态后再对账最后事件和最终报告。
- [ ] 中断/长流错误释放连接，日志不记录 auth/prompt；Web 与 SDK 遵守同一游标规则。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t03"></a>
### M7-T03：本地/远程配置 →CLI 一致输出

**依赖：** M7-T01, M7-T02, M6-T10。**估算：** 2-3 人日。**范围：** server/local 明确路由、同参数验证、错误和 JSON。

**需求关联：** M7-G02, M7-G04；**主责验收：** M7-A04。

**文件与职责落位：**

- 修改 `packages/cli/motte_cli/main.py`。
- 修改 `packages/cli/motte_cli/commands.py`。
- 新增计划 `packages/sdk-python/motte_sdk/client.py`。

**测试：** `tests/cli/test_remote_parity.py`；主断言名 `test_remote_failure_never_runs_local`（Web 使用同义描述）。

- [ ] 准备反例和期望：远程断开 CLI 非零退出且本地 DB/Provider 调用计数 0；同参数两模式验证一致；stdout 可解析 JSON。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/cli/test_remote_parity.py
```

- [ ] 显式 server/local 选择和配置优先级，远程只调用 HTTP client。
- [ ] 统一 create/wait/cancel/retry/events/pass/compare/Gate 参数与错误 envelope。
- [ ] 保留旧 CLI 兼容及弃用说明，诊断仅 stderr，live subject/Judge/Harness 各自显式开关。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t04"></a>
### M7-T04：report/Gate →pytest/导出

**依赖：** M7-T03, M6-T10。**估算：** 2-4 人日。**范围：** 显式 marker、一次 callable Trace、JSON/JUnit/退出码。

**需求关联：** M7-G06, M7-G07；**主责验收：** M7-A06, M7-A07。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/tracing.py`。
- 新增计划 `packages/sdk-python/motte_sdk/pytest_plugin.py`。
- 新增计划 `packages/sdk-python/motte_sdk/export.py`。
- 修改 `packages/sdk-python/pyproject.toml`。

**测试：** `tests/sdk/test_pytest_and_exports.py`；主断言名 `test_pytest_opt_in_trace_once_and_junit_unknown`（Web 使用同义描述）。

- [ ] 准备反例和期望：普通 pytest 安装插件 0 付费调用；callable 抛错/flush 失败仍只执行一次；证据不足为 JUnit error。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/sdk/test_pytest_and_exports.py
```

- [ ] 显式 marker/fixture 区分读取报告 Gate 与真实运行，不自动发现并执行数据集。
- [ ] Trace decorator/context manager 记录一次调用与嵌套 span、脱敏、截断及部分 flush。
- [ ] 扩展 M6 同一 exporter：quality failure/error/not_applicable 对应 failure/error/skipped，空集显式不足。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t05"></a>
### M7-T05：workspace →可安装产物

**依赖：** M7-T01。**估算：** 3-5 人日。**范围：** 真实包依赖、wheel、资源打包、Web/Runner 隔离。

**需求关联：** M7-G08, M7-G09；**主责验收：** M7-A01。

**文件与职责落位：**

- 修改 `packages/sdk-python/motte_sdk/__init__.py`。
- 修改 `packages/sdk-python/pyproject.toml`。
- 修改 `pyproject.toml`。
- 修改 `infra/docker-compose.yml`。
- 修改 `.github/workflows/ci.yml`。

**测试：** `tests/packaging/test_clean_install.py`；主断言名 `test_clean_wheel_install_outside_checkout`（Web 使用同义描述）。

- [ ] 准备反例和期望：临时 venv 在仓库外无 PYTHONPATH 能 import/client 调假 API；各 wheel 依赖可解析；Runner 不在 API 必需依赖。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/packaging/test_clean_install.py
```

- [ ] 逐包审计 imports 和 pyproject 真实依赖/build backend/资源打包，修正 SDK 根导入副作用。
- [ ] 构建所有声明交付 wheel 与 Web 静态资源，在干净 venv 安装并测试 CLI/资源读取。
- [ ] 固定 Runner 独立镜像与 API/client extras 边界，记录离线 wheelhouse/lock 与构建 provenance。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t06"></a>
### M7-T06：旧导出包 →迁移计划

**依赖：** M5-T01, M5-T06。**估算：** 3-4 人日。**范围：** ImportManifest、来源验证、数量/hash、dry-run、凭据待绑定。

**需求关联：** M7-G10, M7-G11；**主责验收：** M7-A08, M7-A11。

**文件与职责落位：**

- 新增计划 `packages/contracts/motte_contracts/imports.py`。
- 新增计划 `packages/sdk-python/motte_sdk/migration/plan.py`。
- 新增计划 `packages/sdk-python/motte_sdk/migration/sources.py`。

**测试：** `tests/migration/test_import_plan.py`；主断言名 `test_dry_run_no_mutation_and_source_hash_change`（Web 使用同义描述）。

- [ ] 准备反例和期望：dry-run 前后目标 DB/Artifact hash 不变；源变更阻断 apply；密钥只输出待绑定引用。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/migration/test_import_plan.py
```

- [ ] 定义 ImportManifest/SourceIdentity/Mapping/ImportReport 和来源 schema/版本/hash/范围。
- [ ] 只读受控导出包，核对配置/数据/DSL/历史 counts、license、路径与压缩/大小限制。
- [ ] 生成逐条 created/reused/rejected/conflicted 计划、未知字段与凭据重绑定清单，不运行旧代码。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t07"></a>
### M7-T07：迁移计划 →版本化资源/历史

**依赖：** M7-T06, M3-T02, M5-T06, M6-T07。**估算：** 4-6 人日。**范围：** 配置→数据→Scenario/Skill→Run/Score/Artifact，历史不可分发。

**需求关联：** M7-G11, M7-G12, M7-G13；**主责验收：** M7-A10。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/migration/apply.py`。
- 新增计划 `packages/sdk-python/motte_sdk/migration/transforms.py`。
- 新增计划 `packages/storage/motte_storage/imports.py`。
- 修改 `packages/sdk-python/motte_sdk/dispatcher.py`。

**测试：** `tests/migration/test_legacy_import.py`；主断言名 `test_historical_import_never_claimed_or_filled_from_present`（Web 使用同义描述）。

- [ ] 准备反例和期望：只一条 legacy summary=.5 无明细不能造两题；模型未知保留 null；导入 completed Run 不被 Worker claim。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/migration/test_legacy_import.py
```

- [ ] 按配置→数据→Scenario/Skill→Run/Trial/Score/Artifact/Baseline 来源顺序转换。
- [ ] 历史 origin=imported 且不可分发，保留 raw schema/hash/ID 映射；不可表达 DSL 不发布。
- [ ] 工件暂存验证后与引用提交，不导入进行中 Job，不迁真实凭据，不从当前 Profile 补历史事实。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t08"></a>
### M7-T08：中断/冲突 →可恢复导入

**依赖：** M7-T07。**估算：** 3-5 人日。**范围：** checkpoint、幂等、冲突、引用核对与受限回退。

**需求关联：** M7-G10, M7-G14；**主责验收：** M7-A09, M7-A12, M7-A13。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/migration/apply.py`。
- 新增计划 `packages/sdk-python/motte_sdk/migration/rollback.py`。
- 新增计划 `packages/storage/motte_storage/imports.py`。

**测试：** `tests/migration/test_import_resume_rollback.py`；主断言名 `test_import_resume_conflict_and_shared_artifact_rollback`（Web 使用同义描述）。

- [ ] 准备反例和期望：半批崩溃恢复无重复映射；同键内容变更 conflict；回退不能删除被其他 Run 引用的 Artifact。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/migration/test_import_resume_rollback.py
```

- [ ] 唯一 source_system/type/id/version/transform key 与 checkpoint 按批事务提交。
- [ ] resume 仅未提交单元；对照 counts/引用/hash 与原始/归一化评分，冲突停止本单元。
- [ ] rollback 仅本批新建且未共享资源或标 inactive，来源保持只读且保留审计。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t09"></a>
### M7-T09：存量 DB/工件 →一致备份恢复

**依赖：** M1-T03, M7-T06。**估算：** 4-6 人日。**范围：** maintenance mode、引用清单、staging restore、hash 检查。

**需求关联：** M7-G15, M7-G16；**主责验收：** M7-A14, M7-A15, M7-A16。

**文件与职责落位：**

- 修改 `packages/storage/motte_storage/maintenance.py`。
- 修改 `packages/storage/motte_storage/artifacts.py`。
- 修改 `packages/sdk-python/motte_sdk/service.py`。
- 修改 `apps/api/app/main.py`。
- 修改 `apps/worker/motte_worker/runtime.py`。

**测试：** `tests/integration/test_backup_restore_consistency.py`, `tests/storage/test_maintenance.py`；主断言名 `test_backup_writers_gc_and_staging_restore`（Web 使用同义描述）。

- [ ] 准备反例和期望：备份期间资源发布/评分/GC 受维护边界控制；缺引用工件恢复失败；未决 Run 恢复不执行。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/integration/test_backup_restore_consistency.py
uv run pytest -q -m "not live" tests/storage/test_maintenance.py
```

- [ ] 扩展现有 SQLite backup API 并补 PG 一致快照，按 DB 快照引用清单复制 hash 校验工件。
- [ ] 维护模式阻止全部写入口/采集/评分/GC，记录 schema/app/保留 pin；默认秘密不入普通备份。
- [ ] staging 新库/root 校验后只读检查，禁自动派发；覆盖现有环境须独立操作确认，旧 restore 覆盖默认行为需显式兼容处理。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t10"></a>
### M7-T10：本地/远程服务 →安全发布边界

**依赖：** M1-T06, M1-T08, M6-T07, M7-T09。**估算：** 4-6 人日。**范围：** Origin/认证、凭据脱敏、retention pin/GC、供应链检查。

**需求关联：** M7-G17, M7-G18, M7-G19；**主责验收：** M7-A17, M7-A18。

**文件与职责落位：**

- 修改 `apps/api/app/main.py`。
- 修改 `apps/api/app/dependencies.py`。
- 修改 `packages/storage/motte_storage/maintenance.py`。
- 修改 `packages/storage/motte_storage/artifacts.py`。
- 修改 `infra/docker-compose.yml`。
- 修改 `.github/workflows/ci.yml`。

**测试：** `tests/security/test_single_user_boundary.py`；主断言名 `test_origin_auth_redaction_and_retention_pins`（Web 使用同义描述）。

- [ ] 准备反例和期望：不可信 Origin/Host 不能创建 Run 或改凭据；baseline pin 工件不被 TTL 删除；重定向不泄漏模型认证。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/security/test_single_user_boundary.py
```

- [ ] 默认 loopback 并验证 Host/Origin/CSRF/CORS，远程显式单用户认证/TLS，允许的内网 endpoint 与凭据绑定。
- [ ] retention 保护 baseline/pass/needs_review/import，GC 默认 dry-run 与采集/评分/备份锁协调，删除留 tombstone。
- [ ] 覆盖 SSE/日志/报告/配置/Artifact 脱敏，固定依赖来源并审计秘密/私有数据/代码与数据许可。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t11"></a>
### M7-T11：当前版本 →发布候选

**依赖：** M7-T04, M7-T05, M7-T08, M7-T10, M1-T10, M2-T10, M3-T10, M4-T09, M5-T12, M6-T11。**估算：** 4-6 人日。**范围：** 空库/升级/真实 Compose/平台矩阵/回退/示例验证。

**需求关联：** M7-G16, M7-G19, M7-G20, M7-G22；**主责验收：** M7-A19, M7-A20。

**文件与职责落位：**

- 新增计划 `tests/integration/test_release_smoke.py`。
- 修改 `infra/docker-compose.yml`。
- 修改 `.github/workflows/ci.yml`。
- 新增计划 `docs/release/support-matrix.md`。
- 修改 `docs/operations/upgrade.md`。
- 修改 `docs/operations/rollback.md`。

**测试：** `tests/integration/test_release_smoke.py`；主断言名 `test_release_clean_install_upgrade_restore_matrix`（Web 使用同义描述）。

- [ ] 准备反例和期望：干净安装→migration→health→合成 Run→报告成功；旧 schema 升级和失败恢复可读；跳过不算支持。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/integration/test_release_smoke.py
```

- [ ] 固定 RC commit/lock/镜像/包，真实 Compose build/up 与 SQLite/PG 两套验证。
- [ ] 按 Linux/WSL2/实际验证环境记录能力，执行上一支持版本升级、未知新状态旧应用拒绝与备份恢复演练。
- [ ] 检查每 backend 指定版本的 fixture/integration/live 证据、示例命令、发布材料与未达条件，不把 compose config 当启动。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m7-t12"></a>
### M7-T12：替代场景与迁移结果 →切换记录

**依赖：** M7-T11, M2-T11, M4-T10, M4-T11。**估算：** 3-5 人日。**范围：** 三场景验收、旧功能归类、只读旧库、回退窗口和最终 release notes。

**需求关联：** M7-G21, M7-G22, M7-G23；**主责验收：** M7-A21。

**文件与职责落位：**

- 新增计划 `tests/integration/test_cutover_readiness.py`。
- 新增计划 `docs/migration/legacy-capability-map.md`。
- 新增计划 `docs/release/cutover.md`。
- 新增计划 `docs/release/release-notes.md`。
- 新增计划 `docs/operations/sdk-and-cli.md`。
- 新增计划 `docs/operations/import-legacy.md`。
- 修改 `docs/operations/backup-restore.md`。
- 新增计划 `docs/verification/M7.md`。

**测试：** `tests/integration/test_cutover_readiness.py`；主断言名 `test_cutover_three_scenarios_and_all_items_accounted`（Web 使用同义描述）。

- [ ] 准备反例和期望：模型比较/Harbor/Scenario Skill 回归少一个不 ready；历史 unknown 不补齐；全部77工作包均有独立结论。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/integration/test_cutover_readiness.py
```

- [ ] 核对三替代场景、恢复证据、历史迁移 counts/hash 和旧能力已替代/迁移/后置/不迁移清单。
- [ ] 安排冻结旧新增/停新付费任务/存量终态/只读备份/dry-run/apply/核对/新任务单路切换与回退窗口。
- [ ] 全七阶段最终 review 修复复验，发布实际支持矩阵/限制/迁移报告；不自动归档旧仓库或覆盖生产，追加项未实现不声称全部完成。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M7.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

## 3. 全部目标到任务的覆盖表

目标复选框保留未完成；该表证明规划覆盖，不证明功能已实现。

| 目标 | 原始要求 | 责任工作包 |
|---|---|---|
| M7-G01 | Python 同步客户端有稳定类型、错误、版本兼容和能力协商。 | [M7-T01](M7.md#m7-t01) |
| M7-G02 | 提交、等待、取消、retry、事件、指定 pass 报告、比较和 Gate 都能通过新 API 使用。 | [M7-T01](M7.md#m7-t01)、[M7-T02](M7.md#m7-t02)、[M7-T03](M7.md#m7-t03) |
| M7-G03 | 客户端重试不会无条件重复付费 POST；创建请求幂等键与内容一致性可验证。 | [M7-T01](M7.md#m7-t01) |
| M7-G04 | 远程服务不可用时 CLI 明确失败，不静默转为本地执行。 | [M7-T03](M7.md#m7-t03) |
| M7-G05 | SSE 重连可恢复游标、补齐缺口，终态与最后事件不丢失。 | [M7-T02](M7.md#m7-t02) |
| M7-G06 | pytest/CI 默认不触发付费评测，启用真实评测需明确授权。 | [M7-T04](M7.md#m7-t04) |
| M7-G07 | JSON/JUnit/退出码保留质量失败、证据不足、不可比、执行错误与安全区别。 | [M7-T04](M7.md#m7-t04) |
| M7-G08 | wheel 和 Web 构建在干净环境可安装，运行不依赖开发者 cwd/PYTHONPATH。 | [M7-T05](M7.md#m7-t05) |
| M7-G09 | 每个工作区包声明真实依赖，Runner 依赖不污染 API 运行环境。 | [M7-T05](M7.md#m7-t05) |
| M7-G10 | 旧资源导入支持 dry-run、来源/版本映射、逐条诊断和幂等重跑。 | [M7-T06](M7.md#m7-t06)、[M7-T08](M7.md#m7-t08) |
| M7-G11 | 凭据只迁引用，不复制密钥、登录会话或整个本机配置目录。 | [M7-T06](M7.md#m7-t06)、[M7-T07](M7.md#m7-t07) |
| M7-G12 | 历史 Run/Score/Artifact 以只读来源记录导入，不进入执行队列。 | [M7-T07](M7.md#m7-t07) |
| M7-G13 | 缺失模型、评分、费用、许可或证据明确 unknown，不从当前配置反推历史。 | [M7-T07](M7.md#m7-t07) |
| M7-G14 | 迁移中断恢复和回退只处理本次新增资源，不删除共享工件或覆盖既有记录。 | [M7-T08](M7.md#m7-t08) |
| M7-G15 | 数据库与 Artifact 备份属于一致快照，恢复后引用与 hash 可核对。 | [M7-T09](M7.md#m7-t09) |
| M7-G16 | 升级、回退、运行中断与旧 schema 读取在支持环境完成演练。 | [M7-T09](M7.md#m7-t09)、[M7-T11](M7.md#m7-t11) |
| M7-G17 | 默认本地访问安全；远程部署具备显式认证与加密传输，不裸露执行接口。 | [M7-T10](M7.md#m7-t10) |
| M7-G18 | 保留/删除政策保护基线、已固定结果和 needs_review 证据，删除有审计。 | [M7-T10](M7.md#m7-t10) |
| M7-G19 | 公开发布前完成密钥、私有数据、依赖与数据许可检查。 | [M7-T10](M7.md#m7-t10)、[M7-T11](M7.md#m7-t11) |
| M7-G20 | 每个正式支持的后端有指定版本和环境证据，跳过测试不计为通过。 | [M7-T11](M7.md#m7-t11) |
| M7-G21 | 三个旧平台替代场景全部验收，未迁移功能有明确归类。 | [M7-T12](M7.md#m7-t12) |
| M7-G22 | 发布文档、示例、兼容矩阵和实际命令一致，不声称所有后端均稳定。 | [M7-T11](M7.md#m7-t11)、[M7-T12](M7.md#m7-t12) |
| M7-G23 | 功能切换与历史迁移分别验收，不以迁移行数代替产品可用性。 | [M7-T12](M7.md#m7-t12) |

## 4. 全部验收场景的执行责任

每行必须在实现阶段记录真实测试/操作证据；矩阵中主责包需写反例，最终交付包汇总复核。跨端/真实环境场景不能由纯函数测试替代。

| 验收 ID | 注入条件 | 必须观察 | 主责工作包 |
|---|---|---|---|
| M7-A01 | SDK 在干净 venv 安装 | 可导入与调用，无源码/PYTHONPATH 依赖 | [M7-T05](M7.md#m7-t05) |
| M7-A02 | 422/认证失败/重试 POST | 分类明确，禁止无条件重复提交付费请求 | [M7-T01](M7.md#m7-t01) |
| M7-A03 | 服务端已创建 Run、响应丢失 | 同幂等键重送得到同一 Run | [M7-T01](M7.md#m7-t01) |
| M7-A04 | 远程服务断开 | CLI 失败，不静默在本地执行 | [M7-T03](M7.md#m7-t03) |
| M7-A05 | SSE 重复、断线、终态同时到达 | 去重/续读/最终报告一致，缺口显式提示 | [M7-T02](M7.md#m7-t02) |
| M7-A06 | 安装 pytest 插件但未启用评测 | 零模型/Benchmark 调用 | [M7-T04](M7.md#m7-t04) |
| M7-A07 | JUnit 导出证据不足或空集 | error/明确状态，不生成假通过 | [M7-T04](M7.md#m7-t04) |
| M7-A08 | dry-run 后源文件改变 | apply 前重新校验并阻断旧计划 | [M7-T06](M7.md#m7-t06) |
| M7-A09 | 重复导入/相同 ID 内容不同 | reused/conflict 区分，无覆盖 | [M7-T08](M7.md#m7-t08) |
| M7-A10 | 缺旧 gold/模型身份/工件 | unknown/不足，不能从当前配置补历史 | [M7-T07](M7.md#m7-t07) |
| M7-A11 | 恶意路径/压缩炸弹/秘密字段 | 受控拒绝或脱敏，不读取任意宿主路径 | [M7-T06](M7.md#m7-t06) |
| M7-A12 | 导入一半崩溃 | 从 checkpoint 恢复，不重复关联或收费 | [M7-T08](M7.md#m7-t08) |
| M7-A13 | 回退批次使用共享 Artifact | 不删除其他 Run 的引用对象 | [M7-T08](M7.md#m7-t08) |
| M7-A14 | 备份时发布模型/评分/GC 写入 | 维护边界阻断或快照协议保持一致 | [M7-T09](M7.md#m7-t09) |
| M7-A15 | 恢复包缺一个被引用文件 | 恢复校验不通过，不标完整成功 | [M7-T09](M7.md#m7-t09) |
| M7-A16 | 备份含未决/进行中 Run | 恢复不自动付费执行，保持待复核 | [M7-T09](M7.md#m7-t09) |
| M7-A17 | 不可信网页请求本地任务接口 | 安全边界拒绝，不触发执行 | [M7-T10](M7.md#m7-t10) |
| M7-A18 | 清理过期但被 baseline pin 的证据 | 保留并说明原因 | [M7-T10](M7.md#m7-t10) |
| M7-A19 | 旧应用读取新状态或迁移失败 | 明确不兼容/恢复路径，不误当 queued | [M7-T11](M7.md#m7-t11) |
| M7-A20 | 一个后端 live 未验证/测试跳过 | 矩阵标未验证，不计作稳定支持 | [M7-T11](M7.md#m7-t11) |
| M7-A21 | 三个替代场景缺任意一个 | cutover readiness 不通过 | [M7-T12](M7.md#m7-t12) |

## 5. 阶段验证、review 与退出门

focused 全绿后执行相邻回归；这些路径在核对基线存在：

```bash
uv run pytest -q -m "not live" tests/storage/test_maintenance.py tests/storage/test_artifacts.py tests/cli/test_json_output.py tests/api/test_credentials_api.py tests/api/test_events.py tests/runtime/test_worker_restart_process.py tests/test_workspace_health.py
uv run ruff check .
uv run mypy packages/contracts
pnpm --dir apps/web test
pnpm --dir apps/web build
make check
```

API 变化还要 `make openapi` 并审阅 `api/openapi.json` / `apps/web/src/api/schema.d.ts` 差异；生成结果应与代码同提交。`make check` 已含 `pnpm -r test`，CI 还含依赖审计、配置扫描和生成类型漂移检查，不能声称两者完全等价。PG/Docker/live 依赖缺失记 blocked 或 not_run，skip 不计通过。

阶段退出条件：干净安装、SQLite/PG 与声明环境演练、三替代场景、迁移对账、恢复、安全、支持矩阵齐备；功能切换与历史导入分别验收。

- [ ] 需求 review：第 3/4/6 节每项有对应代码、测试与证据，所有新增/增强项保留状态。
- [ ] 契约 review：上游输入和下游消费一致；版本/缺失/身份/状态/评分不可变，无重复注册表或新调度主权。
- [ ] 故障 review：创建、dispatch、外部完成、采集、评分提交、取消与恢复边界都有注入，调用次数/资源所有权可核验。
- [ ] 安全 review：gold/秘密隔离、输入限额、路径/权限/费用政策及不可观察范围有具体断言。
- [ ] 用户链路 review：API/CLI/Web 对同一 Run/pass 一致，禁用与错误原因、键盘/空态/长日志可用。
- [ ] 交付 review：文档/兼容矩阵/操作命令/回退与代码一致；未验证外部组合不宣传 supported。
- [ ] 将 review 缺陷登记为 R-ID、严重度、复现、责任包、修复 commit、复验命令和结果。P0/P1/P2 修复后重跑受影响 focused、相邻回归及阶段门禁；P3 也应关闭或明确记录不影响退出条件的理由。未满足任一必备退出项不得通过。

实现时新增/更新的文档：

- `docs/operations/sdk-and-cli.md`
- `docs/operations/import-legacy.md`
- `docs/operations/backup-restore.md`
- `docs/operations/upgrade.md`
- `docs/operations/rollback.md`
- `docs/migration/legacy-capability-map.md`
- `docs/release/support-matrix.md`
- `docs/release/cutover.md`
- `docs/release/release-notes.md`
- `docs/verification/M7.md`

回退遵循需求文档：先禁止新能力创建并确认存量执行/评分状态，再停用对应 backend/adapter/policy；保留 schema、原始证据和不可变历史，不删除未知活跃工作目录。涉及导入/恢复时仅操作明确归属资源。

下游交接：形成 release notes、未迁移能力分类、支持矩阵、只读旧平台与可执行回退手册。

## 6. 非编号要求覆盖与计划 review

不能只核对 G/T/A 数量。下表把原需求的所有二级章节交给责任工作包；章节内的字段、不变量、接口、样例、运行约束、验证和回退均属于这些工作包的验收范围。原 Spec 随本计划一起交付，完整内容不被缩减成表格摘要。

| 原需求章节 | 责任工作包 | review 重点 |
|---|---|---|
| 第 1 节：依赖与范围 | [M7-T01](M7.md#m7-t01)、[M7-T05](M7.md#m7-t05)、[M7-T09](M7.md#m7-t09)、[M7-T10](M7.md#m7-t10)、[M7-T12](M7.md#m7-t12) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 2 节：最终具体目标清单 | [M7-T01](M7.md#m7-t01)、[M7-T02](M7.md#m7-t02)、[M7-T03](M7.md#m7-t03)、[M7-T04](M7.md#m7-t04)、[M7-T05](M7.md#m7-t05)、[M7-T06](M7.md#m7-t06)、[M7-T07](M7.md#m7-t07)、[M7-T08](M7.md#m7-t08)、[M7-T09](M7.md#m7-t09)、[M7-T10](M7.md#m7-t10)、[M7-T11](M7.md#m7-t11)、[M7-T12](M7.md#m7-t12) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 3 节：模块与文件范围 | [M7-T01](M7.md#m7-t01)、[M7-T02](M7.md#m7-t02)、[M7-T03](M7.md#m7-t03)、[M7-T04](M7.md#m7-t04)、[M7-T05](M7.md#m7-t05)、[M7-T06](M7.md#m7-t06)、[M7-T07](M7.md#m7-t07)、[M7-T08](M7.md#m7-t08)、[M7-T09](M7.md#m7-t09)、[M7-T10](M7.md#m7-t10)、[M7-T11](M7.md#m7-t11) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 4 节：Python 客户端与 CLI 契约 | [M7-T01](M7.md#m7-t01)、[M7-T02](M7.md#m7-t02)、[M7-T03](M7.md#m7-t03) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 5 节：pytest、Trace 与 CI 导出 | [M7-T04](M7.md#m7-t04) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 6 节：历史迁移模型与流程 | [M7-T06](M7.md#m7-t06)、[M7-T07](M7.md#m7-t07)、[M7-T08](M7.md#m7-t08) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 7 节：备份、恢复、升级与证据保留 | [M7-T09](M7.md#m7-t09)、[M7-T10](M7.md#m7-t10)、[M7-T11](M7.md#m7-t11) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 8 节：单用户安全与供应链边界 | [M7-T10](M7.md#m7-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 9 节：构建、CI 与支持矩阵 | [M7-T05](M7.md#m7-t05)、[M7-T11](M7.md#m7-t11) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 10 节：详细实施工作包 | [M7-T01](M7.md#m7-t01)、[M7-T02](M7.md#m7-t02)、[M7-T03](M7.md#m7-t03)、[M7-T04](M7.md#m7-t04)、[M7-T05](M7.md#m7-t05)、[M7-T06](M7.md#m7-t06)、[M7-T07](M7.md#m7-t07)、[M7-T08](M7.md#m7-t08)、[M7-T09](M7.md#m7-t09)、[M7-T10](M7.md#m7-t10)、[M7-T11](M7.md#m7-t11)、[M7-T12](M7.md#m7-t12) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 11 节：强制验收矩阵 | [M7-T11](M7.md#m7-t11)、[M7-T12](M7.md#m7-t12) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 12 节：发布与旧平台切换清单 | [M7-T12](M7.md#m7-t12) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 13 节：验证命令与交付文件 | [M7-T11](M7.md#m7-t11)、[M7-T12](M7.md#m7-t12) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 14 节：固定来源 | [M7-T06](M7.md#m7-t06)、[M7-T12](M7.md#m7-t12) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |

计划 review 结论及修订见 [REVIEW.md](REVIEW.md)。本次只完成文档审阅，以上所有实现步骤与阶段退出门保持未勾选。
