# M1 原生 Agent 与通用评分 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. 本次只完成规划与计划 review；后续逐工作包实现，不将本文件作为付费调用、生产切换或自动合并的授权。

**Goal:** 完整交付本阶段需求文档中的全部目标、工作包及验收场景，包括明确列出的追加/增强项。

**Architecture:** 复用现有 RunDispatcher、CaseAttempt、不可变资源和 ScoringPass。契约由责任阶段维护，后续阶段增量消费；错误、缺失、取消和不确定结果分别保留。

**Tech Stack:** Python 3.12 / uv / FastAPI / Pydantic；React / Vite / pnpm 9.15.0 / Node 24；SQLite / PostgreSQL；按任务需要使用受控 Docker 或独立 Runner。

**Spec:** [M1-native-agent-and-evaluation.md](../../../roadmap/M1-native-agent-and-evaluation.md) 全文；[执行总安排](README.md)与[需求索引](../../../roadmap/README.md)。

**核对基线:** `b6c402b62f0a19f79cde719af60c8bb73672c877`；计划状态 `planned`，实施验证 `not_run`。当前仓库事实是静态代码核对，不是本次 runtime 测试通过证明。

## Global Constraints

- “保持单用户、API-first、Python/FastAPI/Pydantic、React/Vite、现有 uv/pnpm workspace。”
- “Run、Case、Trial、传输重试、操作员 retry 和评分 pass 的身份分开。”
- “已发布资源与终态评分不可覆盖。”
- “MetricResult 是评估视图，持久化仍进入既有 ScoreSet。”
- “默认测试不使用真实模型或个人订阅。” subject、Judge、Harness 的 live 分别授权和计量。
- 遵守 [AGENTS.md](../../../../AGENTS.md) 与 [DESIGN.md](../../../../apps/web/DESIGN.md)。运行状态由 STATUS_META 派生；Radix / Phosphor Icon 后缀；新 token 先登记 DESIGN.md 再加 :root。
- 各任务的路径是仓库相对路径；“修改”表示当前存在，“新增计划”表示尚不存在，最终落位在开工时重新核对。新 schema 沿实际 migration head 追加，不预占 revision。
- 下文测试命令为将来执行步骤，缺文件/ImportError 只说明尚未实现，不能作为业务红灯或通过记录。先写可收集的断言测试，红灯必须对应列出的行为缺口。

## Review Focus

- 旧 Observation 与新证据视图名称碰撞，兼容 roundtrip 由 T01 验证。
- 同 Case 多指标的存储唯一键，三 store 与指定 pass 读取由 T03 验证。
- second-chance 引发副作用重做，崩溃与调用计数由 T07 验证。
- symlink 在检查后替换，受控路径与 TOCTOU 由 T06 验证。
- regex 阻塞或轨迹不完整时误判，执行期限及不足证据由 T02 验证。

## 1. 进入条件与分批安排

现有 M0 与 Direct LLM v2 契约；不需要真实密钥。

1. T01 → T02 → T03：先稳定证据与多指标存储。
2. T04 → T05 → T06 → T07 → T08：模型、工具、预算、调用日志和冻结证据。
3. T09 → T10：公共入口、三类代表任务与阶段 review。

工程量估算 27–45 人日，包含实现、focused 回归和工作包 review，不包含等待许可证据、预算批准、外部环境与人工标注。它是初步排期范围，不是交付承诺；完成前两包后按实际吞吐重新估计。每个工作包一般拆 1–3 个可独立测试的提交，固定“一项主功能 + 一项验证”的在制品限制。

## 2. 工作包实施卡

接口字段、错误与不变量以本阶段 Spec 第 3 节及后续契约章节为输入；下方“消费 → 产出”明确交接边界。远期 Python 私有函数签名不预先锁死；实际开工时先核对已合入公共契约并补可执行单测，再实现。每张卡都须完成所有步骤，不能用最后一个集成测试替代前置反例。

<a id="m1-t01"></a>
### M1-T01：现有 Case/Score → Observation/MetricResult

**依赖：** 当前主干完整性基线。**估算：** 2-3 人日。**范围：** 演进契约，定义 hash 和状态映射，保留旧读兼容。

**需求关联：** M1-G11；**主责验收：** 使用本卡专属反例与阶段集成验收，不借用其他工作包的通过状态。

**文件与职责落位：**

- 修改 `packages/contracts/motte_contracts/evidence.py`。
- 新增计划 `packages/contracts/motte_contracts/evaluation.py`。
- 修改 `packages/contracts/motte_contracts/compat.py`。
- 修改 `packages/contracts/motte_contracts/identity.py`。

**测试：** `tests/contract/test_observation_v1.py`；主断言名 `test_legacy_and_multimetric_roundtrip`（Web 使用同义描述）。

- [ ] 准备反例和期望：旧 name/value Observation 往返不变；一个 Case 两个 metric 合法，完全重复的 metric key 拒绝；NaN 与外部 evidence ref 拒绝。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/contract/test_observation_v1.py
```

- [ ] 保留旧 Observation 读协议，以显式 schema_version 演进证据视图，不复用 name 字段冒充 Case 身份。
- [ ] 定义 MetricResult、EvaluatorSpec 和 score key=(case_id,trial_id,metric_id,evaluator_id,version)，旧 Score 只作兼容投影。
- [ ] 对冻结 JSON 与 Artifact hash 清单做规范 hash，缺证据以 coverage 表示，输出版本化协议文档。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t02"></a>
### M1-T02：Observation →确定性指标

**依赖：** M1-T01。**估算：** 3-5 人日。**范围：** 注册上述评分器、分母政策、输入与耗时上限。

**需求关联：** M1-G11, M1-G12；**主责验收：** M1-A04。

**文件与职责落位：**

- 修改 `packages/evaluators/motte_eval/scorer_registry.py`。
- 修改 `packages/evaluators/motte_eval/deterministic.py`。
- 修改 `packages/evaluators/motte_eval/direct_llm.py`。
- 新增计划 `packages/evaluators/motte_eval/observation.py`。
- 新增计划 `packages/evaluators/motte_eval/files.py`。
- 新增计划 `packages/evaluators/motte_eval/tools.py`。
- 修改 `packages/contracts/motte_contracts/jsonschema.py`。

**测试：** `tests/evaluators/test_deterministic_matrix.py`；主断言名 `test_deterministic_success_fail_missing_error`（Web 使用同义描述）。

- [ ] 准备反例和期望：每个评分器各测成功/有效失败/缺证据/异常；regex 灾难回溯期限可终止；不完整工具轨迹不能证明未调用。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/evaluators/test_deterministic_matrix.py
```

- [ ] 复用现有封闭注册策略与 exact/contains/regex 兼容语义，不开放任意模块 import。
- [ ] 增加 JSON Schema、文件存在/内容、退出码、tool-call、no-forbidden-write，逐指标隔离异常。
- [ ] 规定 max_input_bytes 和执行期限，regex 放可终止的执行边界，保留 no_expectation 与每指标分母。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t03"></a>
### M1-T03：多指标 → ScoringPass

**依赖：** M1-T01, M1-T02。**估算：** 3-5 人日。**范围：** 连接现有评分入口，多指标 key 不冲突，追加不可变历史。

**需求关联：** M1-G11, M1-G13, M1-G17；**主责验收：** M1-A10, M1-A14。

**文件与职责落位：**

- 修改 `packages/sdk-python/motte_sdk/service.py`。
- 修改 `packages/storage/motte_storage/audit_store.py`。
- 修改 `packages/storage/motte_storage/pg_audit_store.py`。
- 修改 `packages/storage/motte_storage/integrity.py`。
- 修改 `packages/storage/motte_storage/run_store.py`。
- 修改 `packages/storage/motte_storage/models.py`。
- 修改 `apps/worker/motte_worker/reporting.py`。

**测试：** `tests/runtime/test_agent_scoring_passes.py`；主断言名 `test_append_multimetric_without_history_mutation`（Web 使用同义描述）。

- [ ] 准备反例和期望：同一 case 两指标落库后分别可读；rescore 生成新 pass、旧 JSON hash 不变、Provider spy 调用数 0；三种 store 结果一致。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/runtime/test_agent_scoring_passes.py
```

- [ ] 把 case-only ScoreSet 存储与查询扩展为复合身份，旧接口只对 legacy 单指标适用，多指标歧义不得取第一行。
- [ ] 迁移 score_sets 唯一约束并保留旧行可读；SQLite 与 PG 迁移沿实际 head 追加，InMemory 同步。
- [ ] 评分落库、current 指针与 Run 终态沿现有事务更新，GET 指定 pass 只读。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t04"></a>
### M1-T04：固定模型+工具 → AgentBackend

**依赖：** M1-T01。**估算：** 2-4 人日。**范围：** 注册 backend，填真实模型请求，模式显式，样本隔离。

**需求关联：** M1-G01, M1-G02, M1-G03, M1-G04；**主责验收：** M1-A06。

**文件与职责落位：**

- 新增计划 `packages/sdk-python/motte_sdk/agent_backend.py`。
- 修改 `packages/sdk-python/motte_sdk/execution_backends.py`。
- 修改 `packages/sdk-python/motte_sdk/resolve.py`。
- 修改 `packages/agent-runtime/motte_agent/runtime.py`。
- 修改 `packages/contracts/motte_contracts/scenario.py`。

**测试：** `tests/runtime/test_builtin_agent_backend.py`；主断言名 `test_backend_rejects_unsupported_before_call`（Web 使用同义描述）。

- [ ] 准备反例和期望：两 Case 不同输入得到独立会话；不支持 tools 的模型 native 模式创建返回结构化错误且调用数 0。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/runtime/test_builtin_agent_backend.py
```

- [ ] 新增 builtin-agent@1 的 validate/build 并保持未完成接线时 unavailable。
- [ ] 从发布 ModelProfile 构造实际 ModelRequest，冻结模式、system prompt、工具 schema 和预算，不再硬编码 builtin-react 模型名。
- [ ] 以 Case 为边界装配 Provider、workspace、runtime 与 EventSink，复用 Dispatcher 执行权。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t05"></a>
### M1-T05：模型响应 →消息/工具循环

**依赖：** M1-T04。**估算：** 3-4 人日。**范围：** 完整历史、参数校验、错误回灌、多 call_id 关联。

**需求关联：** M1-G02, M1-G06, M1-G07；**主责验收：** M1-A02, M1-A03, M1-A05, M1-A13。

**文件与职责落位：**

- 修改 `packages/agent-runtime/motte_agent/builtin_react.py`。
- 新增计划 `packages/agent-runtime/motte_agent/native_tools.py`。
- 新增计划 `packages/agent-runtime/motte_agent/budget.py`。
- 修改 `packages/contracts/motte_contracts/messages.py`。

**测试：** `tests/runtime/test_agent_tool_history.py`；主断言名 `test_assistant_tool_history_and_duplicate_ids`（Web 使用同义描述）。

- [ ] 准备反例和期望：第二轮请求含上一轮 assistant/tool 消息；两个 tool call 顺序确定；重复 call_id 无二次副作用；工具失败可在预算内恢复。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/runtime/test_agent_tool_history.py
```

- [ ] 为 legacy-json 追加 assistant 历史并版本化 prompt，保持显式模式且无静默降级。
- [ ] native 模式使用现有 canonical tools/tool_calls/tool_call_id，验证参数 schema、未知工具和畸形响应。
- [ ] 每轮及每次调用前检查步骤/工具/单调时钟/可观察 token 与 cost 预算，记录 enforced/observed/unknown。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t06"></a>
### M1-T06：工具请求 →受控文件产物

**依赖：** M1-T04。**估算：** 3-5 人日。**范围：** 沙箱绑定、路径校验、资源限额、前后快照。

**需求关联：** M1-G04, M1-G10, M1-G14；**主责验收：** M1-A07, M1-A12。

**文件与职责落位：**

- 修改 `packages/sandbox/motte_sandbox/tools.py`。
- 修改 `packages/sandbox/motte_sandbox/policy.py`。
- 修改 `packages/sandbox/motte_sandbox/runner.py`。
- 修改 `packages/sandbox/motte_sandbox/docker.py`。
- 新增计划 `packages/sandbox/motte_sandbox/workspace.py`。

**测试：** `tests/sandbox/test_agent_workspace_policy.py`；主断言名 `test_workspace_escape_and_quota_rejection`（Web 使用同义描述）。

- [ ] 准备反例和期望：../、绝对路径、指向宿主的 symlink、设备文件拒绝；同名产物不跨 Case；写入超额触发停止并保留清理结果。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/sandbox/test_agent_workspace_policy.py
```

- [ ] 将工具绑定 Case 的受控 workspace 与权限交集，参数不拼接 shell，默认禁网。
- [ ] 实现读写与产物限额、工作区前后快照，防 TOCTOU 链接替换且不挂载凭据或 Docker socket。
- [ ] 在沙箱/受控子进程实施 CPU/内存/PID/时长及可兑现磁盘限制，不能兑现的限额预检拒绝。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t07"></a>
### M1-T07：多步动作 →调用日志与终止

**依赖：** M1-T05, M1-T06。**估算：** 3-5 人日。**范围：** 持久调用边界、期限、取消、不确定结果与 cleanup。

**需求关联：** M1-G05, M1-G07, M1-G08, M1-G09；**主责验收：** M1-A08, M1-A09。

**文件与职责落位：**

- 新增计划 `packages/contracts/motte_contracts/evaluation.py`。
- 修改 `packages/sdk-python/motte_sdk/service.py`。
- 新增计划 `packages/sdk-python/motte_sdk/agent_backend.py`。
- 新增计划 `packages/storage/motte_storage/invocations.py`。
- 新增计划 `packages/agent-runtime/motte_agent/budget.py`。

**测试：** `tests/runtime/test_agent_interruption.py`；主断言名 `test_crash_after_dispatch_does_not_replay`（Web 使用同义描述）。

- [ ] 准备反例和期望：写文件后在结果提交前杀 Worker，恢复为 needs_review 且调用数不增加；取消模型时不再调工具；超时后无背景写入。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/runtime/test_agent_interruption.py
```

- [ ] 持久化 InvocationRecord 的 prepared/dispatching/settled 边界与请求摘要，模型和工具分别计量。
- [ ] 取消先持久审计再中断进程并收集 cleanup；未决调用保持 indeterminate，不走整段 Agent second-chance。
- [ ] 增加 SDK 重试适用性检查，现有 Direct/GSM8K 已知暂态策略保留，新副作用 backend 默认不可整段重跑。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t08"></a>
### M1-T08：产物+事件 → Observation

**依赖：** M1-T01, M1-T06, M1-T07。**估算：** 2-4 人日。**范围：** 脱敏、完整度、截断、稳定引用、哈希验证。

**需求关联：** M1-G05, M1-G10, M1-G14；**主责验收：** M1-A11。

**文件与职责落位：**

- 修改 `packages/trace/motte_trace/sink.py`。
- 修改 `packages/trace/motte_trace/redaction.py`。
- 修改 `packages/storage/motte_storage/artifacts.py`。
- 新增计划 `packages/evaluators/motte_eval/observation.py`。
- 新增计划 `packages/sdk-python/motte_sdk/agent_backend.py`。

**测试：** `tests/trace/test_agent_observation_capture.py`；主断言名 `test_frozen_capture_missing_and_secret_redaction`（Web 使用同义描述）。

- [ ] 准备反例和期望：模型输入不含 expected；秘密哨兵不出现 SSE/Artifact/异常；冻结后改 workspace 不改变评分；丢工件降 coverage。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/trace/test_agent_observation_capture.py
```

- [ ] 终止后收集稳定事件/文件引用、媒体类型、大小、sha256、可用状态与截断范围。
- [ ] 证据采集失败也生成可解释的 Observation，验证内容 hash，不把未采集等同不存在。
- [ ] 统一脱敏与评分视图的版本/hash，秘密只留安全命中标记，大日志以 Artifact 引用传递。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t09"></a>
### M1-T09：公共 Run → Agent 工作区

**依赖：** M1-T03, M1-T05, M1-T07, M1-T08。**估算：** 3-5 人日。**范围：** API 响应类型、CLI、逐步轨迹和多指标 UI。

**需求关联：** M1-G15；**主责验收：** 使用本卡专属反例与阶段集成验收，不借用其他工作包的通过状态。

**文件与职责落位：**

- 修改 `apps/api/app/main.py`。
- 修改 `apps/api/app/schemas.py`。
- 修改 `packages/cli/motte_cli/main.py`。
- 新增计划 `apps/web/src/evalTypes/agent/AgentPages.tsx`。
- 修改 `apps/web/src/evalTypes/registry.ts`。
- 修改 `apps/web/src/api/client.ts`。

**测试：** `tests/api/test_agent_run_flow.py`, `apps/web/tests/AgentPages.test.tsx`；主断言名 `test_agent_api_cli_web_same_pass`（Web 使用同义描述）。

- [ ] 准备反例和期望：配置失败保留表单；提交真正入队；取消/retry 子 Run/历史 pass 对 API CLI Web 一致；长日志截断提示可见。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/api/test_agent_run_flow.py
pnpm --dir apps/web test -- tests/AgentPages.test.tsx
```

- [ ] 扩展公共 Run 创建/预检/事件/产物/报告类型，生成 OpenAPI 与 TS 类型，新增 Artifact 读取做归属验证。
- [ ] 新增 Agent 操作、监控、样本、结果与并列入口，复用 ModelPicker/BatchMonitor/CaseDrillTable/STATUS_META。
- [ ] 按 DESIGN.md 实现键盘、加载、空态、禁用原因、错误定位；Agent 对比初期并列且不宣称正式可比。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

<a id="m1-t10"></a>
### M1-T10：代表任务 →交付证据

**依赖：** M1-T09。**估算：** 3-5 人日。**范围：** 本地集成、真实 Docker、显式 live、兼容矩阵和运维说明。

**需求关联：** M1-G16, M1-G17, M1-G18；**主责验收：** M1-A01, M1-A15。

**文件与职责落位：**

- 新增计划 `tests/integration/test_native_agent_slice.py`。
- 新增计划 `docs/operations/native-agent.md`。
- 修改 `docs/protocols/provider-compatibility.md`。
- 修改 `docs/PROGRESS.md`。
- 新增计划 `docs/verification/M1.md`。

**测试：** `tests/integration/test_native_agent_slice.py`；主断言名 `test_native_slice_and_old_suite_regression`（Web 使用同义描述）。

- [ ] 准备反例和期望：正常文件任务、读错纠正、越权/无限循环三组闭环；Direct/GSM8K/Replay 原评分和恢复保持；live 缺失不写 supported。
- [ ] 运行 focused 命令，确认断言暴露真实缺口，记录红灯原因；不得因 import/收集失败就跳到完成。

```bash
uv run pytest -q -m "not live" tests/integration/test_native_agent_slice.py
```

- [ ] 先以 scripted Provider 经真实 API/Worker/存储运行三组任务，再独立真实 Docker 校验边界。
- [ ] 备妥两个模型同任务的手动 live 配置与预算、版本、费用记录模板，操作者执行后才更新 supported。
- [ ] 完成阶段全量 review、修复回归、更新协议和操作文档；backend 禁用回退仍读历史。
- [ ] 用同一 focused 命令复验，再跑本阶段第 5 节相邻回归；有 API 变化生成 OpenAPI/TS，有 UI 变化跑完整 Web test/build。
- [ ] 更新第 5 节中本任务相关操作/协议文档，并在 `docs/verification/M1.md` 记录本包命令、commit、环境、结果与证据。完成需求/正确性/安全/兼容 review，修复后复验；仅提交本包及对应文档。

## 3. 全部目标到任务的覆盖表

目标复选框保留未完成；该表证明规划覆盖，不证明功能已实现。

| 目标 | 原始要求 | 责任工作包 |
|---|---|---|
| M1-G01 | Backend Registry 可注册并执行 `builtin-agent@1`，未接线时保持 unavailable。 | [M1-T04](M1.md#m1-t04) |
| M1-G02 | 执行模式、system prompt 版本、工具 schema、预算及模型快照进入 manifest。 | [M1-T04](M1.md#m1-t04)、[M1-T05](M1.md#m1-t05) |
| M1-G03 | native-tool 模式在模型不支持工具时创建期拒绝；不自动切换 legacy-json。 | [M1-T04](M1.md#m1-t04) |
| M1-G04 | 每个样本独立消息历史和 workspace，不继承上一个 Case 的内容。 | [M1-T04](M1.md#m1-t04)、[M1-T06](M1.md#m1-t06) |
| M1-G05 | 所有模型请求、工具请求/结果、拒绝和终止都产生持久证据。 | [M1-T07](M1.md#m1-t07)、[M1-T08](M1.md#m1-t08) |
| M1-G06 | 工具错误能回灌并在预算内恢复，未知工具不能执行。 | [M1-T05](M1.md#m1-t05) |
| M1-G07 | 时长、步数、工具次数、可观察 token 预算生效并有具体停止原因。 | [M1-T05](M1.md#m1-t05)、[M1-T07](M1.md#m1-t07) |
| M1-G08 | 取消可阻断后续模型/工具调用，运行进程与沙箱清理结果可查。 | [M1-T07](M1.md#m1-t07) |
| M1-G09 | 外部副作用可能已经发生时转入 needs_review，不自动重放整个 Agent。 | [M1-T07](M1.md#m1-t07) |
| M1-G10 | 产物具有安全相对路径、媒体类型、大小、内容哈希和可用状态。 | [M1-T06](M1.md#m1-t06)、[M1-T08](M1.md#m1-t08) |
| M1-G11 | 同一 Observation 可产出多个带来源、版本、分母的指标。 | [M1-T01](M1.md#m1-t01)、[M1-T02](M1.md#m1-t02)、[M1-T03](M1.md#m1-t03) |
| M1-G12 | 无期望、证据缺失、不适用、评分器错误不被映射成通过。 | [M1-T02](M1.md#m1-t02) |
| M1-G13 | 历史评分保留，读取报告不重算，离线重评分不调用被测模型。 | [M1-T03](M1.md#m1-t03) |
| M1-G14 | 模型输入不含 gold、隐藏文件断言、评分规则或本机凭据。 | [M1-T06](M1.md#m1-t06)、[M1-T08](M1.md#m1-t08) |
| M1-G15 | Web、CLI、Worker、API 对同一 Run 的状态和分数一致。 | [M1-T09](M1.md#m1-t09) |
| M1-G16 | 正常产物、可恢复错误、越权/预算耗尽三组代表性任务通过验收。 | [M1-T10](M1.md#m1-t10) |
| M1-G17 | 既有 Direct LLM/GSM8K/Replay 的评分与恢复回归不变。 | [M1-T03](M1.md#m1-t03)、[M1-T10](M1.md#m1-t10) |
| M1-G18 | 支持声明附版本、环境、fixture/integration/live 证据及已知限制。 | [M1-T10](M1.md#m1-t10) |

## 4. 全部验收场景的执行责任

每行必须在实现阶段记录真实测试/操作证据；矩阵中主责包需写反例，最终交付包汇总复核。跨端/真实环境场景不能由纯函数测试替代。

| 验收 ID | 注入条件 | 必须观察 | 主责工作包 |
|---|---|---|---|
| M1-A01 | 正常读输入、写 report、final | 文件断言通过，工具和文件证据完整 | [M1-T10](M1.md#m1-t10) |
| M1-A02 | 首次读错文件，第二次纠正 | tool_error 回灌，恢复成功但额外调用计入预算 | [M1-T05](M1.md#m1-t05) |
| M1-A03 | 未注册工具 | 无工具副作用，记录 tool_denied | [M1-T05](M1.md#m1-t05) |
| M1-A04 | 模型只有 final，无实际产物 | execution 可结束，file 指标失败 | [M1-T02](M1.md#m1-t02) |
| M1-A05 | 不断提出工具调用 | 步数或工具预算阻断，明确 stop reason | [M1-T05](M1.md#m1-t05) |
| M1-A06 | native 模式模型不支持 tools | 创建期结构化错误，模型调用数 0 | [M1-T04](M1.md#m1-t04) |
| M1-A07 | 磁盘满/文件超过限额 | 产物不完整、错误分类明确，cleanup 执行 | [M1-T06](M1.md#m1-t06) |
| M1-A08 | 模型调用中取消 | 无后续工具执行；取消和调用不确定性均保留 | [M1-T07](M1.md#m1-t07) |
| M1-A09 | 写文件后进程崩溃 | 不自动重复危险操作，Run needs_review | [M1-T07](M1.md#m1-t07) |
| M1-A10 | 一个评分器异常 | 其他结果保留，异常指标不通过，不覆盖历史 pass | [M1-T03](M1.md#m1-t03) |
| M1-A11 | 轨迹截断或 Artifact 丢失 | no-forbidden-write 不因未看到违规就判通过 | [M1-T08](M1.md#m1-t08) |
| M1-A12 | 两个 Case 使用同一路径名 | workspace 隔离，结果互不污染 | [M1-T06](M1.md#m1-t06) |
| M1-A13 | Provider 不报告 token/cost | null+coverage，不虚构 0 或严格硬预算 | [M1-T05](M1.md#m1-t05) |
| M1-A14 | GET 与 rescore 反复请求 | GET 零模型/评分副作用；每次 rescore 新 pass | [M1-T03](M1.md#m1-t03) |
| M1-A15 | replay/旧 Direct/GSM8K 数据 | 既有公共契约和合法分母不退化 | [M1-T10](M1.md#m1-t10) |

## 5. 阶段验证、review 与退出门

focused 全绿后执行相邻回归；这些路径在核对基线存在：

```bash
uv run pytest -q -m "not live" tests/contract/test_platform_integrity.py tests/runtime/test_dispatcher.py tests/runtime/test_builtin_react.py tests/runtime/test_gsm8k_smoke.py tests/runtime/test_direct_llm_v2_smoke.py tests/storage/test_run_integrity.py
uv run ruff check .
uv run mypy packages/contracts
pnpm --dir apps/web test
pnpm --dir apps/web build
make check
```

API 变化还要 `make openapi` 并审阅 `api/openapi.json` / `apps/web/src/api/schema.d.ts` 差异；生成结果应与代码同提交。`make check` 已含 `pnpm -r test`，CI 还含依赖审计、配置扫描和生成类型漂移检查，不能声称两者完全等价。PG/Docker/live 依赖缺失记 blocked 或 not_run，skip 不计通过。

阶段退出条件：Core 必须通过全部离线、公共入口与实际 Docker 验证；Supported 还需两个已发布模型的同文件任务与取消证据。

- [ ] 需求 review：第 3/4/6 节每项有对应代码、测试与证据，所有新增/增强项保留状态。
- [ ] 契约 review：上游输入和下游消费一致；版本/缺失/身份/状态/评分不可变，无重复注册表或新调度主权。
- [ ] 故障 review：创建、dispatch、外部完成、采集、评分提交、取消与恢复边界都有注入，调用次数/资源所有权可核验。
- [ ] 安全 review：gold/秘密隔离、输入限额、路径/权限/费用政策及不可观察范围有具体断言。
- [ ] 用户链路 review：API/CLI/Web 对同一 Run/pass 一致，禁用与错误原因、键盘/空态/长日志可用。
- [ ] 交付 review：文档/兼容矩阵/操作命令/回退与代码一致；未验证外部组合不宣传 supported。
- [ ] 将 review 缺陷登记为 R-ID、严重度、复现、责任包、修复 commit、复验命令和结果。P0/P1/P2 修复后重跑受影响 focused、相邻回归及阶段门禁；P3 也应关闭或明确记录不影响退出条件的理由。未满足任一必备退出项不得通过。

实现时新增/更新的文档：

- `docs/protocols/observation-evaluation.md`
- `docs/operations/native-agent.md`
- `docs/protocols/provider-compatibility.md`
- `docs/PROGRESS.md`
- `docs/verification/M1.md`

回退遵循需求文档：先禁止新能力创建并确认存量执行/评分状态，再停用对应 backend/adapter/policy；保留 schema、原始证据和不可变历史，不删除未知活跃工作目录。涉及导入/恢复时仅操作明确归属资源。

下游交接：M2/M4/M5 消费 Observation、MetricResult、AgentResult、受控工具及 InvocationRecord；M3 使用预留 trial_id 评分维度。

## 6. 非编号要求覆盖与计划 review

不能只核对 G/T/A 数量。下表把原需求的所有二级章节交给责任工作包；章节内的字段、不变量、接口、样例、运行约束、验证和回退均属于这些工作包的验收范围。原 Spec 随本计划一起交付，完整内容不被缩减成表格摘要。

| 原需求章节 | 责任工作包 | review 重点 |
|---|---|---|
| 第 1 节：起点、依赖与交付边界 | [M1-T04](M1.md#m1-t04)、[M1-T10](M1.md#m1-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 2 节：最终具体目标清单 | [M1-T01](M1.md#m1-t01)、[M1-T02](M1.md#m1-t02)、[M1-T03](M1.md#m1-t03)、[M1-T04](M1.md#m1-t04)、[M1-T05](M1.md#m1-t05)、[M1-T06](M1.md#m1-t06)、[M1-T07](M1.md#m1-t07)、[M1-T08](M1.md#m1-t08)、[M1-T09](M1.md#m1-t09)、[M1-T10](M1.md#m1-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 3 节：模块划分与实现范围 | [M1-T01](M1.md#m1-t01)、[M1-T02](M1.md#m1-t02)、[M1-T03](M1.md#m1-t03)、[M1-T04](M1.md#m1-t04)、[M1-T05](M1.md#m1-t05)、[M1-T06](M1.md#m1-t06)、[M1-T07](M1.md#m1-t07)、[M1-T08](M1.md#m1-t08)、[M1-T09](M1.md#m1-t09) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 4 节：关键契约与数据流 | [M1-T01](M1.md#m1-t01)、[M1-T03](M1.md#m1-t03)、[M1-T04](M1.md#m1-t04)、[M1-T05](M1.md#m1-t05)、[M1-T07](M1.md#m1-t07)、[M1-T08](M1.md#m1-t08) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 5 节：确定性评分器范围 | [M1-T02](M1.md#m1-t02) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 6 节：预算、错误与安全策略 | [M1-T05](M1.md#m1-t05)、[M1-T06](M1.md#m1-t06)、[M1-T07](M1.md#m1-t07) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 7 节：分批实施任务 | [M1-T01](M1.md#m1-t01)、[M1-T02](M1.md#m1-t02)、[M1-T03](M1.md#m1-t03)、[M1-T04](M1.md#m1-t04)、[M1-T05](M1.md#m1-t05)、[M1-T06](M1.md#m1-t06)、[M1-T07](M1.md#m1-t07)、[M1-T08](M1.md#m1-t08)、[M1-T09](M1.md#m1-t09)、[M1-T10](M1.md#m1-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 8 节：验收测试矩阵 | [M1-T10](M1.md#m1-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 9 节：API、CLI 与 UI 目标 | [M1-T09](M1.md#m1-t09) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 10 节：验证命令、发布与回退 | [M1-T03](M1.md#m1-t03)、[M1-T10](M1.md#m1-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |
| 第 11 节：固定来源 | [M1-T10](M1.md#m1-t10) | 与原文逐段核对，变更语义必须版本化并记录消费者影响 |

计划 review 结论及修订见 [REVIEW.md](REVIEW.md)。本次只完成文档审阅，以上所有实现步骤与阶段退出门保持未勾选。
