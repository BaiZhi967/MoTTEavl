# M5 开工与交接

状态：planned。本次交付计划，不实施 M5。主执行文件是 [M5 详细执行计划](2026-09-21-m5-execution.md)，可直接转交的全文提示词见 [M5 development agent](../../prompts/M5-development-agent.md)。旧的 2026-09-19 M5 计划保留为历史推导。

## 基线核对

用户于 2026-09-21 明确要求先合并主干并推送，再交付 M5 开发提示词。M4 已快进合入并推送至 `origin/main`，实际合入点为 `adf468aec4b46ab5f17db7ded56354d93dd2673e`；该提交的 [完整 Linux/PG CI](https://github.com/BaiZhi967/MoTTEavl/actions/runs/35556677158) 已通过。之后的主干提交可包含交接文档更新。

M5 **可以立即开展开发与离线验证**，不因 M4 仍为 implementation_complete/live_pending 而只做只读准备或再次请求开工确认。M4 的真实模型支持仍未验收，不能把合入等同于 live 通过；实际记录见 [M4 完成复核](../../verification/M4-completion-review-2026-09-21.md)。只有具体工作确实依赖未验证能力时才隔离该依赖并继续其他包。开工检查最新主干包含上述合入点，记录实际起始 SHA、工作区和 Alembic head；保护用户修改，在 `codex/m5-scenarios-skills-judges` 隔离工作区实施。

M4 的交互 runtime 不等于已经存在 M5 TargetSession，native steer 也不等于通用业务多轮会话。先给 Builtin 实现真正的持续上下文；其余 runtime 按实现过的能力逐一适配，不能用 capability 布尔值替代消费者。

## 第一批交付

1. 阅读需求全文、共通约束、执行计划，再逐项核对当前公共契约和三种存储实现。
2. T01：固定七种 step、受限条件、嵌套预算、Workflow/Fixture 版本与诊断式旧 DSL 转换。不得引入 eval、任意 shell 或兼容时静默丢步骤。
3. T02：实现目标可见数据与 checker 私有真值隔离，以及可验证的清理所有权。
4. T03：先以假 Provider 的真实 Builtin runtime 证明历史跨 send 保留、预算累计，再验证真实中断与停止确认。
5. T04/T05：接上确定性断言、公共 Run/CaseAttempt/Observation/ScoringPass 纵向链路。完成这些后再推进 Skill/Judge。

每包按“反例红灯 → 最小实现 → 回归绿灯 → 规格审查 → 质量审查 → 修复 → 提交”交付。一个主实现包搭配只读验证；共享契约、存储和 API 文件指定单一写入负责人。

## 外部验收条件

- 旧项目源码已按固定提交作只读核对：`BaiZhi967/llm_agent__evaluation_platform@b661bcdf83e1c3dfb8d6062ee78817d249e86a4c` 的 `workers/scenario-runner/src/scenario_runner/engine.py`。使用前重新核验来源与许可；不直接继承其中的 shell/eval/调用后超时实现。

  本次取得文件为 22,561 字节，SHA256 `11093949c0e9dee75c52217ac6d1ca147249b64670f78037884e503fd8880a94`；这是只读来源收据，不是代码迁移或许可通过记录。
- 校准必须来自至少 30 个人工复核样本，记录 rubric、复核人标识、时间和裁决依据；Agent 自评或生成标签不能充当人审。
- Subject/Judge 分别需要明确模型、凭据引用和费用或调用上限。没有授权则运行离线 fake/scripted 验证，live 留为 not_run；不得偷用个人登录态。
- PostgreSQL 与目标平台进程测试需要真实环境。环境缺失时继续独立工作，保留对应验收缺口，不降低要求。

完成矩阵以执行计划的 G01–G22、T01–T12、A01–A18 为分母；不得只交第一条 happy path 或把三组 Skill 对照、校准、人审、API/CLI/Web 和完整验收挪出 M5。
