[project]
name = "motte-eval"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "motte-contracts",
    "jsonschema>=4.23,<5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["motte_eval"]
