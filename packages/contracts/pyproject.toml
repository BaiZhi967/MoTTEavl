[project]
name = "motte-contracts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cel-python>=0.5,<0.6",
    "pydantic>=2.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["motte_contracts"]
