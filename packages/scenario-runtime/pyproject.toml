[project]
name = "motte-scenario-runtime"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["motte-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["motte_scenario"]
